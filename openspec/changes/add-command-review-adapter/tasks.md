## Implementation Tasks

- [ ] Add JSON/JSONC configuration loading for command adapters with discovery precedence `--config`, `.review-gauntlet/config.jsonc`, `.review-gauntlet/config.json`, `review-gauntlet.jsonc`, `review-gauntlet.json`. (verification: unit - add `tests/test_config.py` covering precedence, missing config, JSON parsing, JSONC comments, JSONC trailing commas, and preservation of comment-like text inside strings)
- [ ] Define typed command adapter configuration models for `adapter.type=command`, argv-array command execution, input mode, output mode, timeout, cwd, env, and supported template variables. (verification: unit - add `tests/test_config.py` cases rejecting unsupported adapter types, shell-string-only command forms, unsupported modes, unknown required fields, invalid timeout values, and unknown template variables)
- [ ] Implement OCR-derived prompt generation for a selected review cell, including file path, content digest, selected rule text, repository context, and the required verdict JSON contract. (verification: unit - add `tests/test_review_prompts.py` snapshot/structure tests asserting the prompt includes selected file/rule context and the OCR-style JSON schema without external credentials)
- [ ] Implement `CommandReviewAdapter` that writes per-cell artifacts, invokes the configured command with `shell=False`, supports stdin and prompt-file input modes, captures stdout/stderr, and enforces timeouts. (verification: integration - add `tests/test_command_review_adapter.py` with local fake command scripts asserting argv execution, cwd handling, stdin prompt delivery, prompt-file delivery, stdout/stderr artifact persistence, and timeout failure behavior)
- [ ] Implement verdict collection and validation for `stdout-json` and `file-json`, validating each comment through the existing OCR comment model before finding normalization. (verification: integration - extend `tests/test_command_review_adapter.py` so fake command scripts produce valid stdout JSON, valid file JSON, empty comments, invalid JSON, missing output files, and schema-invalid comments)
- [ ] Enforce safe artifact and output path handling so configured file output cannot escape the intended run/cell artifact area unless explicitly allowed by documented safe template expansion. (verification: unit - add `tests/test_command_review_adapter.py` path safety cases rejecting traversal and absolute unsafe output paths while allowing `{cell_dir}`/`{output_file}` paths)
- [ ] Wire `review-gauntlet review --config <path>` and default config discovery into `src/review_gauntlet/cli.py`, while preserving `--fixture` as the deterministic fake adapter path. (verification: integration - extend `tests/test_cli_session_review.py` to cover `--fixture`, explicit `--config`, discovered config, no fixture/no config failure, and successful command-adapter review creating findings)
- [ ] Ensure adapter failures keep affected cells pending or stale rather than reviewed and cause the review command to exit non-zero with actionable JSON/human output. (verification: integration - extend `tests/test_cli_session_review.py` to assert non-zero exit and unchanged cell coverage for command not found, non-zero command exit, timeout, and invalid verdict output)
- [ ] Preserve existing review-session invariants: one `review` invocation creates at most one run, does not auto-loop, does not auto-fix, and does not auto-triage findings. (verification: integration - extend `tests/test_cli_session_review.py` to assert command adapter success/failure still records one run and leaves findings untriaged)
- [ ] Update user-facing command documentation for JSON/JSONC command adapter configuration and local smoke examples for opencode/claude/codex-style invocation without embedding provider secrets. (verification: manual - inspect `README.md` and run `python -m pytest tests/test_cli_session_review.py` after documentation-adjacent CLI examples are updated; confirm examples use JSON/JSONC, argv arrays, no API keys, and no shell strings)
- [ ] Run the project quality gate after implementation. (verification: integration - `make check`)

## Future Work

- Real-world smoke testing against installed `opencode`, `claude`, and `codex` CLIs may require local authentication and is intentionally outside automated test requirements.
- Additional provider-specific config presets can be proposed later after the generic command adapter contract is stable.

## Final Validation

Archive validation is the authoritative final OpenSpec validation gate. Expected archive gate: `cflx openspec validate add-command-review-adapter --archive-gate`.
