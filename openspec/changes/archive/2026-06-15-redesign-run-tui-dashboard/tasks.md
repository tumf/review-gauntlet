## Implementation Tasks

- [x] Keep Textual as the interactive TTY dashboard framework and redesign the Textual layout structure in `src/review_gauntlet/run_tui.py` into header, metrics, current operation, activity timeline, and one-line footer regions. (verification: unit - `uv run pytest tests/test_run_tui.py`; manual - run an interactive `review-gauntlet run` session or Textual headless snapshot to confirm dashboard regions render in order)
- [x] Introduce a `RunViewState`-style display model or equivalent transformation layer so Textual widgets render human-facing dashboard fields rather than raw `RunSnapshot`/`RunEvent` payloads. (verification: unit - `tests/test_run_tui.py` asserts the view-model fields for status, short session ID, agent name, coverage metrics, finding metrics, task title, command label, and timeline events)
- [x] Replace accent/yellow-normal panel styling with semantic Textual CSS theme colors and panel classes for normal, active, blocked, failed, and finalized states. (verification: unit - `tests/test_run_tui.py` asserts CSS/classes avoid warning styling for normal panels and expose terminal-state classes; manual - inspect TUI colors in an interactive terminal because color rendering is terminal-dependent)
- [x] Update coverage formatter output to show percent, bar, `reviewed / total cells`, and reviewed/pending/stale/superseded breakdown without `current cells`, `! pending`, or `! stale`. (verification: unit - `tests/test_run_tui.py` formatter tests fail if old wording or markers remain)
- [x] Update findings formatter output to always render open, untriaged, confirmed, reopened, fixed-pending, and closed counts, including zero-count sessions. (verification: unit - `tests/test_run_tui.py` covers empty and populated finding states, including `fixed_pending_verification` display as `fixed-pending`)
- [x] Add human task title and description formatting for known ready prompt intents while preserving safe sanitized fallback text for unknown prompts. (verification: unit - `tests/test_run_tui.py` covers pending review, stale review, triage, fix, verify, finalize, malicious markup/control characters, and unknown prompt fallback)
- [x] Replace command placeholder rendering so unresolved command state shows `command resolving...` or omits the row, and no formatter emits `command n/a` or `argv=[]`. (verification: unit - `tests/test_run_tui.py` asserts placeholder behavior; integration - `uv run pytest tests/test_run_controller.py` if controller snapshot/event metadata is adjusted)
- [x] Expose or derive an agent/command display label early enough for running-state TUI updates, using adapter configuration or equivalent runtime state instead of waiting for post-run argv. (verification: integration - `uv run pytest tests/test_run_controller.py tests/test_run_tui.py` simulates a configured command adapter in `src/review_gauntlet/run_controller.py`/`src/review_gauntlet/run_tui.py` and asserts running snapshots/events can render an agent label without raw empty argv)
- [x] Replace raw event rendering with a human-readable activity timeline that converts timestamps to `HH:MM:SS`, shortens session IDs, maps event types to labels, and summarizes details without raw prompts or raw argv. (verification: unit - `uv run pytest tests/test_run_tui.py` covers run_started, status_refreshed, step_started, agent_started, failed, blocked, finalized, malformed timestamp fallback, and escaping in `src/review_gauntlet/run_tui.py`)
- [x] Add blocked, failed, and finalized dashboard summaries with distinct semantic wording and styling hooks while preserving run result semantics. (verification: unit - `tests/test_run_tui.py` covers terminal-state text; integration - existing run tests continue to assert completed/failed result payloads)
- [x] Implement or verify compact layout behavior for approximately 80x24 terminals, stacking metrics or shortening text without losing required status, coverage, findings, task, activity, and footer information. (verification: unit - `uv run pytest tests/test_run_tui.py` covers compact formatter/layout output from `src/review_gauntlet/run_tui.py`; manual - run or snapshot the TUI at 80x24 because terminal wrapping is presentation-dependent)
- [x] Ensure the footer lists only implemented controls and update help text to match the dashboard controls. (verification: unit - `uv run pytest tests/test_run_tui.py` asserts footer text from `src/review_gauntlet/run_tui.py` excludes unimplemented prompt/artifact keys and includes implemented `q`, `r`, `h`, and `Ctrl-C` controls)
- [x] Run project checks after implementation. (verification: integration - `make check` passes)

## Future Work

- Add a prompt expansion toggle (`p prompt`) only if a future change wants in-TUI prompt inspection.
- Add an artifact browser shortcut (`a artifacts`) only after run artifacts have a navigable TUI surface.
- Capture visual regression screenshots for multiple terminal themes if the project later adopts screenshot-based TUI tests.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate redesign-run-tui-dashboard --archive-gate`
