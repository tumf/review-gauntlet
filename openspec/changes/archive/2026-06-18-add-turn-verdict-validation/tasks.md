## Implementation Tasks

- [x] Add a turn-verdict validation CLI command distinct from OCR `validate-verdict`; completion condition: `src/review_gauntlet/cli.py` accepts `review-gauntlet validate-turn-verdict <path> --format json`, reads the provided path, and reports structured `valid: true` / `valid: false` results without invoking an external adapter. (verification: unit - add parser/CLI coverage in `tests/test_cli_run.py` or `tests/test_continuation.py`; run `uv run pytest tests/test_cli_run.py tests/test_continuation.py`.)
- [x] Reuse the continuation verdict schema for validation; completion condition: `src/review_gauntlet/cli.py` calls `validate_continuation_verdict()` or an equivalent shared wrapper from `src/review_gauntlet/continuation.py`, and does not duplicate the `ContinuationVerdict` schema in CLI code. (verification: unit - add a valid turn verdict test in `tests/test_continuation.py`; run `uv run pytest tests/test_continuation.py`.)
- [x] Report actionable invalid-turn diagnostics; completion condition: invalid JSON, unsafe/malformed continuation paths, missing files, and schema errors produce `--format json` output containing `valid: false`, `path`, and an actionable `error` string, then exit non-zero. (verification: unit - add the observed invalid `resolutions[].state: "fixed"` regression in `tests/test_continuation.py` or `tests/test_cli_run.py`; run `uv run pytest tests/test_continuation.py tests/test_cli_run.py`.)
- [x] Add active-session transition validation for resolution states; completion condition: when the verdict references findings in the active session, the command detects resolution states that cannot transition from the current ledger state and reports them before `run` or `resolve` applies resolutions. (verification: integration - add an active-session ledger fixture test in `tests/test_cli_run.py` or `tests/test_finding_transitions.py`; run `uv run pytest tests/test_cli_run.py tests/test_finding_transitions.py`.)
- [x] Update continuation-aware prompt generation to require self-validation; completion condition: `_continuation_prompt_sections()` in `src/review_gauntlet/cli.py` includes `review-gauntlet validate-turn-verdict <continuation_path> --format json` with the exact path rendered for the current task. (verification: unit - add prompt rendering assertions in `tests/test_cli_run.py` or `tests/test_run_controller.py`; run `uv run pytest tests/test_cli_run.py tests/test_run_controller.py`.)
- [x] Instruct agents to repair invalid verdicts before ending the turn; completion condition: the generated prompt in `src/review_gauntlet/cli.py` explicitly tells external agents to run the validation command after writing the verdict, rewrite the verdict if validation fails, re-run validation, and not end the turn until validation succeeds. (verification: unit - add prompt rendering assertions in `tests/test_cli_run.py`; run `uv run pytest tests/test_cli_run.py`.)
- [x] Preserve OCR verdict validation behavior; completion condition: existing `review-gauntlet validate-verdict` behavior in `src/review_gauntlet/cli.py` for OCR-style `{"comments": [...]}` verdict files is unchanged. (verification: unit - keep existing OCR verdict tests passing and add separation coverage in `tests/test_review_adapter.py` or `tests/test_cli_run.py`; run `uv run pytest tests/test_review_adapter.py tests/test_cli_run.py`.)

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate add-turn-verdict-validation --archive-gate`

Repository implementation verification SHALL include `make check` after code changes are complete.

## Future Work

Consider whether a future compatibility layer should offer more actionable suggestions for common invalid aliases such as `"fixed"` without accepting them silently.

## Acceptance #1 Failure Follow-up
- [x] make check fails: pyright reports 6 errors in tests/test_cli_run.py — reportPrivateUsage on line 8 (ignore comment is on line 7 but pyright checks line 8), and 4 reportUnknownLambdaType/reportUnknownArgumentType errors on lines 152-159 from untyped lambdas in @pytest.mark.parametrize. Fixed by moving the pyright ignore to the private symbol import, replacing untyped parameterized lambdas with typed helper functions, and verifying with `uv run pyright tests/test_cli_run.py`, focused pytest, and `make check`. (verification: unit/typecheck/full-suite - run `uv run pyright tests/test_cli_run.py`, `uv run pytest tests/test_cli_run.py tests/test_continuation.py tests/test_finding_transitions.py tests/test_review_adapter.py`, and `make check`.)
