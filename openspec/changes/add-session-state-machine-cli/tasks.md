## Implementation Tasks

- [ ] Add durable session storage under `.review-gauntlet/` with an active-session pointer and ledger records for sessions, runs, review cells, findings, finding occurrences, and finding events. (verification: unit - add `tests/test_session_store.py` covering create/load/update against `tmp_path/.review-gauntlet/ledger.sqlite` and assert no writes occur outside `tmp_path`)
- [ ] Add target resolution for `init --from <base> --to <head>`, `init --worktree`, and `init --commit <oid>` with explicit `head_mode` semantics for moving and fixed targets. (verification: unit - add `tests/test_targets.py` covering branch/worktree/commit metadata plus invalid mixed options)
- [ ] Vendor the pinned `alibaba/open-code-review` review rule corpus and prompt-relevant review contract from commit `c323c6b40c72aa95d7cb801bedcb957b52ff9807`, including `system_rules.json`, every `rule_docs/*.md`, attribution/license metadata, and OCR-style comment fields. (verification: unit - add `tests/test_ocr_rules.py` asserting all upstream rule docs are present, the pinned upstream commit is recorded, and representative path mappings resolve to the same rule documents)
- [ ] Wire `review-gauntlet init` so it creates a session, records base/head policy, captures ruleset digest, builds the initial review universe, initializes review cells, and does not execute review work. (verification: integration - extend `tests/test_cli.py` with `main(["init", ...])` assertions for `.review-gauntlet/active-session.json`, ledger session rows, and zero run rows)
- [ ] Convert the existing plan/matrix concepts into review-cell generation for `file x rule x slice` without regressing existing `inventory`, `plan`, or `report` behavior. (verification: unit - keep `tests/test_planner.py` passing and add `tests/test_review_cells.py` for deterministic cell IDs/states from a `ReviewPlan`)
- [ ] Implement `review-gauntlet review` as a single-run session advancement that recalculates the current target digest, creates one immutable run record, selects eligible pending/stale/verification-needed cells within budget, records coverage updates, and exits without looping. (verification: integration - add `tests/test_cli_session_review.py` asserting one run row per invocation and pending cells remain when invoked with a limited budget)
- [ ] Add a deterministic fake review adapter for tests and local smoke verification so review behavior can be verified without real LLM credentials. (verification: unit - add `tests/test_review_adapter.py` asserting the fake adapter emits configured clean cells/findings from fixtures)
- [ ] Implement an OCR-compatible review result normalizer that maps OCR-style line-level comments into session findings and preserves imprecisely positioned comments where `start_line` and `end_line` are both `0`. (verification: unit - extend `tests/test_review_adapter.py` with OCR comment fixtures containing `path`, `content`, `suggestion_code`, `existing_code`, `start_line`, `end_line`, and `thinking`)
- [ ] Implement session-level finding IDs and fingerprint deduplication using stable inputs that avoid line-number-only identity. (verification: unit - add `tests/test_findings.py` asserting repeated findings with shifted line numbers reuse one `RGF-*` ID while `finding_occurrences` gains rows)
- [ ] Implement finding state transitions for `untriaged`, `confirmed`, `fixed_pending_verification`, `fixed_verified`, `false_positive`, `waived`, `accepted_risk`, and `reopened`. (verification: unit - add `tests/test_finding_transitions.py` covering allowed transitions, rejected invalid transitions, terminal states, and expiry metadata)
- [ ] Wire `review-gauntlet mark` to record triage events and update current finding state without executing review work. (verification: integration - extend `tests/test_cli_session_review.py` to mark findings confirmed/false-positive/waived/accepted-risk/fixed and assert no additional run row is created)
- [ ] Verify `fixed_pending_verification` findings during later `review` runs and transition them to `fixed_verified` or `reopened` based on detection/verification evidence. (verification: integration - add `tests/test_cli_session_review.py` cases where the fake adapter omits the fingerprint on the next run and where it emits it again)
- [ ] Reconcile changed targets between runs by marking affected current cells stale or superseded and adding new pending cells for the current review universe. (verification: integration - add `tests/test_session_reconciliation.py` mutating files under `tmp_path` after an initial run and asserting status reports stale/pending before finalization)
- [ ] Wire session command output modes so `review`, `status`, `findings`, and `finalize` accept `--format human|json` and `--audience human|agent`, with `--audience agent --format json` producing automation-safe output without progress UI. (verification: integration - extend `tests/test_cli.py` with JSON parse assertions for pending review, untriaged findings, confirmed findings, verification-needed findings, finalize-ready sessions, and no decorative output in agent audience mode)
- [ ] Wire `review-gauntlet status` to report coverage, finding state counts, target digest freshness, `can_finalize`, and `next_required_action` in human-readable and JSON forms. (verification: integration - extend `tests/test_cli.py` with `status --format json` parse assertions for pending review, untriaged findings, confirmed findings, verification-needed findings, and finalize-ready sessions)
- [ ] Wire `review-gauntlet findings` to show open findings by default and include terminal findings with `--all`. (verification: integration - add `tests/test_cli_findings.py` asserting default stdout suppresses terminal findings and `--all` includes them)
- [ ] Wire `review-gauntlet finalize` to validate completion conditions without running review work, return actionable failure reasons when incomplete, and finalize only complete sessions. (verification: integration - add `tests/test_cli_finalize.py` covering pending cells, untriaged findings, fixed-pending-verification, stale target digest, expired risk/waiver, successful finalization, and no new run row)
- [ ] Preserve existing command contracts for `inventory`, `plan`, and `report`, including JSON output via `model_dump_json(indent=2)` and usage error exit code 64. (verification: unit - run existing `tests/test_cli.py`, `tests/test_inventory.py`, and `tests/test_planner.py` unchanged as part of `make check`)
- [ ] Add CLI smoke documentation or test fixtures showing `init -> review -> status -> mark -> review -> finalize` on a temporary repository without external credentials. (verification: manual - run `uv run review-gauntlet init`, `uv run review-gauntlet review`, `uv run review-gauntlet status`, `uv run review-gauntlet mark`, and `uv run review-gauntlet finalize` against a temporary repository and record the smoke flow in a test fixture or README section)

## Future Work

- Real LLM review provider integration and prompt management beyond deterministic fake/local adapters.
- CI/CD, PR bot, notification, or external wait-loop orchestration around the session CLI.
- Advanced expiry policy automation for long-lived waivers or accepted risks beyond MVP date checks.

## Final Validation

Expected implementation validation: `make check`.
Expected OpenSpec archive gate: `cflx openspec validate add-session-state-machine-cli --archive-gate`.
