## Implementation Tasks

- [ ] Extend `finalize` to produce latest checkpoint files only after completion blockers are clear. (verification: integration - new `tests/test_cli_finalize_checkpoint.py` asserts `uv run review-gauntlet finalize --format json` creates `.review-gauntlet/checkpoints/latest/status.json`, `findings.json`, `events.json`, and `summary.md`; completion condition: successful output includes generated files, `checkpoint_state: complete`, `usable_as_review_base: true`, and `review_base_commit`)
- [ ] Ensure failed `finalize` is no-write and no-cleanup. (verification: integration - `tests/test_cli_finalize_checkpoint.py` seeds pending cells or untriaged findings, runs `finalize`, and asserts checkpoint file mtimes/content are unchanged and active session state remains available; completion condition: failed finalization cannot advance the review base)
- [ ] Implement checkpoint status serialization with Git base-anchor fields. (verification: integration - `tests/test_cli_finalize_checkpoint.py` asserts `status.json` includes schema version, session metadata, target, current `target_digest`, `last_run_target_digest`, `ruleset_digest`, coverage, finding counts, run count, blockers, current `HEAD`, `review_base_commit`, `checkpoint_state`, and `usable_as_review_base`; completion condition: JSON is deterministic and parseable)
- [ ] Implement checkpoint findings serialization for all finalized-session findings. (verification: integration - `tests/test_cli_finalize_checkpoint.py` seeds open-then-terminal findings and asserts `findings.json` includes terminal findings with stable ordering and latest occurrence details; completion condition: default terminal suppression from `findings` does not apply to checkpoint output)
- [ ] Implement checkpoint event serialization with safe metadata handling. (verification: integration - `tests/test_cli_finalize_checkpoint.py` asserts valid event metadata is parsed into JSON objects and malformed metadata is represented without crashing; completion condition: event rows are emitted in deterministic order with raw fallback evidence)
- [ ] Implement deterministic `summary.md` rendering for finalized checkpoints. (verification: integration - `tests/test_cli_finalize_checkpoint.py` asserts the summary includes checkpoint state, review base commit, session ID, coverage table, findings table, event summary, and blocker section; completion condition: dynamic table cells are escaped or normalized enough to preserve Markdown structure)
- [ ] Archive or clean up active session runtime state after successful `finalize`. (verification: integration - `tests/test_cli_finalize_checkpoint.py` asserts `active-session.json` is removed or invalidated and `review`/`status` fail with an actionable `review-gauntlet init` message after successful finalization; completion condition: old sessions cannot be silently advanced)
- [ ] Change default `init` target selection to latest checkpoint or all-files fallback. (verification: integration - `tests/test_init_targets.py` asserts `init` without flags uses `.review-gauntlet/checkpoints/latest/status.json` `review_base_commit -> HEAD` when present, behaves like `--all` when no checkpoint exists, and fails when a present checkpoint is invalid/unusable or references an unknown commit; completion condition: no-flag init is deterministic and safe)
- [ ] Preserve explicit `init` target override behavior. (verification: unit/integration - existing and new `tests/test_init_targets.py` cases assert `--all`, `--worktree`, `--from/--to`, and `--commit` keep their current target semantics even when latest checkpoint exists; completion condition: checkpoint default is used only when no explicit target mode is selected)
- [ ] Update `.gitignore` to track finalized checkpoint snapshots but ignore runtime state. (verification: manual - `git check-ignore -v .review-gauntlet/ledger.sqlite`, `.review-gauntlet/active-session.json`, `.review-gauntlet/runs/example`, and `.review-gauntlet/archive/example` report ignored while `git check-ignore -v .review-gauntlet/checkpoints/latest/status.json` reports not ignored after creation; completion condition: only latest checkpoint files are intended for Git review)
- [ ] Document the finalized checkpoint workflow in README. (verification: manual - inspect `README.md` and run `uv run review-gauntlet finalize --help` to confirm documented command/options match the CLI; completion condition: README shows `init -> review/triage/verify -> finalize -> git add .review-gauntlet/checkpoints/latest` and does not document an independent checkpoint command)
- [ ] Run project validation. (verification: manual - `make check`; completion condition: format, lint, typecheck, and tests pass)

## Future Work

- Add optional checkpoint history if teams later need committed checkpoint archives.
- Add an opt-in external wrapper workflow for Git commit automation if users want it outside the core CLI.
- Consider removing or migrating any legacy `session_state: finalized` persistence once checkpoint-based finalization is established.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate add-checkpoint-snapshots --archive-gate`
