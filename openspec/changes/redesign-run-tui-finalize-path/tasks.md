## Implementation Tasks

- [x] Extend run snapshot status data for TUI gate and agent-output classification. Completion condition: `RunSnapshot` carries `session_state`, `can_finalize`, `finalize_blockers`, `next_required_action`, `run_count`, current agent lifecycle status, last output age, timeout remaining when known, artifact path, and output-tail entries or equivalent fields populated from `_status()` / command runner state with safe fallback values when no active session exists. (verification: unit - `uv run pytest tests/test_run_tui.py` covers snapshot-driven gate and agent-output rendering without direct widget-level session queries.)

- [x] Add finalize-path gate view models and classification logic. Completion condition: a pure function derives six ordered gates with title, index, state, and detail from a `RunSnapshot`, ready-prompt intent, agent status, and recent run result when available. (verification: unit - add/update `tests/test_run_tui.py` tests covering Review coverage, Triage findings, Fix confirmed findings, Verify fixes, Resolve blockers, Finalize checkpoint, zero-cell/no-finding skipped or done cases, blocked cases, failed cases, and finalized cases; run `uv run pytest tests/test_run_tui.py`.)

- [x] Add agent output capture, artifact persistence, and activity stream state for session-level run commands. Completion condition: session-level command execution records full stdout and stderr to per-step artifacts such as `.review-gauntlet/runs/<run-id>/agent-stdout.log`, `.review-gauntlet/runs/<run-id>/agent-stderr.log`, and structured activity entries such as `activity.jsonl`, while retaining existing run result stdout/stderr fields. (verification: integration - add/update `tests/test_cli.py` or `tests/test_run_tui.py` fake session-command tests that write stdout/stderr and assert `.review-gauntlet/runs/<run-id>/agent-stdout.log`, `agent-stderr.log`, and `activity.jsonl` or the implemented equivalent artifact paths exist; run the focused pytest file.)

- [x] Stream or incrementally surface agent stdout/stderr tail to the TUI while the process is active. Completion condition: the TUI can display recent stdout/stderr lines before the command exits, distinguish stdout from stderr, cap displayed output to the latest configured number of lines, and truncate long lines. (verification: integration - add/update `tests/test_run_tui.py` or `tests/test_cli.py` with a fake long-running command that emits multiple stdout/stderr lines and asserts the controller/TUI state exposes latest tail lines before final result; run the focused pytest file.)

- [x] Sanitize and redact agent output for TUI display without corrupting raw artifacts. Completion condition: TUI-visible output strips ANSI/control characters, handles carriage-return progress updates safely, removes excessive blank lines, truncates long lines, and redacts common secret patterns such as `*_TOKEN=`, `*_SECRET=`, and `*_KEY=` while raw stdout/stderr artifacts remain complete. (verification: unit - add/update `tests/test_run_tui.py` tests feeding ANSI, control characters, long lines, blank lines, carriage-return progress, and secret-like strings to output-formatting helpers and assert safe display output; integration artifact preservation is covered by the artifact tests.)

- [x] Add agent lifecycle and heartbeat state. Completion condition: the run controller or command runner exposes `starting`, `running`, `quiet`, `finishing`, `completed`, `failed`, `timed_out`, and `cancelled` states or equivalent display states; heartbeat activity is emitted or synthesized when the process is alive but quiet, without flooding repeated messages. (verification: unit - add/update `tests/test_run_tui.py` tests covering status transitions, quiet threshold behavior, timeout-approaching display when timeout is known, stderr-not-failed behavior, non-zero failure, timeout, and cancellation; run `uv run pytest tests/test_run_tui.py`.)

- [x] Replace Coverage-primary dashboard layout with Finalize-path-primary layout. Completion condition: Textual composition and refresh paths render `Finalize path` as the largest/primary panel, render `Current operation` and `Session metrics` as secondary panels, and no longer render standalone Coverage as the first primary dashboard region. (verification: unit - add/update `tests/test_run_tui.py` text-rendering tests asserting `Finalize path`, `Session metrics`, `gate x/6`, and `agent step` appear while the old header-style `Review dashboard` and primary `step N` progress wording no longer appear.)

- [x] Rework header and current-operation text around finalize gates and agent liveness. Completion condition: header text shows `Review Gauntlet`, high-level run state, `gate x/6`, active gate title, elapsed time, agent display name, last output/quiet duration, and shortened session ID; current operation shows stable active-gate title/description plus agent status, command, timeout/quiet information when known, and artifact path. (verification: unit - add/update `tests/test_run_tui.py` tests covering pending review, stale review, reopened/untriaged triage, confirmed fix, fixed-pending verification, blocker, finalize prompt mappings, last-output display, quiet display, timeout-remaining display, and artifact path display.)

- [x] Rework activity text around gate transitions, agent lifecycle, and agent output tail. Completion condition: activity formatter labels gate events, agent step events, stdout/stderr lines, heartbeat messages, return codes, timeout, and artifact hints without raw prompts, raw argv payloads, ISO timestamps, empty argv artifacts, unsanitized control characters, or unbounded output. (verification: unit - add/update `tests/test_run_tui.py` tests asserting mixed event/output activity rows, event-only and agent-only model support if implemented, row limits, older-line hidden marker, stderr warning styling/labeling without failure, returncode display, and malformed timestamp handling.)

- [x] Keep the TUI visible after run completion until explicit quit. Completion condition: `_run_controller()` stores the run result, updates final display state, and does not call `exit(result)` immediately; post-completion `q` exits with the stored result. (verification: integration - update `tests/test_run_tui.py::test_run_app_executes_controller_in_headless_mode` or add an equivalent Textual app-driver test proving controller completion alone does not return the result and explicit quit returns the stored result.)

- [x] Preserve active-run controls and terminal-state behavior. Completion condition: during active runs, `q` still requests stop-after-current-step; during active runs, `Ctrl-C` interrupts with the existing structured interrupted result; after run completion, `q` and `Ctrl-C` quit using the stored result. (verification: unit - add/update `tests/test_run_tui.py` tests covering active `q`, active interrupt, post-completion quit, and post-completion interrupt/quit behavior.)

- [x] Preserve non-TUI execution semantics. Completion condition: `--format json`, `--no-tui`, non-TTY text executions, and missing Textual fallback continue to bypass the interactive dashboard and return/emit results as before. (verification: integration - `uv run pytest tests/test_cli.py` covers JSON, no-TUI, non-TTY, and fallback warning paths.)

- [x] Run project checks. Completion condition: the repository quality gate passes after implementation. (verification: integration - `make check`.)

## Future Work

- Optional manual display-mode keybindings (`a` agent output, `e` events, `m` mixed) may be implemented after the default mixed activity stream is stable; the initial implementation may keep mixed mode fixed if keybindings would complicate the first release.
- Optional coverage-delta activity rows such as `0 / 4 -> 2 / 4 cells reviewed` may be added later if the controller records prior snapshots or emits richer status-change events.
- Optional responsive layout refinements for very small terminals may be handled after the gate-first and output-tail information architecture is implemented.
- Configuring whether persisted artifacts store raw or redacted agent output is future work; this proposal requires raw artifacts and redacted TUI display only.

## Final Validation

Archive validation itself is the authoritative final OpenSpec validation gate.
Expected archive gate: `cflx openspec validate redesign-run-tui-finalize-path --archive-gate`

## Acceptance #1 Failure Follow-up
- [x] Spec delta openspec/changes/redesign-run-tui-finalize-path/specs/review-sessions/spec.md not satisfied: line 21 requires the TUI to show 'last-output age ..., quiet duration ..., timeout remaining ..., and artifact path when available'; the scenario at lines 90-98 ('Run TUI shows agent liveness during quiet periods') requires a non-flooding heartbeat row such as 'agent still running' plus visible last-output/quiet duration. Implemented by adding snapshot-derived liveness detail, quiet heartbeat rows, timeout display, and artifact path rendering in `src/review_gauntlet/run_tui.py`; verified by `uv run pytest tests/test_run_tui.py tests/test_run_controller.py`.
- [x] Task 6 (tasks.md) marked [x] but unimplemented + falsely verified: completion condition requires 'heartbeat activity is emitted or synthesized when the process is alive but quiet'. Implemented synthesized quiet state in `RunController._current_agent_lifecycle()` and non-flooding TUI heartbeat rows in `heartbeat_timeline_events()`; added controller/TUI tests for quiet threshold, timeout remaining, and heartbeat throttling; verified by `uv run pytest tests/test_run_tui.py tests/test_run_controller.py`.
- [x] Task 8 (tasks.md) marked [x] but unimplemented + falsely verified: completion condition requires current operation to show 'artifact path' and 'timeout/quiet information' and header to show 'last output/quiet duration'. Implemented `RunViewState.liveness_detail`/`artifact_path`, header liveness detail, and current-operation artifact/timeout/quiet rendering; added TUI rendering tests; verified by `uv run pytest tests/test_run_tui.py tests/test_run_controller.py`.

## Acceptance #2 Failure Follow-up
- [x] Archive commit-path blocker: the installed prek pre-push hook runs `make typecheck` + `make test` (prek.toml `stages = ["pre-push"]`), and `make typecheck` fails, so the archive push path is blocked. The pre-commit hook (make format + make lint) passes, so a bare local commit would succeed, but the required gate and any push do not. Resolved the pyright errors by aligning `_agent_timeout_seconds` with the float adapter timeout type and adding a public test seam for synthesized quiet-liveness setup; verified `make check` passes via agent-exec job `73c14d790f38874b348c43d6135951c9`.
- [x] Task 13 (openspec/changes/redesign-run-tui-finalize-path/tasks.md:27) is marked [x] with completion condition 'the repository quality gate passes' and verification `make check`, but `make check` failed at typecheck. Re-ran the full repository quality gate after fixes; `make check` exits 0 via agent-exec job `73c14d790f38874b348c43d6135951c9`.
- [x] `make check` failed at the typecheck step (regression introduced by this change), so the required quality gate was red even though the 44 focused run_tui/run_controller tests passed. Fixed pyright error 1 by annotating `RunController._agent_timeout_seconds` as `float | None`, matching `CommandAdapterConfig.timeout_seconds`; fixed pyright error 2 by replacing direct private test mutation with `RunController.set_agent_step_started_at_for_testing()`. Verified by `make check` via agent-exec job `73c14d790f38874b348c43d6135951c9`.
