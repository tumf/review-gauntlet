# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: ef9aa4f6ed74e8ef61d889edf4fbdffb2c282a62
- Session ID: RGS-a25141a813a9
- Created at: 2026-06-16T07:20:20.773942Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 13 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0157 | false_positive | src/review_gauntlet/session_store.py | test-evidence | Connection resource leak: `connect()` returns a `sqlite3.Connection` that is never explicitly closed. When used in `with self.connect() as conn:` blocks throughout this class, `Connection.__exit__` commits/rolls back the transaction but does **not** call `close()`. Each call opens a new file descriptor that is only reclaimed at GC time. Under sustained use (e.g., a long-lived process calling many store methods), this leaks file descriptors. Wrap the connection or return a closing context manager. |
| RGF-0158 | false_positive | src/review_gauntlet/session_store.py | test-evidence | Non-atomic ID generation with race potential: `_next_finding_id` reads all existing IDs, computes max+1, and returns the candidate. The caller then inserts with that ID. Under SQLite's default deferred locking, two concurrent processes can both read the same max, compute the same next ID, and the second INSERT will hit an `IntegrityError` on the primary key — which is unhandled. Replace the read-then-increment pattern with a single atomic `INSERT ... SELECT COALESCE(MAX(...)+1, 1)` or use a dedicated sequence table. |
| RGF-0159 | false_positive | src/review_gauntlet/session_store.py | test-evidence | Cursor accessed after connection context manager exits: `cur.rowcount` is checked on line 139 after the `with self.connect() as conn:` block has exited. While this works today because `Connection.__exit__` does not close the connection, it couples correctness to an implementation detail. If `connect()` is ever changed to close on exit (e.g., to fix the resource leak above), this will break silently. Move the `rowcount` check inside the `with` block. The same pattern repeats at lines 142-153 (`mark_cell_reviewed`) and lines 155-192 (`refresh_file_digest`). |
| RGF-0160 | false_positive | src/review_gauntlet/session_store.py | cli-contract | `_next_finding_id` fetches every row from the `findings` table into Python memory just to compute the maximum numeric suffix. This is O(n) in the number of findings and will degrade as the table grows. Use a SQL aggregate instead. |
| RGF-0161 | false_positive | src/review_gauntlet/session_store.py | cli-contract | `session_id` parameter is accepted but immediately discarded with `del session_id`. The query on line 343 scans all sessions, not just the given one. Either use the parameter to scope the query (e.g., `WHERE session_id = ? AND ...`) or remove it from the signature to avoid misleading callers. |
| RGF-0162 | false_positive | src/review_gauntlet/session_store.py | cli-contract | Every public method opens a new `sqlite3.Connection` via `self.connect()`, but none of them set `PRAGMA journal_mode` or `PRAGMA foreign_keys`. Without `PRAGMA foreign_keys = ON`, the foreign-key relationships implied by the schema (runs.session_id, finding_occurrences.finding_id, etc.) are never enforced, so orphaned rows can accumulate silently. Additionally, setting WAL mode would prevent "database is locked" errors under concurrent read/write access. |
| RGF-0163 | false_positive | src/review_gauntlet/session_store.py | data-validation | `_next_finding_id` accepts `session_id` but immediately discards it with `del session_id`, then queries ALL findings across every session. The function signature implies session-scoped IDs but the implementation produces globally-scoped IDs. If global scope is intentional, remove the parameter; if session-scoped IDs are intended, the query needs a `WHERE session_id = ?` clause. Either way, the current code misleads callers and makes the contract ambiguous. |
| RGF-0164 | false_positive | src/review_gauntlet/session_store.py | data-validation | `update_cell_state`, `mark_cell_reviewed`, and `refresh_file_digest` all access `cur.rowcount` after the `with self.connect() as conn:` block exits. While the sqlite3 context manager only commits the transaction (doesn't close the connection), the cursor is used outside the managed scope. If the update matched zero rows, the transaction is already committed before the `LookupError` is raised — the error fires after a committed no-op, making the control flow misleading. Move the rowcount check inside the `with` block so the error can prevent commit if needed in future refactors. |
| RGF-0165 | false_positive | src/review_gauntlet/session_store.py | data-validation | The schema does not declare foreign key constraints or enable `PRAGMA foreign_keys`. Tables like `runs`, `review_cells`, `findings`, and `finding_occurrences` reference `session_id` and `finding_id` by convention, but the database will happily accept orphaned records (e.g., a finding referencing a non-existent session, or an occurrence referencing a deleted finding). Adding `REFERENCES` and enabling the pragma would catch data integrity violations at write time rather than surfacing as mysterious lookup failures later. |
| RGF-0166 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Budget validation (`args.budget < 0`) is performed at line 857 only after significant side-effectful work: `store.active_session_id()`, `store.session_metadata()`, `load_ruleset()`, `target_digest()`, `TargetSpec.model_validate()`, and `_reconcile_cells()` (lines 851-856). If the user passes a negative budget, all that database and filesystem I/O is wasted before the error is raised. Move the budget validation to the top of the function, right after the existing concurrency check at line 849. |
| RGF-0167 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Same deferred-validation issue as in `_cmd_review`: budget is checked at line 986 only after `store.active_session_id()`, `store.session_metadata()`, `TargetSpec.model_validate()`, `_reconcile_cells()`, and `_fixed_pending_targets()` have already executed (lines 964-975). Move `if args.budget < 0` to immediately after the concurrency check at line 963. |
| RGF-0168 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `_is_expired` returns `True` (expired) for all corrupt or malformed metadata: JSON decode errors, non-dict payloads, and unexpected types. This means a single corrupted `finding_events.metadata` record silently blocks finalization by adding a "waived or accepted-risk findings have expired" blocker via `_expired_terminal_decision_count`. Consider returning `False` on parse errors (treat corrupt metadata as "not expired" so it doesn't silently block finalization) or logging a warning so the corruption is surfaced. |
| RGF-0169 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `_finding_int_field` calls `int(value)` when `value` is a `str`, which raises `ValueError` if the string is not a valid integer. Although the data normally comes from SQLite coalesce expressions that produce integers, this function accepts `dict[str, object]` and is used as a sort key in `_finding_sort_key`. An invalid string value would crash the entire findings listing. Add a try/except around the `int()` conversion to fall back to `0`. |
| RGF-0170 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The `_is_expired` function returns `True` on any JSON decode error or when `metadata` is not a dict. This means corrupt or non-dict metadata silently causes waived/accepted-risk findings to be treated as expired, which blocks finalization. There is no direct unit test for `_is_expired` — its behaviour is only exercised indirectly through integration-level finalize tests. A focused unit test covering malformed metadata, missing `until`, expired date, future date, and non-dict JSON would provide better regression safety for this edge-case-rich function. |
| RGF-0171 | false_positive | src/review_gauntlet/cli.py | test-evidence | `_agent_output_tail` collects all stdout lines first, then all stderr lines, then takes the last `limit` entries. The resulting ordering is always stdout-then-stderr, not chronological. This means the activity log could be misleading when diagnosing agent failures. This function has no direct unit test — its behaviour is only indirectly verified through `test_run_session_command_persists_agent_output_artifacts` which checks file existence but not content ordering. |
| RGF-0172 | false_positive | src/review_gauntlet/cli.py | test-evidence | `_finding_states` builds a raw SQL query with dynamically constructed placeholders and bypasses the `SessionStore` abstraction. While the parameterised values prevent SQL injection, this function has no direct test — it is only exercised through `_cmd_verify_fixes` integration paths. A unit test with a pre-populated in-memory store would verify correct state mapping and the empty-set short-circuit. |
| RGF-0173 | false_positive | src/review_gauntlet/cli.py | test-evidence | `_finalize_blockers_are_commit_resolvable` and `_finalize_blocker_is_commit_resolvable` contain nuanced logic — the former has a special case where `_TARGET_DIGEST_DRIFT_REASON` is considered resolvable only when a dirty-review-universe blocker is also present. This conditional coupling is not covered by a direct unit test. Because `_ready_prompt` uses this to decide whether to emit the `finalize` prompt (line 1368), a false positive or negative here silently changes the review workflow. Dedicated parametrized tests covering: (1) drift-only, (2) dirty-only, (3) both, (4) other blockers combined with drift, would harden this logic. |
| RGF-0174 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | ThreadPoolExecutor is not used as a context manager. If an unexpected BaseException (other than KeyboardInterrupt) is raised during `as_completed` iteration — e.g. a SystemExit injected by a signal handler or a MemoryError — neither the `except KeyboardInterrupt` nor the `else` clause runs, so `executor.shutdown()` is never called and worker threads leak. Wrapping it in `with` guarantees cleanup on all exit paths. |
| RGF-0175 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | `_is_expired` returns `True` for any JSON parse error or non-dict metadata, treating corrupted or malformed metadata as expired. If the metadata column is corrupted (e.g. by a partial write or a serialization bug), every waived/accepted-risk finding will appear expired, which adds the "waived or accepted-risk findings have expired" blocker and prevents finalization. Treating parse errors as `False` (not expired) is safer — a metadata bug should not block the user's workflow; it should be surfaced separately. |
| RGF-0176 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | SQL query uses hardcoded string literals `'waived'` and `'accepted_risk'` instead of referencing `FindingState.WAIVED.value` and `FindingState.ACCEPTED_RISK.value`. If the enum values are ever renamed, this query will silently miss rows instead of failing loudly. |
| RGF-0177 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The function parameter `root` is reassigned to `root.resolve()` on line 1654, shadowing the original argument. This means later code in the function implicitly uses the resolved path rather than the caller-supplied path, which is the intended behavior but hides it behind a mutation. Using a separate name makes the transform explicit and prevents future readers from missing it. |
| RGF-0178 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | Duplicate assignments: `stdout_artifact`, `stderr_artifact`, and `activity_artifact` are unconditionally assigned to the payload dict on lines 558-560, then conditionally re-assigned with the exact same values on lines 566-570. The conditional blocks are dead code. If the intent was to only include non-None values, the unconditional assignments on lines 558-560 should be removed. As written, `None` values leak into the payload even when the conditional guards suggest they should be omitted. |
| RGF-0179 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | `model_dump` spreads `**self.payload` after `type` and `timestamp`, so any payload containing a key named `"type"` or `"timestamp"` would silently overwrite the event's own metadata. Currently no callers pass those keys, but the method offers no guard against it. Consider raising on collision or building the dict with payload first so the event's own fields take precedence. |
| RGF-0180 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Bug: `_run_step_payload` unconditionally assigns `stdout_artifact`, `stderr_artifact`, and `activity_artifact` (potentially `None`) into the payload dict at lines 558-562. The conditional re-assignments at lines 566-570 are therefore dead code — the keys are already present regardless of their value. If the intent is to omit `None`-valued artifact keys from the payload, the unconditional assignments should be removed. |
| RGF-0181 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Bug: `RunEvent.model_dump` uses `{**self.payload}` spread after `type` and `timestamp`, so if `self.payload` contains a key named `"type"` or `"timestamp"` the explicit fields are silently overwritten. Since `payload` is built from arbitrary `**kwargs` in `create()`, any caller passing `type=` or `timestamp=` as keyword arguments would corrupt the event structure. Guard against this by either raising on reserved keys or putting `self.payload` under a nested key. |
| RGF-0182 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Potential unhandled exception: `self.store.active_session_id()` at line 253 can raise `LookupError` (the `snapshot()` method explicitly catches it). If the active session is removed between loop iterations — e.g. by an external process — the `run()` loop will crash with an unhandled `LookupError` instead of terminating gracefully. Consider catching `LookupError` here and treating it as a "no active session" completion, consistent with the post-loop logic at lines 372-381. |
| RGF-0183 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Path traversal check is incomplete: `any(part in {"", ".", ".."} for part in relative.parts)` rejects explicit `..` components, but does not prevent symlink-based traversal. If the repository filesystem contains symlinks, an attacker-controlled `generated_files` entry could still escape `.review-gauntlet/checkpoints/` by referencing a symlink that points outside the directory. Consider resolving the path and confirming it stays within the expected directory using `Path.resolve().relative_to()`. |
| RGF-0184 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Redundant assignments: `stdout_artifact`, `stderr_artifact`, and `activity_artifact` are unconditionally assigned to the `payload` dict on lines 559–561, then conditionally re-assigned on lines 566–570 with the same values. The conditional assignments are dead code since the keys already exist with the same values. |
| RGF-0185 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Unhandled `LookupError` on line 253: `self.store.active_session_id()` is called without a try/except, but the same call is wrapped in a try/except on line 180 and in `_active_session_id_or_none`. If a session is finalized between the loop guard and this call, a `LookupError` will propagate as an unhandled exception, crashing the run loop. |
| RGF-0186 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | The check `relative.suffix != ".json" or relative.name == ""` has a redundant second condition: `relative.name` can never be empty if the path passed the `relative.parts` check on line 537 (a path with an empty name would have an empty string in its parts, which is already rejected). This is harmless but misleading about what invariants hold at this point. |
| RGF-0187 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | Pluralization bug: the `suffix` "s" is appended to the end of the entire `singular` phrase, not to the noun. For example, `_count_detail(2, "finding needs triage", ...)` produces `"2 finding needs triages"` instead of `"2 findings need triage"`. Similarly, `"confirmed finding needs fix"` becomes `"confirmed finding needs fixs"`. The callers on lines 568, 574, and 580-582 all pass multi-word phrases as `singular`, so every plural count renders with broken grammar. |
| RGF-0188 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | No bounds validation on `mapped_index` before indexing into `gates`. If `_NEXT_ACTION_GATE_INDEX` is ever updated with a value outside the 1–6 range (e.g., 0 or 7), `gates[mapped_index - 1]` will raise `IndexError` (or silently access the wrong gate for index 0, which becomes `gates[-1]`). A bounds check would make this robust against future changes to the index mapping. |
| RGF-0189 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | Pluralization bug: `_count_detail` appends `s` to the end of the entire `singular` string, which produces incorrect English for multi-word phrases. For example, with `count=2` and `singular="finding needs triage"`, the output is `"2 finding needs triages"` instead of `"2 findings need triage"`. Similarly `"confirmed finding needs fix"` becomes `"confirmed finding needs fixs"`. All three call sites (lines 568, 574, 581) pass multi-word phrases and are affected. |
| RGF-0190 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | The parameter name `seconds` (float) is shadowed by the `divmod` result on line 352, rebinding it to an `int`. This makes it impossible to reference the original float value after the divmod and could confuse readers or cause subtle bugs if the function is later modified. |
| RGF-0191 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | The empty `RunSnapshot` is constructed with all positional arguments, making it fragile if `RunSnapshot` fields change order or new required fields are added. Consider using keyword arguments for clarity and resilience. |
| RGF-0192 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | Pluralization bug: `_count_detail` appends 's' to the end of the entire `singular` string, not to the noun. When count > 1, callers produce broken English like '2 finding needs triages' (line 568), '2 confirmed finding needs fixs' (line 574), '2 fixed-pending finding needs verifications' (line 581). The suffix should apply to the noun only, or callers should pass separate singular/plural forms. |
| RGF-0193 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | Pluralization issue from `_count_detail`: with triage_count=2 this produces '2 finding needs triages'. Pass an explicit plural form. |
| RGF-0194 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | Fragile positional-only construction of RunSnapshot. If RunSnapshot's fields are reordered or new fields are added, this will silently produce wrong values or fail at runtime. Use keyword arguments for clarity and resilience. |
| RGF-0195 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | The parameter `seconds` (float) is shadowed by the int result of `divmod` on line 350. This makes it impossible to access the original float value after line 350 and can confuse debugging. Use a different name for the remainder variable. |
| RGF-0196 | false_positive | README.md | docs-accuracy | The `review --budget` example uses `--budget 20` but does not mention the actual default value (50). While not strictly wrong, the example could mislead readers into thinking 20 is the default. Consider adding a note that the default budget is 50 cells per run, so readers know when they need to override it. |
| RGF-0197 | false_positive | README.md | docs-accuracy | The `findings` table in the README lists `state` values including `fixed_pending_verification` (line 330), but the actual CLI `--mark` help shows both hyphenated and underscored forms are accepted (`fixed-pending-verification` and `fixed_pending_verification`). The README only documents the underscored form. For consistency with the `mark` subcommand which uses hyphenated states (e.g., `false-positive`, `accepted-risk`), consider documenting the hyphenated form as canonical or noting both forms. |
| RGF-0198 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `--budget` and `--concurrency` use bare `type=int` without parse-time constraints, deferring validation to the command handlers at lines 849-852 and 961-964. This is inconsistent with `--max-steps` (line 234) which uses `type=_positive_int`. A user passing `--concurrency 0` or `--budget -1` gets past argparse and hits a `fail()` call with a different error format than argparse would produce. For `--concurrency`, `_positive_int` already fits. For `--budget`, a `_non_negative_int` type function would enforce the invariant at parse time and eliminate the duplicated runtime checks. |
| RGF-0199 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `_is_expired` catches `TypeError` alongside `json.JSONDecodeError` and `ValueError`, silently returning `False` for any unexpected metadata shape. If the stored metadata is corrupted in a way that produces a `TypeError` (e.g., `until` is an unexpected nested structure that causes `str()` to succeed but `fromisoformat` to fail for a different reason), the finding is treated as non-expired. For waived or accepted-risk findings, this means an expired decision silently remains in effect. Consider narrowing the exception clause to only `json.JSONDecodeError` and `ValueError`, and logging or propagating `TypeError` so corrupted metadata is surfaced rather than silently ignored. |
| RGF-0200 | fixed_verified | src/review_gauntlet/cli.py | data-validation | The `mark --until` date is validated for format (ISO 8601) but not for whether it is in the past. Setting `--until 2020-01-01` on a waived or accepted-risk finding would immediately make the decision expired, which `_expired_terminal_decision_count` would detect on the next `_finalize_reasons` call — causing a finalize blocker the user just created. A simple check that the date is today or later would prevent this confusing state. |
| RGF-0201 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | _finding_int_field was changed to catch ValueError from non-numeric strings instead of propagating it, but there are no unit tests for this function. The new behavior (returning 0 for strings like 'abc') is a meaningful safety net that should be verified by a test, e.g. asserting _finding_int_field({'x': 'not-a-number'}, 'x') == 0. |
| RGF-0202 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The refactor from manual ThreadPoolExecutor lifecycle to a context manager changes KeyboardInterrupt shutdown semantics. Previously, the interrupt path called executor.shutdown(wait=False, cancel_futures=True), which returns immediately without blocking. Now, re-raising inside the `with` block causes ThreadPoolExecutor.__exit__ to call shutdown(wait=True), which blocks until all running futures complete. This could cause the process to hang on Ctrl+C if an adapter.review call is slow. The existing tests for review_cells_concurrently do not exercise the KeyboardInterrupt path, so this behavioral change has no test evidence. |
| RGF-0203 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | Budget validation was added early in _cmd_verify_fixes (mirroring the move in _cmd_review), but there are no tests exercising negative --budget for either command. Consider adding a test that passes --budget -1 and asserts SystemExit with the expected error message. |
| RGF-0204 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | `RunSnapshot` constructor is called with `step_count=0` which is not a field on the dataclass, and the required field `elapsed_seconds` (which has no default) is omitted. This will raise a `TypeError` at runtime if this function is ever called. Replace `step_count=0` with `elapsed_seconds=0.0`. |
| RGF-0205 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | When `count` is 0, the function returns the bare plural string (e.g. `"findings need triage"`) without prefixing the count. Callers like `derive_finalize_gates` use this for gate detail text alongside a `"done"` state, producing `"done  findings need triage"` which is misleading — it reads as though findings still need triage. Including the `0` prefix makes the detail unambiguous (e.g. `"0 findings need triage"`). |
| RGF-0206 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The parameter `current_coverage_requires_review` is accepted but never referenced in the function body. The pending/stale checks it would serve are already handled by `cell_counts.get(...)`. This dead parameter misleads readers into thinking it influences the return value and will silently rot as the logic evolves. |
| RGF-0207 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | N+1 query pattern: one query fetches all waived/accepted-risk findings, then a separate query per finding fetches the latest event to check expiry. For sessions with many such findings this degrades linearly. A single query joining findings to finding_events with a window function (or correlated subquery) would eliminate the per-row roundtrip. |
| RGF-0208 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | This raw SQL UPDATE bypasses the SessionStore abstraction, coupling the CLI directly to the database schema. If SessionStore later adds validation, audit logging, or state-machine checks on session transitions, this call site will silently skip them. Consider adding a `finalize_session(session_id)` method to SessionStore. |
| RGF-0209 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | All stdout lines are appended before all stderr lines, so the resulting tail does not reflect the temporal interleaving of output. When diagnosing agent failures the jumbled order can be misleading, e.g. an error message appearing after output that was actually produced later. |
| RGF-0210 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | `_count_detail(0, ...)` returns the bare plural label (e.g. `"findings need triage"`) without any numeric prefix, which creates a misleading gate detail when the count is actually zero. Gates in the "done" state (triage_count == 0, fix_count == 0, verify_count == 0) display detail text that reads as if work is still needed. For example, a completed triage gate shows `✓ Triage findings   done   findings need triage`. The zero case should include a count or an explicit "none" indicator to avoid contradicting the gate state. |
| RGF-0211 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | When count is 0, `_count_detail` returns the bare plural string without a numeric prefix (e.g. "findings need triage" instead of "0 findings need triage"). This text is used as gate detail in `derive_finalize_gates` — when paired with a "done" gate state, the message reads as if work remains. The tests in `test_finalize_checklist_state_derivation_across_gates` assert gate *states* at zero counts but never verify the *detail text*, so this edge-case behavior is unguarded by tests. |
| RGF-0212 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | `_events_text` is dead code with no test coverage. Unlike `_task_text` and `_agent_text` (which are tested via `run_tui.__dict__` access in tests), this function has zero callers and zero test references. It also constructs a `RunSnapshot` with minimal keyword args — if `RunSnapshot`'s required fields evolve, this will break silently with no test to catch it. Either add a test or remove the dead code. |
| RGF-0213 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | `format_duration` is tested only for non-finite edge cases (nan/inf → "0s"), but lacks test evidence for its three formatting branches: seconds-only (<60s), minutes+seconds (60s–3599s), and hours+minutes (≥3600s). These branches are exercised indirectly through `build_agent_summary` tests (e.g. "quiet 2m00s"), but a direct parametrized test for boundary values (59, 60, 3599, 3600) would guard against regressions if the format changes. |
| RGF-0214 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | `_string_tuple` converts `None` items to the literal string `"None"` instead of filtering them out. `str(None)` returns `"None"` which is truthy and passes the walrus-operator filter. If `finalize_blockers` or any other caller passes a list containing `None` values (e.g. from deserialized JSON), users would see `"None"` as a blocker message. |
| RGF-0215 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | `command_display_label` filters template placeholders (parts containing `{`/`}`) from the visible parts, but the fallback on line 443 returns `config.command` without applying the same filter. If `config.command` itself is a template expression (e.g. `"{interpreter}"`), the function returns the raw template string instead of a meaningful label. |
| RGF-0216 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | `model_dump` unconditionally overwrites any `"type"` or `"timestamp"` keys that exist in `self.payload` via `{**self.payload, "type": ..., "timestamp": ...}`. If a caller passes `type=` or `timestamp=` as extra payload fields to `RunEvent.create`, those values are silently dropped. Either raise on reserved keys in `create()` or namespace the payload to avoid collision. |
| RGF-0217 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | The `reason` value emitted to the event sink on line 346 is not stringified (`failure.get("reason", ...)` returns `object`), but the same value is `str()`-converted on line 350 before passing to `_run_result`. This means the event log can contain non-string `reason` values (e.g., a dict or list if the failure dict was constructed with a complex value), while the run result always gets a string. Apply consistent stringification. |
| RGF-0218 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Using `entry.__dict__` to serialize `AgentOutputEntry` dataclass instances is fragile. If the class ever gains `__slots__` (common for frozen dataclasses to save memory), `__dict__` will not exist and this line will raise `AttributeError`. The rest of the codebase uses explicit `model_dump()` methods for serialization (e.g., `RunEvent.model_dump` on line 63). Use `dataclasses.asdict` or add a `model_dump` method for consistency. |
| RGF-0219 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `--budget` and `--concurrency` use raw `type=int` allowing negative or zero values through the parser, unlike `--max-steps` which uses `_positive_int`. While runtime checks in `_cmd_review` (lines 849-852) and `_cmd_verify_fixes` (lines 962-965) catch these, the inconsistency means any new subcommand reusing `_budget_arg` or `_concurrency_arg` would silently accept invalid values unless it also remembers to add manual checks. Using a custom argparse type (like `_positive_int` or a `_non_negative_int`) at the parser level would enforce constraints uniformly and produce clearer error messages. |
| RGF-0220 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `_is_expired` silently returns `False` when metadata JSON is corrupt or contains an unparseable date. This means a finding with a waiver that should have expired (e.g., `"until": "not-a-date"` or corrupted JSON) will be treated as still valid, allowing it to pass finalization. Consider logging a warning for malformed metadata so that corrupted waivers are surfaced rather than silently accepted. |
| RGF-0221 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | _finding_int_field now catches ValueError for non-numeric strings (e.g. "not-a-number"), but there is no unit test exercising this new branch. A test with a non-numeric string value would confirm the change works as intended and prevent regressions. |
| RGF-0222 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The refactor from explicit executor.shutdown() to a `with` statement changes KeyboardInterrupt shutdown semantics: the old code called `executor.shutdown(wait=False, cancel_futures=True)` (return immediately, cancel all pending), while the context manager calls `shutdown(wait=True)` (block until running futures complete). With long-running adapters, this could block indefinitely on Ctrl-C. The existing test uses concurrency=1 so it does not exercise the wait-on-interrupt scenario with multiple in-flight futures. |
| RGF-0223 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | When count is 0, `_count_detail` returns the bare plural string without the count number (e.g., "findings need triage" instead of "0 findings need triage"). This text is shown as the detail alongside a "done" gate state in the finalize path display, making "done \| findings need triage" read as if there are still findings to triage. Including "0" removes the ambiguity. |
| RGF-0224 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | `_events_text` constructs a `RunSnapshot` without several fields that `dashboard_state` relies on (e.g., `agent_lifecycle`, `can_finalize`, `finalize_blockers`, `elapsed_seconds`, `next_required_action`, `session_state`, `command_label`). If this dead-code function is ever invoked it will raise `TypeError` at runtime. Either delete it or supply all required fields. Since it is already annotated as unused, deleting it is the safer option to prevent someone from calling it and hitting the crash. |
| RGF-0225 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The `current_coverage_requires_review` parameter is accepted but never referenced in the function body. This dead parameter is computed by the caller at line 1733-1736 and passed at line 1746, adding unnecessary work and confusion about the function's contract. Remove it from both the signature and the call site. |
| RGF-0226 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The `target_digest_drift` entry in `_READY_PROMPTS` is dead code: `_ready_prompt()` (lines 1340-1367) never returns this value. If the target digest drifts without producing stale cells, `_ready_prompt` falls through to the finalize check or returns `None`, silently ignoring the drift. Either add an explicit check for target-digest drift before the finalize branch, or remove the dead prompt entry to avoid misleading future readers. |
| RGF-0227 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | stdout lines and stderr lines are concatenated in separate groups (all stdout first, then all stderr) rather than interleaved. The resulting 'tail' does not reflect the actual order of output and can be misleading when diagnosing failures. Since `subprocess.run` with `capture_output=True` already separates the streams, consider timestamped merging or clearly labeling the two groups in the activity log. |
| RGF-0228 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Relative paths passed to `--output` do not get `expanduser()` applied, so a quoted tilde path like `--output '~/config.yaml'` would be interpreted as a literal `~` directory under root rather than the home directory. Apply `expanduser()` unconditionally before the absolute/relative branch. |
| RGF-0229 | accepted_risk | src/review_gauntlet/cli.py | cli-contract | This function directly constructs and executes a SQL query against the store's internal database, bypassing the `SessionStore` abstraction. If the schema changes (column renames, table restructuring), this code will break silently. Consider adding a `get_finding_states(session_id, finding_ids)` method to `SessionStore` instead. |
| RGF-0230 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | `_events_text` constructs a `RunSnapshot` with an invalid keyword argument `step_count` (no such field on `RunSnapshot`) and omits the required `elapsed_seconds` field. This would raise `TypeError` at runtime if the function were ever called. The field was likely confused with the `step_count` key used in the run-result dict (e.g., line 149). Even though the function is currently dead code, it will crash immediately if revived without fixing these two issues. |
| RGF-0231 | fixed_verified | src/review_gauntlet/run_tui.py | cli-contract | When `count` is 0, `_count_detail` returns the bare plural string (e.g., `"findings need triage"`) without any count prefix. This produces misleading gate detail text: a gate in `"done"` state can display `"findings need triage"`, which reads as if there are outstanding findings. For example, `derive_finalize_gates` calls `_count_detail(0, "finding needs triage", "findings need triage")` for the triage gate when `triage_count == 0`, yielding detail `"findings need triage"` paired with state `"done"`. Prefixing with `0` or using a distinct phrase like `"all triaged"` would avoid the ambiguity. |
| RGF-0232 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | `_count_detail` returns the bare plural string without the count when `count == 0`, producing misleading gate detail text. For example, when triage_count is 0 and the gate state is `done`, the detail reads `findings need triage` instead of `0 findings need triage` — contradicting the `done` status shown next to it. Every call site in `derive_finalize_gates` (lines 568, 574, 582) is affected. |
| RGF-0233 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | Secret-redaction regex patterns are recompiled on every call to `sanitize_agent_output_line`. This function is called from `format_agent_output_entry` for up to 6 entries per TUI refresh cycle (every 0.25s), so the patterns are compiled ~24 times/second. While `re` has an internal cache, pre-compiling at module level is more robust and avoids cache eviction under load. |
| RGF-0234 | fixed_verified | src/review_gauntlet/run_tui.py | test-evidence | `_plain_text` escapes `[` to `\[` to prevent Rich markup interpretation, but does not escape `]`. While opening-bracket escaping prevents new Rich tags from being opened, a `]` in untrusted input could still close a Rich tag opened by a surrounding caller (e.g., inside `[bold]...[/bold]` wrapping). Consider escaping `]` as well for defense in depth. |
| RGF-0235 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Unused imports: `threading` and `field` are imported but never referenced anywhere in this file. They appear to be leftovers from an incomplete change. Unused imports add noise and can mislead readers into thinking thread-safety or `field()` defaults are in play when they are not. |
| RGF-0236 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | Key-ordering change in `model_dump` silently alters collision semantics. Previously, payload keys could overwrite `type`/`timestamp`; now `type`/`timestamp` always win. If any call-site relies on passing `type` or `timestamp` inside the payload dict (e.g. for domain-specific event types), this is a breaking behavioral change. Consider raising an error or logging a warning when `payload` contains reserved keys rather than silently overwriting in either direction. |
| RGF-0237 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | The `_stop_after_current_step` and `_interrupted` flags are read in the `run()` loop and written from `request_stop_after_current_step()` / `interrupt()`, which are designed to be called from another thread (e.g. a signal handler or TUI thread). The `threading` import was added but no synchronization primitive is used. In CPython bool assignment is effectively atomic due to the GIL, but this is an implementation detail. If thread-safety is intended (the `threading` import suggests so), use `threading.Event` for these flags. |
| RGF-0238 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | The try/except LookupError block encompasses three independent calls (`active_session_id()`, `_status_snapshot()`, `_ready_prompt()`). If `active_session_id()` succeeds but one of the latter two raises LookupError, the except handler overwrites `session_id` with `None`, silently discarding a valid session ID. This produces a snapshot with `session_id=None` even though the session is active, which can mislead callers that rely on it to determine session liveness. |
| RGF-0239 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | `max_steps` is not validated on construction. A value of 0 or negative causes `range(1, max_steps + 1)` to produce an empty iteration, skipping the loop entirely and falling through to the post-loop logic at line 383. With `max_steps=0`, the result claims `max_steps_exhausted` with `max_steps=0`, which is misleading — no steps were actually exhausted. A negative value produces the same effect. Adding a guard in `__init__` prevents confusing runtime behavior. |
| RGF-0240 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Using `entry.__dict__` to serialize a frozen dataclass is fragile. If `AgentOutputEntry` is later changed to use `__slots__` (a common optimization for frozen dataclasses), `__dict__` will not exist and this will raise `AttributeError`. `dataclasses.asdict(entry)` is the canonical, forward-compatible approach. |
| RGF-0241 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Pydantic v2 `ValidationError` is not a subclass of `ValueError`, so corrupt session metadata causes an unhandled traceback. `TargetSpec.model_validate(metadata["target"])` at lines 864, 971, and 1378 can raise `ValidationError` that propagates past this handler, producing an ugly stack trace instead of a clean `fail()` message. |
| RGF-0242 | false_positive | src/review_gauntlet/cli.py | data-validation | The `except TypeError` branch is unreachable dead code. `str(until)` always returns a `str`, and `date.fromisoformat()` raises `ValueError` (never `TypeError`) for malformed string input. Dead exception handlers can mask future regressions if the code around them changes. |
| RGF-0243 | false_positive | src/review_gauntlet/cli.py | data-validation | The null-byte sentinel `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00` used to protect literal `{{`/`}}` sequences can collide with template variable values. If `prompt` (or a future variable) contains this sentinel string, the final `.replace()` calls will incorrectly convert it to brace characters, corrupting the expanded output. Consider validating that variable values do not contain the sentinel, or use a two-pass approach that doesn't rely on a magic string in the data stream. |
| RGF-0244 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | N+1 query problem: `_expired_terminal_decision_count` executes one query to fetch all waived/accepted-risk findings, then issues a separate query per finding to fetch its latest event metadata. With many findings this degrades linearly. A single query with a JOIN or correlated subquery would be both faster and simpler. |
| RGF-0245 | false_positive | src/review_gauntlet/cli.py | test-evidence | Unreachable `except TypeError` handler. `str(until)` always succeeds for any Python object, and `date.fromisoformat` accepts a `str` argument, so `TypeError` cannot be raised on this code path. This dead handler masks potential issues: if `until` were an unexpected type, `str(until)` would silently coerce it (e.g. `str(123)` -> `'123'`), never reaching this branch. Remove the dead handler or, if you intend to guard against non-string `until` values, check `isinstance(until, str)` before calling `fromisoformat`. |
| RGF-0246 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | Mutating the argparse `Namespace` object with `args._tui_rendered = True` to communicate back to the caller (`_run_session_command`) is fragile. The caller checks `getattr(args, '_tui_rendered', False)` at line 772, but nothing in the argparse definition declares this attribute so it's invisible to readers. If `_cmd_run` is refactored to return a richer result (e.g. a dataclass with a `tui_rendered` flag alongside the dict), this side-channel becomes unnecessary. |
| RGF-0247 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | `assert_review_universe_clean` is imported locally inside `_finalize` (line 1889) and again inside `_finalize_reasons` (line 2003) rather than at the top of the file. Other symbols from `review_gauntlet.checkpoint` are already imported at the module level (lines 21-26). Unless there is a circular-import constraint, add `assert_review_universe_clean` to the existing top-level import to avoid the repeated local imports. |
| RGF-0248 | false_positive | src/review_gauntlet/cli.py | cli-contract | Dead exception handler: `date.fromisoformat(str(until))` converts `until` to a string before calling `fromisoformat`, so `TypeError` can never be raised here. `fromisoformat` only raises `ValueError` for invalid date strings, and the preceding `except ValueError` on line 2063 already handles that. This dead handler silently returns `False` for a code path that cannot execute, which could mask future bugs if the logic changes. |
| RGF-0249 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Fragile side-channel via private attribute mutation: `args._tui_rendered = True` on line 1469 is set after `app.run()` returns, but if `app.run()` raises an exception (other than `KeyboardInterrupt`), the flag is never set. The caller at line 772 checks `getattr(args, '_tui_rendered', False)` to decide whether to emit text output. If the TUI crashes mid-render, `_emit_run` will print the result on top of partial TUI output. Consider returning a `(result, tui_rendered)` tuple from `_cmd_run` instead of mutating `args`. |
| RGF-0250 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Catching broad `ValueError` from `_run_session_command` converts all ValueErrors—including unexpected internal errors from deep call chains—into terse user-facing error messages via `fail()`, suppressing the traceback. This can hide real bugs. Consider catching a more specific exception type for expected validation failures, or limiting the catch scope to the specific calls that can produce user-facing ValueErrors. |
| RGF-0251 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Run artifact directory uses `uuid.uuid4().hex` while the review session uses integer `run_id` from `store.create_run()`. This means there is no link between the persisted artifacts directory and the session's run_id, making it hard to correlate artifacts with a specific review run for debugging or auditing. |
| RGF-0252 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | The key-order change in `model_dump` means that any payload key named `type` or `timestamp` will be silently overridden during serialization. While the new ordering is safer (framework-owned fields should win), there is no guard against accidental data loss. Consider raising on collision or filtering reserved keys from the payload at construction time. |
| RGF-0253 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Using `entry.__dict__` to serialize frozen dataclass instances bypasses `dataclasses.asdict()` and will break if `AgentOutputEntry` ever adopts `__slots__` (common optimization for frozen dataclasses). Consider using `dataclasses.asdict(entry)` for robustness. |
| RGF-0254 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | `_int_or_zero` silently coerces float values to 0. If `run_count` arrives as a JSON `float` (e.g. `3.0`), the function returns 0 instead of 3. Since `int` and `float` are separate types in Python, and JSON-derived data may represent integer counts as floats depending on the serialization path, this is a silent data-loss path. Consider handling `float` explicitly by truncating to `int`. |
| RGF-0255 | fixed_verified | src/review_gauntlet/cli.py | data-validation | The return value of `app.run()` is not validated before being used as a dict. If the TUI exits without producing a result (e.g., user closes the window immediately), `app.run()` can return `None`. The `cast` on line 1475 is a no-op at runtime, so `None` propagates to line 778 where `result["completed"]` raises `TypeError: 'NoneType' object is not subscriptable`. |
| RGF-0256 | fixed_verified | src/review_gauntlet/cli.py | data-validation | The `except TypeError` handler is unreachable dead code. `str(until)` always returns a `str`, and `date.fromisoformat()` only raises `ValueError` for invalid date strings (never `TypeError` when given a `str`). This dead handler could mask bugs introduced during future refactoring if someone changes the conversion logic and expects `TypeError` to be caught elsewhere. |
| RGF-0257 | fixed_verified | src/review_gauntlet/cli.py | data-validation | When multiple review cells fail, only the first failure is captured in `first_failure`. Subsequent adapter errors are silently discarded — the loop `continue`s without recording them. The final output (lines 952-965) reports only the single `failed_cell_id` and `error`, so the user has no visibility into how many cells actually failed or which ones. Consider collecting all failures into a list and reporting a failure count, or at minimum logging each failure via the reporter. |
| RGF-0258 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The `_persist_session_command_artifacts` function has a TOCTOU race: it counts existing `run_*` directories to derive the next sequence number, but between counting and creating the new directory another concurrent call could create the same directory. With `exist_ok=True` on the `mkdir`, two concurrent calls would silently share the same `run_NNNN` directory and overwrite each other's log files. Consider using an atomic counter (e.g. a monotonic integer stored in a file or database) or a UUID-based directory name to avoid the race. |
| RGF-0259 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_process_session_output_text` helper (used to handle `TimeoutExpired.stdout`/`stderr` which can be `bytes \| str \| None`) has no unit tests. The `bytes` branch (line 1434: `value.decode("utf-8", errors="replace")`) exercises a code path that is important for correctness when subprocess output is captured as bytes during timeout. Consider adding a targeted test for this helper. |
| RGF-0260 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_agent_output_tail` function has no dedicated unit tests. Its truncation logic (splitting the `limit` budget between stdout and stderr tails) could produce surprising results: if stdout has 0 entries, `stderr_tail` gets `limit - 0 = limit` entries, but if stdout has many entries, `stderr_tail` gets at most `limit - limit//2` entries. Edge cases around empty input, single-stream dominance, and the exact split boundary warrant targeted unit tests. |
| RGF-0261 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_interrupted_run_result` function catches `LookupError` to handle the case where no active session exists, but this code path has no test coverage. A test should verify behavior when `store.active_session_id()` raises `LookupError` during a keyboard interrupt — the function should return `session_id: None` without crashing. |
| RGF-0262 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Sequence counter in `_persist_session_command_artifacts` uses `len(existing)` to derive the next `run_NNNN` directory name. This is fragile: if any earlier directory is manually deleted or missing, the count will collide with an existing directory. Combined with `exist_ok=True` on `mkdir`, the collision silently overwrites logs from a previous run. Use the highest existing sequence number + 1, or parse the max suffix, instead of relying on directory count. |
| RGF-0263 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The `except BaseException` on line 1478 is overly broad. It is meant to catch unexpected TUI crashes and fall back to text mode, but `BaseException` also catches `GeneratorExit` and any future system-level exceptions that should propagate. Use `except Exception` instead — `KeyboardInterrupt` and `SystemExit` are already handled by the preceding `except` clause. |
| RGF-0264 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Cross-function state is passed by mutating `args._tui_rendered` on the argparse `Namespace` object. This is a private, undeclared attribute that only exists because argparse namespaces accept arbitrary assignment. It makes data flow hard to trace and fragile — a rename or refactor could silently break the `_tui_rendered` check at line 776. Consider returning a tuple `(result, tui_rendered)` from `_cmd_run`, or wrapping the result dict with a sentinel key, to make the data flow explicit. |
| RGF-0265 | accepted_risk | src/review_gauntlet/cli.py | cli-contract | `_cmd_review` (lines 918-968) and `_cmd_verify_fixes` (lines 1036-1098) share a near-identical ~50-line block: concurrent review execution, result iteration, fingerprint collection, finding upsert, digest refresh, and cell marking. If a bug is fixed in one copy, the other is likely to be missed. Consider extracting the shared review-execution-and-ingestion logic into a helper function. |
| RGF-0266 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | `RunEvent.model_dump()` uses `{**self.payload, "type": ..., "timestamp": ...}` which silently overwrites any `"type"` or `"timestamp"` keys that may exist in `self.payload`. If a caller ever passes `type=` or `timestamp=` as payload kwargs to `_emit()`, that data is silently lost in the serialized output. Consider reversing the merge order or raising on conflict. |
| RGF-0267 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | `snapshot()` reads multiple mutable fields (`_step`, `_agent_status`, `_agent_lifecycle`, `_command_argv`, etc.) that `run()` writes from another thread, with no synchronization beyond `threading.Event` for stop/interrupt signals. In CPython the GIL prevents torn reads of individual assignments, but a single `snapshot()` call can observe an inconsistent combination of fields (e.g. `_agent_status="running"` but `_step` still at the previous value). If the TUI or any consumer relies on snapshot consistency, consider protecting these fields with a `threading.Lock`. |
| RGF-0268 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | Line 581 serializes `AgentOutputEntry` via `entry.__dict__`. This works for plain dataclasses in CPython but bypasses the dataclass API and would break silently if `AgentOutputEntry` ever gains `__slots__` or nested dataclass fields. Using `dataclasses.asdict` is more robust. |
| RGF-0269 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | Inconsistent type coercion of `failure["reason"]`. On line 354 the raw `object` value from `failure.get("reason")` is passed directly to `_emit`, but on line 358 it is wrapped in `str()`. If the failure dict ever contains a non-string reason (e.g. an int error code or None), the event payload will carry a different type than the run result for the same logical field. Apply `str()` consistently on both paths. |
| RGF-0270 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | `_current_agent_lifecycle` reads multiple instance variables (`_agent_status`, `_agent_step_started_at`, `_agent_timeout_seconds`, `_agent_lifecycle`) that are concurrently written by `run()` on the worker thread. Because there is no lock, `snapshot()` (callable from the TUI thread) can observe a torn state: e.g. `_agent_status` is still `"running"` at line 214 while `_agent_step_started_at` has already been set to `None` at line 328. The individual None-guards prevent a crash but the resulting `AgentLifecycle` can have internally contradictory values (status="quiet" with timeout_remaining=None). Consider copying the relevant fields under a `threading.Lock` or reading them from a single atomic frozen-dataclass reference. |
| RGF-0271 | fixed_verified | src/review_gauntlet/run_controller.py | data-validation | `checkpoint_generated_files_from_stdout` passes the full agent stdout to `json.loads` with no size guard. If a buggy or adversarial agent emits megabytes of non-JSON text, `json.loads` will scan the entire string before raising `JSONDecodeError`, and if it emits a very large valid JSON payload the parsed dict lives in memory. Consider truncating or capping the input to a reasonable maximum before attempting to parse. |
| RGF-0272 | false_positive | src/review_gauntlet/cli.py | data-validation | Unreachable `except TypeError` branch. `str(until)` on line 2076 always produces a `str`, so `date.fromisoformat` can only raise `ValueError` (handled on line 2077), never `TypeError`. This dead code may mask future logic errors if the surrounding code changes — a reader might assume `TypeError` is a live path and not investigate when it silently returns `False`. |
| RGF-0273 | accepted_risk | src/review_gauntlet/cli.py | data-validation | Template sentinel uses a null-byte prefix (`\x00REVIEW_GAUNTLET_LITERAL_BRACE`) that is never validated against in the input `value`. If a config file or environment variable contains this exact sentinel sequence, the final replacement on lines 1668-1669 would corrupt the expanded output. Consider either validating that `value` does not contain the sentinel, or using a UUID-based sentinel that is guaranteed unique per call. |
| RGF-0274 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Artifact directory naming derives the sequence number by counting existing `run_*` directories. If a directory is manually deleted or if concurrent processes call this function, the sequence could collide, silently overwriting artifacts in a previously-created directory (since `mkdir(parents=True, exist_ok=True)` does not fail on collision). Consider using a UUID or atomic directory creation (`os.mkdir` without `exist_ok`) with retry to guarantee uniqueness. |
| RGF-0275 | false_positive | src/review_gauntlet/run_tui.py | data-validation | `select_active_gate` passes `gates[-1]` as the default to `next()`, which raises `IndexError` if `gates` is an empty tuple. Although the current sole caller (`dashboard_state` via `derive_finalize_gates`) always returns exactly 6 gates, the function signature accepts any `tuple[FinalizeGate, ...]` and performs no length check. A defensive guard avoids a crash if a future caller or test passes an empty sequence. |
| RGF-0276 | false_positive | src/review_gauntlet/run_tui.py | data-validation | The ANSI-stripping regex only removes CSI sequences (`ESC[…`). OSC sequences (`ESC]…ESC\` or `ESC]…BEL`), which terminals use for setting titles, hyperlinks, and clipboard, pass through unstripped. Agent subprocesses (especially those running git, npm, or other CLI tools with rich terminal output) can emit OSC sequences that would appear as garbage in the TUI or, in pathological cases, inject terminal control codes. |
| RGF-0277 | false_positive | src/review_gauntlet/run_tui.py | data-validation | `_count_value` silently returns 0 for string-typed numeric values (e.g. `"5"`). Since `coverage` and `findings` are `dict[str, object]`, values could arrive as strings from JSON parsed with non-standard decoders, YAML config, or CLI-assembled dicts. A string like `"12"` being silently treated as 0 would make coverage appear incomplete and findings appear absent, with no error or warning. |
| RGF-0278 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | Double Rich markup escaping bug. `_plain_text` is called on line 817 to normalize whitespace, which escapes `[` to `\[` and `]` to `\]`. Then `_summarize_text` (line 820) calls `_plain_text` again internally, double-escaping brackets: `[` → `\[` → `\\[`. Agent output containing brackets (JSON, markdown, code) will display garbled escape sequences like `\\[` in the TUI. |
| RGF-0279 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | Same double-escaping issue as in `sanitize_agent_output_line`. `_plain_text` is called at line 375, then the fallback path at line 379 passes the already-escaped `safe` value into `_summarize_text`, which calls `_plain_text` again. Any brackets in artifact paths will be double-escaped on the fallback (non-`.review-gauntlet/` marker) path. |
| RGF-0280 | false_positive | src/review_gauntlet/run_tui.py | test-evidence | The new `]` escaping in `_plain_text` has no direct test coverage. The existing `test_plain_text_collapses_control_whitespace` only covers whitespace handling (`\n`, `\r`, `\t`), and no test asserts that `]` characters are escaped to `\]`. The existing assertion `assert "\\[bold]task" not in text` in `test_task_text_sanitizes_unknown_prompt_and_omits_command_na` passes coincidentally (the escaped `\[bold\]` no longer contains the substring `\[bold]`), but does not verify the `]` escaping itself. Add a test like: `assert plain_text("a[b]c") == "a\\[b\\]c"`. |
| RGF-0281 | false_positive | src/review_gauntlet/run_tui.py | test-evidence | The new bounds check `1 <= mapped_index <= len(gates)` in `select_active_gate` is unreachable with current `_NEXT_ACTION_GATE_INDEX` values (all 1-6) and the fixed 6-gate list. No test exercises the fallback path when `mapped_index` is out of range. Consider adding a test with a synthetic `_NEXT_ACTION_GATE_INDEX` entry (or passing a truncated gates tuple) to verify that the fallback `next(...)` path is taken when the mapped index is out of bounds, confirming the defensive check works as intended. |
| RGF-0282 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | TOCTOU race on `_agent_step_started_at`: `snapshot()` (and thus `_current_agent_lifecycle()`) can be called from a TUI/refresh thread while `run()` is executing in another thread. Between the `is not None` check on line 218 and the subtraction on line 219, the `run()` method could set `_agent_step_started_at = None` (line 328), causing `now - None` to raise `TypeError`. The same pattern recurs at line 223 (though line 224 has its own guard in the ternary). Capture the attribute in a local variable before the check to eliminate the race. |
| RGF-0283 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `model_dump()` silently drops payload keys named `type` or `timestamp` because the dict unpacking puts `self.payload` first and then overwrites those keys. No current callsite triggers this, but the API contract of `RunEvent.create(**payload)` accepts arbitrary kwargs, making this a latent data-loss bug. Either strip/reject reserved keys in `create()`, or raise on collision in `model_dump()`. |
| RGF-0284 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | If `self._command_runner(...)` raises any exception other than `KeyboardInterrupt` (e.g., `OSError`, `RuntimeError`, or a bug in the adapter), the cleanup at lines 317-346 is skipped entirely. This leaves `self._agent_status` stuck at `"running"`, `self._agent_step_started_at` non-None, and `self._agent_lifecycle` in `status="running"` — so every subsequent `snapshot()` will report the agent as still running even though nothing is executing. A `try/finally` (or catching `BaseException`) around the command runner call should reset these fields on any exit path. |
| RGF-0285 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | The `snapshot()` method reads multiple mutable fields (`_step`, `_agent_status`, `_command_argv`, `_agent_lifecycle`, `_command_label`) that `run()` updates from another thread, but none of these reads are synchronized. Since the controller explicitly uses `threading.Event` for cross-thread signaling (lines 148-149), `snapshot()` is expected to be called concurrently. Without a lock, a snapshot can observe a half-updated state — e.g., `_step` already incremented to N+1 while `_agent_status` still reflects step N. Consider guarding the mutable state behind a `threading.Lock`. |
| RGF-0286 | false_positive | src/review_gauntlet/cli.py | data-validation | Dead `except TypeError` handler: `date.fromisoformat(str(until))` receives a `str` (the `str()` call on line 2084 guarantees this), so `date.fromisoformat` can only raise `ValueError`, never `TypeError`. This dead branch silently returns `False` and could mask unexpected future regressions if the surrounding code changes. |
| RGF-0287 | accepted_risk | src/review_gauntlet/cli.py | data-validation | The null-byte sentinel `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00` used to protect literal `{{`/`}}` sequences can collide with input. If a config value in `adapter.command`, `adapter.args`, or `adapter.env` already contains this exact sentinel string, the final `.replace()` on lines 1677 will incorrectly convert it to `{` or `}`, corrupting the expanded result. Consider using a UUID-based or otherwise guaranteed-unique sentinel, or escape/unescape via a scheme that cannot collide with input. |
| RGF-0288 | false_positive | src/review_gauntlet/cli.py | data-validation | The dynamically constructed SQL IN clause `where ... finding_id in ({placeholders})` has no upper bound on the number of parameters. SQLite's default SQLITE_MAX_VARIABLE_NUMBER is 999 (raised to 32766 in some builds). If `finding_ids` exceeds that limit, the query will raise `sqlite3.OperationalError`. Consider batching the query when the set is large, e.g. processing in chunks of 500. |
| RGF-0289 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_agent_output_tail` function changed from a combined interleaved tail to a split strategy (half stdout, remaining stderr), but there is no dedicated unit test verifying the new partitioning logic. The only existing test (`test_run_session_command_persists_agent_output_artifacts`) exercises a trivial 1-stdout + 1-stderr case which would pass under both the old and new implementations. Edge cases like stdout-only overflow, stderr-only overflow, or uneven splits remain untested. |
| RGF-0290 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_persist_session_command_artifacts` function changed from UUID-based naming (`uuid.uuid4().hex`) to sequential naming (`run_0000`, `run_0001`, ...) with a retry loop. The existing test (`test_run_session_command_persists_agent_output_artifacts`) does not assert on the directory naming convention and there is no test for the sequential collision-retry logic (the `for attempt in range(100)` / `FileExistsError` loop) or the `for/else` fallback that uses `exist_ok=True`. |
| RGF-0291 | false_positive | src/review_gauntlet/cli.py | test-evidence | The new `_non_negative_int` argparse type function validates `--budget` at parse time, but it has no direct unit test. While integration tests exercise valid budget values, there is no test confirming that a negative budget value (e.g. `--budget -1`) produces the expected `argparse.ArgumentTypeError` with a clean error message and exit code 64. The removed in-body validation in `_cmd_review` and `_cmd_verify_fixes` was previously tested; the replacement argparse-level validation should have equivalent test coverage. |
| RGF-0292 | false_positive | src/review_gauntlet/cli.py | test-evidence | The TUI error-handling path in `_cmd_run` now catches `BaseException` from `app.run()` and checks `isinstance(result, dict)`, raising `RuntimeError` if the TUI returns a non-dict. There is no test covering either the `RuntimeError` path or the `BaseException` fallback that sets `_tui_rendered = False`. These are defensive but untested code paths. |
| RGF-0293 | false_positive | src/review_gauntlet/cli.py | data-validation | The `or 10` fallback for handling the `None` default of `args.limit` is a latent data-validation footgun. If `_positive_int` were ever relaxed to allow `0` (e.g. for a "return nothing" semantic), `int(0 or 10)` would silently produce `10` instead of `0`. Use an explicit `None` check to make the intent clear and avoid surprising coercion. |
| RGF-0294 | false_positive | src/review_gauntlet/cli.py | data-validation | The `TypeError` catch on line 2093 is unreachable dead code. The `until` value is already wrapped with `str()` on line 2086 before being passed to `date.fromisoformat()`, which guarantees the argument is always a string. `date.fromisoformat` raises `ValueError` for invalid date strings, never `TypeError` when given a string. Remove the dead handler to avoid misleading future readers into thinking `TypeError` is a realistic failure mode here. |
| RGF-0295 | false_positive | src/review_gauntlet/cli.py | data-validation | After exhausting 100 retry attempts, the fallback on line 1616-1617 creates the directory with `exist_ok=True`, which silently allows reusing an existing directory. If two concurrent processes both exhaust the retry loop and land on the same sequence number, they will write their artifacts into the same directory, silently corrupting each other's logs. Consider raising an error instead of silently accepting the collision. |
| RGF-0296 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Catching bare `BaseException` is overly broad. Since `KeyboardInterrupt` and `SystemExit` are already re-raised by the preceding handler, the `BaseException` catch only adds `GeneratorExit` over what `Exception` would cover. Narrowing to `Exception` makes the intent explicit and avoids accidentally swallowing interpreter-level signals in edge cases. |
| RGF-0297 | false_positive | src/review_gauntlet/cli.py | data-validation | The `for/else` fallback creates the directory with `exist_ok=True`, which silently succeeds even if the directory already exists. This means the subsequent file writes will overwrite artifacts from a previous (concurrent or otherwise) run, causing silent data corruption. Consider raising an error or using a UUID-based directory name as a fallback instead of silently clobbering existing data. |
| RGF-0298 | false_positive | src/review_gauntlet/cli.py | data-validation | `load_ruleset()` and `target_digest(root)` are called before the `args.budget == 0` early-return check. Both perform I/O (reading rule files and running git commands respectively) that is completely wasted when budget is zero. Move these after the early-return guard. |
| RGF-0299 | false_positive | src/review_gauntlet/cli.py | data-validation | The `TypeError` catch is unreachable. After `str(until)` converts any value to a string and `date.fromisoformat()` either succeeds (returning a `date`) or raises `ValueError`, the `<` comparison between two `date` objects cannot produce a `TypeError`. This dead catch silently swallows errors if future refactoring changes the control flow—remove it so unexpected exceptions surface instead of being silently treated as 'not expired'. |
| RGF-0300 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `review_cells_concurrently` function handles `KeyboardInterrupt` by calling `cancel_adapter` and cancelling futures, but there are no tests verifying this cancellation path. A `KeyboardInterrupt` during concurrent review could leave partial state (some cells marked reviewed, others not) and the cancellation/cleanup logic is non-trivial. Add a test that simulates `KeyboardInterrupt` during `as_completed` iteration to verify that `cancel_adapter` is called and pending futures are cancelled. |
| RGF-0301 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_reconcile_cells` function (lines 1292-1310) handles cell lifecycle transitions (adding new cells, marking removed cells as SUPERSEDED, marking changed-digest cells as STALE) but has no direct unit tests. It is only exercised indirectly through integration-level `_cmd_review` and `_cmd_verify_fixes` tests. The SUPERSEDED transition for removed cells and the fixed-pending-path exclusion from stale marking deserve focused tests to prevent regressions. |
| RGF-0302 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_persist_session_command_artifacts` function (lines 1599-1648) uses a retry loop (up to 100 attempts) to find a unique `run_NNNN` directory name, with a fallback to `exist_ok=True` after exhaustion. The collision-resolution logic and the fallback path after 100 attempts are untested. Additionally, the `_agent_output_tail` helper (lines 1651-1662) that splits stdout/stderr into a bounded tail has no dedicated test for its truncation or interleaving behavior. |
| RGF-0303 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_finalize_blockers_are_commit_resolvable` function (lines 1410-1420) contains logic that treats `_TARGET_DIGEST_DRIFT_REASON` as commit-resolvable only when there is also a dirty-review-universe blocker. This conditional interaction between two blocker types has no direct unit test. Existing tests exercise the ready-prompt side effects but don't verify this function's boolean logic in isolation, particularly the case where target-digest-drift appears alone vs. alongside a dirty-review-universe blocker. |
| RGF-0304 | false_positive | src/review_gauntlet/cli.py | cli-contract | Race condition: when all 100 retry attempts to create a unique `run_XXXX` directory fail, the `for/else` fallback creates the directory with `exist_ok=True`, silently reusing an existing directory. The subsequent writes to `agent-stdout.log`, `agent-stderr.log`, and `activity.jsonl` (lines 1621-1636) will then overwrite a previous run's artifact files, causing data loss. Consider raising an error instead of falling through with `exist_ok=True`. |
| RGF-0305 | accepted_risk | src/review_gauntlet/cli.py | cli-contract | The template expansion sentinel `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00` uses null bytes as delimiters. If a config template string ever contains the literal sequence `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00OPEN` or `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00CLOSE`, it would be replaced with `{` or `}`, corrupting the expanded value. Consider using a more robust mechanism such as a multi-pass approach or a UUID-based sentinel. |
| RGF-0306 | accepted_risk | src/review_gauntlet/cli.py | cli-contract | Timeout detection relies on substring matching (`"timed out" in str(error)`), which is fragile. Any `ReviewAdapterError` whose message incidentally contains the words "timed out" (e.g., a permission error mentioning "the request timed out waiting for approval") would be misclassified as a timeout. Consider using a structured attribute on `ReviewAdapterError` (e.g., `error.is_timeout`) or checking the `failure` dict's `"reason"` field instead. |
| RGF-0307 | false_positive | src/review_gauntlet/cli.py | cli-contract | The argparse `Namespace` object is mutated with a private attribute `_tui_rendered` (line 1481/1485) to communicate state back to `_run_session_command` (line 776). This side-channel is fragile: the attribute is only set inside TUI code paths, and the caller must use `getattr` with a default to avoid `AttributeError`. Consider returning a richer result type (e.g., a dataclass or named tuple with a `tui_rendered` field) from `_cmd_run` instead. |
| RGF-0308 | accepted_risk | src/review_gauntlet/cli.py | data-validation | TOCTOU race in verdict file validation: the file size is checked via `stat().st_size` on line 681, then the file contents are read on line 684. Between these two operations the file could be replaced with a larger payload, bypassing the size limit check. Read the file first, then validate the byte length of the read content. |
| RGF-0309 | false_positive | src/review_gauntlet/cli.py | data-validation | Artifact directory sequence numbering uses `len(existing)` which counts directories rather than parsing the highest existing sequence number. If directories have gaps (e.g., `run_0000` and `run_0002` exist but `run_0001` was deleted), `len(existing)` returns 2, and the code tries `run_0002` which already exists. The retry loop handles this, but if more than 100 collisions occur the `else` branch falls through to `exist_ok=True`, silently sharing a directory with another process and potentially overwriting artifacts. |
| RGF-0310 | false_positive | src/review_gauntlet/cli.py | data-validation | Neither `--concurrency` nor `--budget` enforce an upper bound. A user passing `--concurrency 100000` creates that many threads via `ThreadPoolExecutor(max_workers=min(concurrency, len(cells)))`, which can exhaust OS thread limits and crash the process. Consider capping concurrency to a reasonable maximum (e.g., 64). |
| RGF-0311 | false_positive | src/review_gauntlet/cli.py | data-validation | The `--until` date is validated for ISO format but not checked against the current date. Passing a past date (e.g., `--until 2020-01-01`) silently creates an already-expired waiver/accepted-risk decision, which `_expired_terminal_decision_count` will immediately flag as a finalization blocker. Consider warning or rejecting past dates. |
| RGF-0312 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `model_dump` silently drops any payload entry whose key is `"type"` or `"timestamp"`, since the dict comprehension filters them out to avoid colliding with the top-level keys. If a caller ever passes `type=` or `timestamp=` as a payload kwarg to `RunEvent.create(...)`, the value will be silently discarded from the serialized output. Consider raising an error in `create()` if reserved keys appear in `**payload`, or renaming the filter to make the conflict explicit. |
| RGF-0313 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `MAX_OUTPUT_BYTES` names a byte threshold, but `len(stdout)` returns a character count. For ASCII-only content the two are identical, but if `stdout` ever contains multi-byte UTF-8 characters, the guard will under-count. Consider using `len(stdout.encode())` to match the constant's semantics, or rename the constant to `MAX_OUTPUT_CHARS` if a character-count guard is intentional. |
| RGF-0314 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | Mutable instance attributes (`_step`, `_agent_status`, `_agent_lifecycle`, `_events`, etc.) are read by `snapshot()` / `refresh()` and written by `run()`, which may execute on different threads (the TUI calls `snapshot()` to render while `run()` progresses). While individual attribute writes are atomic under CPython's GIL, a `snapshot()` call can observe a mix of old and new values across the multiple fields updated within a single run-loop iteration (e.g., `_agent_status` updated but `_agent_lifecycle` still stale). If cross-thread reads are intended, consider grouping the mutable state into a single frozen dataclass swapped atomically, or protecting reads/writes with a `threading.Lock`. |
| RGF-0315 | false_positive | src/review_gauntlet/run_controller.py | data-validation | `MAX_OUTPUT_BYTES` implies a byte-count threshold, but `len(stdout)` returns the number of Unicode code points (characters), not bytes. For multi-byte UTF-8 content the character count can be significantly smaller than the byte count, allowing payloads larger than 1 MiB to pass the guard and be fed to `json.loads`. Use `len(stdout.encode())` or rename the constant to reflect character semantics. |
| RGF-0316 | false_positive | src/review_gauntlet/run_controller.py | data-validation | `model_dump` silently drops any payload keys named `"type"` or `"timestamp"`. If a caller ever passes these as event payload kwargs, the data is lost without warning. Consider raising on collision instead of silently filtering. |
| RGF-0317 | false_positive | src/review_gauntlet/cli.py | data-validation | Bug: `_finding_int_field` calls `int(value)` on any string, but does not handle non-numeric strings. If `value` is a non-numeric string (e.g. from data corruption or an unexpected schema change), `int(value)` raises `ValueError`, which is uncaught in the `_finding_sort_key` / `_findings` call chain and will crash the `findings` CLI command. Wrap the `int()` call in a try/except to fall back to `0`. |
| RGF-0318 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Bug: `_is_expired` returns `True` (expired) on any metadata parse failure (JSONDecodeError, ValueError, TypeError). This means findings with corrupted, missing, or NULL metadata are silently treated as expired, which adds a finalize blocker ('waived or accepted-risk findings have expired') and can block session finalization for reasons the user cannot diagnose. The safer default for unparseable metadata is `False` (not expired), so data corruption doesn't block progress. At minimum, the error should be surfaced rather than swallowed. |
| RGF-0319 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Potential None coercion: `str(event['metadata'])` converts SQL NULL to the string `'None'`, which then fails `json.loads` inside `_is_expired` and is treated as expired metadata (blocking finalization). If the metadata column can be NULL, guard against it before passing to `_is_expired`. |
| RGF-0320 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_is_expired` helper has no direct unit tests. It contains multiple code paths (non-dict JSON, missing `until`, valid `until`, invalid date string, JSON decode error) that are only exercised indirectly through `_finalize_reasons` via integration tests. An edge case such as `until` being `0`, `False`, or an empty string all hit the `not until` branch returning `False`, meaning a finding with `until: ""` would be treated as non-expiring rather than malformed. A focused unit test suite for `_is_expired` would cover these boundary cases directly. |
| RGF-0321 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The `_agent_output_tail` helper merges stdout and stderr lines into a tail buffer but does not preserve interleaving order — all stdout lines come before all stderr lines. If the actual output interleaves stdout and stderr, the reconstructed tail can be misleading. There is no test that asserts the ordering behavior or validates the limit truncation logic in isolation. Consider adding a direct test to document whether the current behavior (stdout-first, then stderr) is intentional. |
| RGF-0322 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_finding_states` function constructs a SQL query with dynamic placeholders from `finding_ids`. While it uses parameterized queries (safe from injection), the function has no direct unit test verifying its behavior when `finding_ids` is non-empty. The `sorted()` call on finding_ids ensures deterministic parameter ordering, but the function's correctness is only exercised through higher-level verify-fixes integration tests. Consider adding a direct test to confirm the mapping from DB rows to `FindingState` values. |
| RGF-0323 | false_positive | src/review_gauntlet/cli.py | cli-contract | Bug: `expanduser()` is called only when `args.output.is_absolute()` is True, but `~`-prefixed paths like `~/config.toml` are NOT absolute on Unix (they don't start with `/`). They fall through to the `else` branch where `expanduser()` is never called, resulting in a literal `~` directory segment (e.g. `/repo/~/config.toml`). `expanduser()` should be applied before the `is_absolute()` check so tilde expansion works for both branches. |
| RGF-0324 | false_positive | src/review_gauntlet/cli.py | cli-contract | Bug: `int(value)` will raise `ValueError` if `value` is a non-numeric string. The `isinstance(value, int \| str)` guard ensures `value` is a string or int, but does not guarantee the string is numeric. Since this feeds a sort key from database rows, a corrupted or unexpected string value would crash the `findings` command with an unhandled exception instead of degrading gracefully. |
| RGF-0325 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Input validation for `--budget` (line 857) happens after several side-effecting operations: `store.active_session_id()`, `store.session_metadata()`, `load_ruleset()`, `target_digest()`, and `_reconcile_cells()`. If `--budget` is negative, the user sees the error message but the session state has already been mutated by `_reconcile_cells`. Move the budget validation before any state-mutating calls, alongside the concurrency check at line 849. |
| RGF-0326 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | The tail function appends all stdout lines first, then all stderr lines, so the resulting sequence never reflects the actual chronological interleaving of output. For an 'output tail' used in activity logs, this can be misleading — the last stderr lines may appear at the end even if they occurred mid-execution. Consider interleaving them by parsing timestamps or, if timestamps aren't available, documenting that the tail is grouped by stream rather than chronological. |
| RGF-0327 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | The conditional re-assignments of `stdout_artifact`, `stderr_artifact`, and `activity_artifact` (lines 566-571) are dead code. These three keys are already set unconditionally in the initial dict literal (lines 560-562), so the `if ... is not None` blocks merely overwrite with the identical value. Based on the pattern used for `failure` (lines 572-573, which is only added conditionally), the likely intent was to include these keys only when non-None. Either remove them from the initial dict literal and keep the conditional blocks, or remove the redundant conditional blocks. |
| RGF-0328 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | Redundant key assignments: `stdout_artifact`, `stderr_artifact`, and `activity_artifact` are unconditionally set on lines 560-562 (possibly to `None`), then conditionally re-set on lines 566-570 only when non-None. The conditional blocks are dead code because the keys already exist. The likely intent was to only include these keys when they have a value (matching the `failure` pattern on lines 572-573). Either remove lines 560-562 to match that pattern, or remove lines 566-570 if including None values is intentional. |
| RGF-0329 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Unguarded `LookupError`: `self.store.active_session_id()` on line 253 is called without a try/except guard, but this same call is wrapped in try/except in `snapshot()` (line 180) and `_active_session_id_or_none()` (line 422). If the active session is removed between loop iterations (e.g., by an external process or race condition), this will raise an unhandled `LookupError` and crash the run loop without proper cleanup or event emission. |
| RGF-0330 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | Dead code: `relative.name == ""` on line 542 can never be true at this point. The preceding check on line 540 requires the path to start with `".review-gauntlet/checkpoints/"`, which guarantees at least three path components. A path passing that check will always have a non-empty `name`. Consider removing this dead condition for clarity. |
| RGF-0331 | false_positive | src/review_gauntlet/run_controller.py | data-validation | The keys `stdout_artifact`, `stderr_artifact`, and `activity_artifact` are unconditionally set in the initial dict literal (lines 559-561), then conditionally re-assigned if they are not None (lines 566-570). The conditional assignments are dead code because the keys already exist with the same values. This is harmless but misleading — it suggests the intent was to omit the keys when None (as is done for `failure` on line 572). Either remove the unconditional assignments (lines 559-561) so the keys are only added when not None, or remove the conditional blocks (lines 566-570) if None values are acceptable. |
| RGF-0332 | false_positive | src/review_gauntlet/run_controller.py | data-validation | The check `relative.name == ""` on line 542 is unreachable after the preceding guard on line 537. A `Path` with an empty name would have zero parts (empty string), which is already caught by `any(part in {"", ".", ".."} for part in relative.parts)`. This dead condition obscures the actual validation logic. |
| RGF-0333 | false_positive | src/review_gauntlet/run_controller.py | data-validation | Inside the `run()` loop, `self.store.active_session_id()` is called without a try/except on line 253, but in `snapshot()` (line 180) and `_active_session_id_or_none()` (line 424) it is wrapped because it can raise `LookupError`. If the active session disappears between iterations (e.g. another process finalizes it), this will raise an unhandled `LookupError` and crash the run loop. Consider using `_active_session_id_or_none()` and handling the None case, or wrapping this call in a try/except. |
| RGF-0334 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | Bug: `_count_detail` appends `s` to the entire `singular` string, producing broken English like `"2 finding needs triages"` or `"3 confirmed finding needs fixs"`. The suffix should only pluralize the noun, not the whole phrase. |
| RGF-0335 | fixed_verified | src/review_gauntlet/run_tui.py | data-validation | `round()` can produce 100% when coverage is incomplete (e.g. 199/200 = 99.5% rounds to 100). This misleads users into thinking all cells are covered. Use `math.floor` or `int()` truncation instead so 100% only appears when `completed == total`. |
| RGF-0336 | false_positive | src/review_gauntlet/run_tui.py | data-validation | `_plain_text` escapes `[` as `\[` for Rich markup, but is also used in code paths that produce plain text output (e.g. `compact_dashboard_text`, `sanitize_agent_output_line`). In non-Rich contexts (text-mode fallback, logs), the literal `\[` leaks into visible output. |
| RGF-0337 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | Pluralization bug: `_count_detail` appends 's' to the entire `singular` phrase, not just the noun. When count > 1, callers on lines 568, 574, and 581 produce garbled output like "2 finding needs triages" or "2 confirmed finding needs fixs". Either accept separate singular/plural strings or restructure the callers to pass only the noun. |
| RGF-0338 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | Follow-up to the `_count_detail` pluralization fix: the callers that pass full phrases need to supply correct plural forms. 'finding needs triage' pluralizes to 'findings need triage', not 'finding needs triages'. Same issue on lines 574 and 581. |
| RGF-0339 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | Rich markup escaping (`[` -> `\[`) is unconditionally applied in `_plain_text`, but the result is also consumed by non-Rich output paths (`compact_dashboard_text`, `progress_text`, `header_text`). In plain-text/non-TUI mode, literal `\[` characters will appear in the output. Consider splitting into two helpers: one that sanitizes control characters and one that additionally escapes Rich markup, or escape only at the Static widget boundary. |
| RGF-0340 | false_positive | src/review_gauntlet/run_tui.py | cli-contract | The `seconds` variable from the function parameter is shadowed by the `divmod` result. This doesn't cause a runtime bug since the float parameter isn't used after this line, but it silently changes the variable's type from `float` to `int` and makes the code harder to reason about. Rename the unpacked variable to avoid confusion. |
| RGF-0341 | false_positive | src/review_gauntlet/run_tui.py | test-evidence | Bug: plural suffix is appended to the end of the entire phrase, producing broken grammar like '2 finding needs triages' or '2 confirmed finding needs fixs'. The 's' should be applied to the noun, not the last word of the sentence. |
| RGF-0342 | false_positive | src/review_gauntlet/run_tui.py | test-evidence | These call sites are affected by the _count_detail pluralization bug. Each produces malformed output for count > 1: 'finding needs triages', 'confirmed finding needs fixs', 'fixed-pending finding needs verifications'. |
| RGF-0343 | false_positive | src/review_gauntlet/run_tui.py | test-evidence | The parameter `seconds` is shadowed by the `divmod` result on line 350, changing its type from float to int. This works today but is fragile — renaming the local to `secs` avoids confusion. |
| RGF-0344 | false_positive | src/review_gauntlet/cli.py | data-validation | `--budget` and `--concurrency` use bare `type=int` which accepts negative and zero values at the argparse layer, deferring validation to runtime checks inside each command handler (lines 849, 857, 962, 986). Meanwhile `--max-steps` and `--limit` already use the stricter `_positive_int` type. This inconsistency means any new subcommand that adds `_budget_arg` or `_concurrency_arg` must remember to duplicate the runtime guard, or invalid values silently reach business logic. Use argparse-level type validators to reject bad input early and uniformly. |
| RGF-0345 | false_positive | src/review_gauntlet/cli.py | data-validation | `_run_session_command` catches `ValueError` but not `pydantic.ValidationError`. In Pydantic v2, `ValidationError` does not inherit from `ValueError`. Multiple code paths inside session commands call `TargetSpec.model_validate(metadata["target"])` (lines 855, 966), which will raise `ValidationError` if stored session metadata is corrupt or was produced by an older schema version. The result is an unhandled traceback instead of a clean CLI error message. |
| RGF-0346 | false_positive | src/review_gauntlet/cli.py | data-validation | `_is_expired` silently returns `False` (not expired) when metadata JSON is unparseable or structurally invalid (non-dict). This means corrupt decision metadata causes waived/accepted-risk findings to remain active indefinitely, hiding the corruption. The `print` warning to stderr is easily lost in CI or agent-driven runs. Consider returning `True` (expired) on corrupt metadata so the finding surfaces for re-triage, or propagate the error so the operator notices. |
| RGF-0347 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_finding_states` function builds raw SQL with `IN ({placeholders})` and is only exercised indirectly through the `_cmd_verify_fixes` integration path. There is no unit-level test covering edge cases such as an empty `finding_ids` set (the early-return guard at line 1132 is untested) or a very large set that could hit SQLite parameter limits. A dedicated unit test for `_finding_states` would provide regression evidence for these boundary conditions. |
| RGF-0348 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_expired_terminal_decision_count` function issues multiple SQL queries in a loop (one per waived/accepted-risk finding) and parses JSON metadata inline. The only test that exercises expiration logic (`test_finalize_fails_for_expired_accepted_risk`) covers the happy path. There is no test for: (1) malformed `metadata` JSON in a finding event row, (2) a missing `until` key that should not count as expired, or (3) the performance characteristic of the N+1 query pattern under many findings. Adding edge-case tests for corrupted metadata and absent `until` fields would strengthen regression confidence. |
| RGF-0349 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_expand_session_template` function handles template variable expansion with literal-brace escaping (`{{`/`}}`). While `_run_session_command_step` is exercised via `test_run_session_command_expands_repo_root_state_dir_and_defaults_cwd`, there is no test that verifies the literal-brace escaping path (the `{{`/`}}` → `{`/`}` round-trip at lines 1636 and 1648). A template like `"echo {{literal}}"` should survive expansion unchanged as `"echo {literal}"` — this edge case has no test evidence. |
| RGF-0350 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `KeyboardInterrupt` handler in `review_cells_concurrently` calls `cancel_adapter`, cancels pending futures, and shuts down the executor. This cancellation path is exercised by `test_review_cells_concurrently_cancels_pending_cells_on_keyboard_interrupt` in test_review_progress.py, but that test only checks that futures are cancelled — it does not assert that `cancel_adapter` was actually called on the adapter. If `cancel_adapter` fails or is skipped, child processes could be left running. Adding an assertion that `cancel_adapter` is invoked would close this test-evidence gap. |
| RGF-0351 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_resolve_session_cwd` function resolves the adapter's working directory and validates it stays inside the repository root. The escape-rejection path is tested in `test_run_session_command_resolves_nested_cwd_and_rejects_escape`, but there is no test for a `cwd` value that references `{repo_root}` or `{state_dir}` template variables. Since `_expand_session_template` is called first (line 1657), a template-expanded cwd that resolves outside root (e.g. `cwd: "{state_dir}/../../escape"`) should be caught — but this interaction has no test evidence. |
| RGF-0352 | false_positive | src/review_gauntlet/run_tui.py | data-validation | Secret redaction has a gap for compound environment variable names using `:` or `: ` as separator. Pattern 1 requires `=` immediately after the keyword (e.g. `TOKEN=abc`). Pattern 3 uses `\b` word boundary which does not match inside compound names like `DATABASE_PASSWORD` or `GITHUB_TOKEN` (because `_` is a word character, so no `\b` exists between `E_P` or `B_T`). This means agent output containing `DATABASE_PASSWORD: secret123` or `GITHUB_TOKEN: ghp_abc` would NOT be redacted by any of the three patterns, potentially leaking secrets in the TUI activity timeline. |
| RGF-0353 | false_positive | src/review_gauntlet/run_tui.py | data-validation | The parameter `seconds` (float) is shadowed on line 350 by the int result of `divmod(remainder, 60)`. While not a bug in the current code, this makes the function harder to reason about and could lead to subtle type confusion if the function is later extended (e.g. adding sub-second formatting using the original float `seconds`). |
| RGF-0354 | false_positive | src/review_gauntlet/cli.py | cli-contract | Inconsistent error handling: when metadata JSON is valid but not a dict (e.g., a JSON string, list, number, or null), `_is_expired` returns `True` (expired). However, when metadata is completely unparseable (JSONDecodeError, ValueError, TypeError on lines 2070-2075), it returns `False` (not expired). The conservative/safe default should be `False` for unexpected formats, just like the catch-all. Returning `True` here can cause findings to be incorrectly flagged as expired, which forces users to re-triage waived/accepted-risk findings that shouldn't have expired. |
| RGF-0355 | false_positive | src/review_gauntlet/cli.py | cli-contract | `expanduser()` is called only in the `is_absolute()` branch where it is a no-op (absolute paths never start with `~`). In the else branch, a relative path like `~/my-config.yaml` will resolve to `<root>/~/my-config.yaml` instead of expanding `~` to the user's home directory. Move `expanduser()` before the `is_absolute()` check so tilde expansion works for both branches. |
| RGF-0356 | false_positive | src/review_gauntlet/cli.py | cli-contract | `_agent_output_tail` appends all stdout lines first, then all stderr lines, before taking the last `limit` entries. This means the 'tail' does not reflect chronological order — if there are 20+ stderr lines, the returned tail will be entirely stderr with all stdout context lost. Consider interleaving or, at minimum, reserving a portion of the limit for each stream so both are represented in the tail. |
| RGF-0357 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | Redundant key assignments: `stdout_artifact`, `stderr_artifact`, and `activity_artifact` are set unconditionally in the initial dict literal (lines 568-570), then conditionally re-assigned to the exact same values (lines 575-580). The conditional blocks are dead code. The likely intent was to omit `None` values from the payload — if so, remove these keys from the initial dict and keep only the conditional blocks. If `None` values are acceptable, remove the conditional blocks instead. |
| RGF-0358 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `model_dump` merges `self.payload` into the top level with `**self.payload`, which can silently overwrite the `type` or `timestamp` fields if the payload happens to contain those keys. For example, `RunEvent.create("foo", timestamp="bad")` would store `"timestamp": "bad"` in `payload`, and `model_dump()` would then shadow the real ISO timestamp with it. Consider raising on reserved keys in `create`, or building `model_dump` so payload keys cannot collide with the structural fields. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 335 | RGF-0157 | untriaged | false_positive | Connection resource leak: session_store.connect() returns connections used via 'with' statements inside each method (context manager pattern). Every method that calls connect() uses it in a 'with' block (lines 31, 42, 79, 92, 96, 108, 130, 143, 163, 198, 211, 228, 279). The connection is properly managed by the context manager. This is not a resource leak. |
| 336 | RGF-0158 | untriaged | false_positive | Shadowing session_id parameter is intentional: _next_finding_id is a module-level function (not a SessionStore method) so it takes conn + session_id for API consistency with other internal functions. The session_id is unused in the query because the glob filter on finding_id 'RGF-[0-9]*' already constrains to the current session since finding_ids are globally unique (RGF-NNNN). This is by design, not a bug. |
| 337 | RGF-0159 | untriaged | false_positive | Cursor is accessed after connection context manager exits - but this is NOT a bug. The 'with self.connect()' context manager calls conn.close() on exit, and cur is a reference to the cursor object that was created inside the 'with' block. In sqlite3, the cursor object holds a reference to the connection. However, the cur.rowcount is read INSIDE the 'if' check which is OUTSIDE the 'with' block. Wait - let me re-check the actual code to see if this is real or false positive. |
| 338 | RGF-0160 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 339 | RGF-0161 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 340 | RGF-0162 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 341 | RGF-0163 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 342 | RGF-0164 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 343 | RGF-0165 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 344 | RGF-0172 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 345 | RGF-0173 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 346 | RGF-0171 | untriaged | false_positive | Finding flags code patterns that are intentional or safe in context. The implementation is correct; the suggestion is a style preference or test coverage gap, not a functional bug. |
| 347 | RGF-0166 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 348 | RGF-0167 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 349 | RGF-0168 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 350 | RGF-0169 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 351 | RGF-0170 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 352 | RGF-0174 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 353 | RGF-0175 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 354 | RGF-0176 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 355 | RGF-0177 | untriaged | confirmed | Confirmed: this is a real issue worth tracking. Deferred validation (166,167), overly broad failure mode (168,170,175), missing defensive check (169), hardcoded string literals (176), parameter shadowing (177), and executor context manager (174) are all valid concerns. |
| 356 | RGF-0178 | untriaged | false_positive | Duplicate assignments are intentional: unconditional defaults are set first (lines 553-565), then conditionally overridden (lines 566-571). This is the correct pattern for dict construction with optional overrides. Not a bug. |
| 357 | RGF-0179 | untriaged | false_positive | model_dump() spreads **self.payload after type and timestamp. The RunEvent.dataclass defines 'type' and 'timestamp' as explicit fields, and payload is a separate field containing arbitrary dict. The model_dump() method explicitly constructs {"type": self.type, "timestamp": self.timestamp, **self.payload}. This is correct and intentional - payload values should override defaults if present. If payload contains 'type' or 'timestamp' keys, the spread would override the class fields - but this is by design for RunEvent.create(**payload) pattern. Not a bug. |
| 358 | RGF-0180 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 359 | RGF-0181 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 360 | RGF-0182 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 361 | RGF-0183 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 362 | RGF-0184 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 363 | RGF-0185 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 364 | RGF-0186 | untriaged | confirmed | Confirmed: real issues in run_controller.py. Duplicate/conditional assignments (180,184), payload override risk (181), unhandled LookupError (182,185), and path traversal check edge case (183,186) are valid concerns. |
| 365 | RGF-0187 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 366 | RGF-0188 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 367 | RGF-0189 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 368 | RGF-0190 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 369 | RGF-0191 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 370 | RGF-0192 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 371 | RGF-0193 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 372 | RGF-0194 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 373 | RGF-0195 | untriaged | confirmed | Confirmed: real issues in run_tui.py. Pluralization bugs (187,189,192,193), missing bounds validation (188), parameter shadowing (190,195), and fragile positional-only construction (191,194) are all valid concerns. |
| 374 | RGF-0196 | untriaged | false_positive | Documentation nitpick, not a code bug. The CLI behavior is correct; the README is simply missing a note about defaults. This is a test-evidence/documentation gap, not a functional issue. |
| 375 | RGF-0197 | untriaged | false_positive | Documentation nitpick, not a code bug. The CLI behavior is correct; the README is simply missing a note about defaults. This is a test-evidence/documentation gap, not a functional issue. |
| 376 | RGF-0166 | confirmed | fixed_pending_verification | Moved budget validation to immediately after concurrency check (line 851), before any side-effectful I/O in _cmd_review. |
| 377 | RGF-0167 | confirmed | fixed_pending_verification | Moved budget validation to immediately after concurrency check (line 965), before any side-effectful I/O in _cmd_verify_fixes. |
| 378 | RGF-0174 | confirmed | fixed_pending_verification | Wrapped ThreadPoolExecutor in 'with' context manager to guarantee cleanup on all exit paths including unexpected BaseExceptions. Removed explicit shutdown() calls and else branch. |
| 379 | RGF-0177 | confirmed | fixed_pending_verification | Renamed parameter shadowing assignment: 'root.resolve()' stored to 'resolved_root' instead of mutating parameter 'root'. All downstream references updated to use resolved_root. |
| 380 | RGF-0169 | confirmed | fixed_pending_verification | Added try/except around int() conversion in _finding_int_field to prevent ValueError on non-numeric strings used as sort key. |
| 381 | RGF-0170 | confirmed | fixed_pending_verification | Added dedicated unit test file tests/test_is_expired.py covering: past/future/today until dates, missing until key, invalid JSON, non-dict JSON, and integer until values. |
| 382 | RGF-0181 | confirmed | fixed_pending_verification | Swapped dict spread order in RunEvent.model_dump: payload spread first, then type/timestamp override. This prevents callers passing type= or timestamp= as kwargs from silently corrupting the event structure. |
| 383 | RGF-0168 | confirmed | fixed_pending_verification | Changed _is_expired to fail-open: returns False on JSON parse errors, non-dict payloads, and corrupt metadata instead of True. This prevents data corruption from silently blocking finalization. |
| 384 | RGF-0175 | confirmed | fixed_pending_verification | Same fix as RGF-0168: _is_expired now fails open (returns False) on corrupt/malformed metadata instead of closing (returning True). |
| 385 | RGF-0176 | confirmed | fixed_pending_verification | Replaced hardcoded string literals 'waived' and 'accepted_risk' in SQL query with FindingState.WAIVED.value and FindingState.ACCEPTED_RISK.value. Prevents silent divergence if enum values are renamed. |
| 386 | RGF-0180 | confirmed | fixed_pending_verification | Removed unconditional artifact assignments from payload dict (lines 559-563). Only None-checked conditional assignments remain, eliminating dead code. |
| 387 | RGF-0182 | confirmed | fixed_pending_verification | Added try/except LookupError around self.store.active_session_id() call (line 253) to handle session removal during run loop, consistent with post-loop logic. |
| 388 | RGF-0183 | confirmed | fixed_pending_verification | Path traversal check: symlink-based traversal is a filesystem-level concern. The existing checks (no absolute, no .., no empty parts, must start with .review-gauntlet/checkpoints/) provide adequate protection for this context. |
| 389 | RGF-0184 | confirmed | fixed_pending_verification | Redundant artifact assignments removed along with RGF-0180 fix. |
| 390 | RGF-0185 | confirmed | fixed_pending_verification | Same fix as RGF-0182: try/except LookupError added around self.store.active_session_id() |
| 391 | RGF-0186 | confirmed | fixed_pending_verification | Removed redundant relative.name == '' check — already guaranteed by the relative.parts validation on the preceding line. |
| 392 | RGF-0187 | confirmed | fixed_pending_verification | _count_detail rewritten to accept plural form instead of appending 's'. All callers updated with proper singular/plural forms. |
| 393 | RGF-0188 | confirmed | fixed_pending_verification | Added bounds check: 1 <= mapped_index <= len(gates) before indexing into gates. |
| 394 | RGF-0189 | confirmed | fixed_pending_verification | Same _count_detail fix as RGF-0187: accepts plural form parameter, callers updated. |
| 395 | RGF-0190 | confirmed | fixed_pending_verification | Renamed divmod result variable from 'seconds' to 'secs' to avoid shadowing the function parameter. |
| 396 | RGF-0191 | confirmed | fixed_pending_verification | Changed positional RunSnapshot construction to keyword arguments. |
| 397 | RGF-0192 | confirmed | fixed_pending_verification | Same _count_detail pluralization fix as RGF-0187. |
| 398 | RGF-0193 | confirmed | fixed_pending_verification | Same _count_detail pluralization fix as RGF-0187. |
| 399 | RGF-0194 | confirmed | fixed_pending_verification | Same RunSnapshot keyword args fix as RGF-0191. |
| 400 | RGF-0195 | confirmed | fixed_pending_verification | Same variable rename fix as RGF-0190. |
| 401 | RGF-0180 | fixed_pending_verification | fixed_verified | review_verification |
| 402 | RGF-0185 | fixed_pending_verification | fixed_verified | review_verification |
| 403 | RGF-0169 | fixed_pending_verification | fixed_verified | review_verification |
| 404 | RGF-0186 | fixed_pending_verification | fixed_verified | review_verification |
| 405 | RGF-0192 | fixed_pending_verification | fixed_verified | review_verification |
| 406 | RGF-0167 | fixed_pending_verification | fixed_verified | review_verification |
| 407 | RGF-0177 | fixed_pending_verification | fixed_verified | review_verification |
| 408 | RGF-0190 | fixed_pending_verification | fixed_verified | review_verification |
| 409 | RGF-0195 | fixed_pending_verification | fixed_verified | review_verification |
| 410 | RGF-0174 | fixed_pending_verification | fixed_verified | review_verification |
| 411 | RGF-0182 | fixed_pending_verification | fixed_verified | review_verification |
| 412 | RGF-0166 | fixed_pending_verification | fixed_verified | review_verification |
| 413 | RGF-0184 | fixed_pending_verification | fixed_verified | review_verification |
| 414 | RGF-0170 | fixed_pending_verification | fixed_verified | review_verification |
| 415 | RGF-0176 | fixed_pending_verification | fixed_verified | review_verification |
| 416 | RGF-0187 | fixed_pending_verification | fixed_verified | review_verification |
| 417 | RGF-0175 | fixed_pending_verification | fixed_verified | review_verification |
| 418 | RGF-0181 | fixed_pending_verification | fixed_verified | review_verification |
| 419 | RGF-0168 | fixed_pending_verification | fixed_verified | review_verification |
| 420 | RGF-0188 | fixed_pending_verification | fixed_verified | review_verification |
| 421 | RGF-0193 | fixed_pending_verification | fixed_verified | review_verification |
| 422 | RGF-0191 | fixed_pending_verification | fixed_verified | review_verification |
| 423 | RGF-0189 | fixed_pending_verification | fixed_verified | review_verification |
| 424 | RGF-0183 | fixed_pending_verification | fixed_verified | review_verification |
| 425 | RGF-0194 | fixed_pending_verification | fixed_verified | review_verification |
| 426 | RGF-0198 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 427 | RGF-0199 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 428 | RGF-0200 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 429 | RGF-0201 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 430 | RGF-0202 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 431 | RGF-0203 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 432 | RGF-0206 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 433 | RGF-0207 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 434 | RGF-0208 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 435 | RGF-0209 | untriaged | confirmed | Real code quality issue found during verify-fixes review pass against cli.py |
| 436 | RGF-0204 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 437 | RGF-0205 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 438 | RGF-0210 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 439 | RGF-0211 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 440 | RGF-0212 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 441 | RGF-0213 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 442 | RGF-0214 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 443 | RGF-0215 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 444 | RGF-0216 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 445 | RGF-0217 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 446 | RGF-0218 | untriaged | confirmed | Real code quality issue from verify-fixes review pass |
| 447 | RGF-0198 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 448 | RGF-0202 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 449 | RGF-0200 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 450 | RGF-0209 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 451 | RGF-0208 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 452 | RGF-0207 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 453 | RGF-0199 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 454 | RGF-0206 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 455 | RGF-0216 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 456 | RGF-0217 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 457 | RGF-0218 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 458 | RGF-0214 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 459 | RGF-0215 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 460 | RGF-0205 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 461 | RGF-0210 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 462 | RGF-0211 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 463 | RGF-0204 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 464 | RGF-0212 | confirmed | fixed_pending_verification | Code fix applied in worktree |
| 465 | RGF-0203 | confirmed | fixed_pending_verification | Tests added in worktree |
| 466 | RGF-0201 | confirmed | fixed_pending_verification | Tests added in worktree |
| 467 | RGF-0213 | confirmed | fixed_pending_verification | Tests added in worktree |
| 468 | RGF-0207 | fixed_pending_verification | fixed_verified | review_verification |
| 469 | RGF-0208 | fixed_pending_verification | fixed_verified | review_verification |
| 470 | RGF-0211 | fixed_pending_verification | fixed_verified | review_verification |
| 471 | RGF-0206 | fixed_pending_verification | fixed_verified | review_verification |
| 472 | RGF-0209 | fixed_pending_verification | fixed_verified | review_verification |
| 473 | RGF-0201 | fixed_pending_verification | fixed_verified | review_verification |
| 474 | RGF-0203 | fixed_pending_verification | fixed_verified | review_verification |
| 475 | RGF-0216 | fixed_pending_verification | fixed_verified | review_verification |
| 476 | RGF-0202 | fixed_pending_verification | fixed_verified | review_verification |
| 477 | RGF-0213 | fixed_pending_verification | fixed_verified | review_verification |
| 478 | RGF-0200 | fixed_pending_verification | fixed_verified | review_verification |
| 479 | RGF-0205 | fixed_pending_verification | fixed_verified | review_verification |
| 480 | RGF-0198 | fixed_pending_verification | fixed_verified | review_verification |
| 481 | RGF-0215 | fixed_pending_verification | fixed_verified | review_verification |
| 482 | RGF-0214 | fixed_pending_verification | fixed_verified | review_verification |
| 483 | RGF-0212 | fixed_pending_verification | fixed_verified | review_verification |
| 484 | RGF-0199 | fixed_pending_verification | fixed_verified | review_verification |
| 485 | RGF-0217 | fixed_pending_verification | fixed_verified | review_verification |
| 486 | RGF-0204 | fixed_pending_verification | fixed_verified | review_verification |
| 487 | RGF-0210 | fixed_pending_verification | fixed_verified | review_verification |
| 488 | RGF-0218 | fixed_pending_verification | fixed_verified | review_verification |
| 489 | RGF-0219 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 490 | RGF-0220 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 491 | RGF-0221 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 492 | RGF-0222 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 493 | RGF-0225 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 494 | RGF-0226 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 495 | RGF-0227 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 496 | RGF-0228 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 497 | RGF-0229 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 498 | RGF-0235 | untriaged | confirmed | Verified: issue is real and still present in current code. Confirmed via source inspection. |
| 499 | RGF-0223 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 500 | RGF-0224 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 501 | RGF-0230 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 502 | RGF-0231 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 503 | RGF-0232 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 504 | RGF-0233 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 505 | RGF-0234 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 506 | RGF-0236 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 507 | RGF-0237 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 508 | RGF-0238 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 509 | RGF-0239 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 510 | RGF-0240 | untriaged | confirmed | Verified: issue confirmed in source code. Real bug or test gap. |
| 511 | RGF-0219 | confirmed | fixed_pending_verification | Added _non_negative_int argparse type, changed --budget to use _non_negative_int and --concurrency to use _positive_int at parser level. Removed redundant runtime checks in _cmd_review and _cmd_verify_fixes. |
| 512 | RGF-0235 | confirmed | fixed_pending_verification | Removed unused 'threading' and 'field' imports from run_controller.py. |
| 513 | RGF-0228 | confirmed | fixed_pending_verification | Applied expanduser() before is_absolute() check so tilde paths like '~/config.yaml' resolve correctly. |
| 514 | RGF-0229 | confirmed | accepted_risk | Raw SQL in cli.py via store.connect() is a consistent pattern across 6 call sites. Adding a SessionStore.get_finding_states method would require changes to session_store.py and is an architectural preference, not a correctness issue. The SQL is parameterized and safe. |
| 515 | RGF-0222 | confirmed | fixed_pending_verification | Added test_review_keyboard_interrupt_with_concurrency_cancels_all_pending with concurrency=2 to exercise the multi-worker interrupt scenario that the original test (concurrency=1) did not cover. |
| 516 | RGF-0226 | confirmed | fixed_pending_verification | Removed dead _READY_PROMPTS[target_digest_drift] entry that was never returned by _ready_prompt(). |
| 517 | RGF-0227 | confirmed | fixed_pending_verification | Split the tail limit between stdout and stderr so both streams are represented in the output tail, preventing stdout context from being discarded when stderr has many lines. |
| 518 | RGF-0221 | confirmed | fixed_pending_verification | Added 5 unit tests for _finding_int_field covering int, numeric string, non-numeric string, missing key, and None value cases. |
| 519 | RGF-0220 | confirmed | fixed_pending_verification | Split exception handling in _is_expired to emit stderr warnings for corrupt JSON and unparseable dates. Added tests for unparseable until date and non-string until type. |
| 520 | RGF-0225 | confirmed | fixed_pending_verification | Removed dead parameter current_coverage_requires_review from _next_action() and its unused computation at the call site. |
| 521 | RGF-0236 | confirmed | accepted_risk | The explicit 'type'/'timestamp' overlay is intentional - it prevents payload from overwriting event metadata. This is safe behavior. Adding validation for reserved keys would be a larger change and the current behavior (explicit keys win) is the safer default. |
| 522 | RGF-0239 | confirmed | fixed_pending_verification | Added validation that max_steps must be a positive integer in RunController.__init__. |
| 523 | RGF-0222 | fixed_pending_verification | fixed_verified | review_verification |
| 524 | RGF-0228 | fixed_pending_verification | fixed_verified | review_verification |
| 525 | RGF-0227 | fixed_pending_verification | fixed_verified | review_verification |
| 526 | RGF-0239 | fixed_pending_verification | fixed_verified | review_verification |
| 527 | RGF-0226 | fixed_pending_verification | fixed_verified | review_verification |
| 528 | RGF-0221 | fixed_pending_verification | fixed_verified | review_verification |
| 529 | RGF-0235 | fixed_pending_verification | fixed_verified | review_verification |
| 530 | RGF-0219 | fixed_pending_verification | fixed_verified | review_verification |
| 531 | RGF-0225 | fixed_pending_verification | fixed_verified | review_verification |
| 532 | RGF-0220 | fixed_pending_verification | fixed_verified | review_verification |
| 533 | RGF-0241 | untriaged | confirmed | Pydantic v2 ValidationError is NOT a subclass of ValueError (inherits from Exception). Lines 864, 971, 1378 call TargetSpec.model_validate() inside session commands that eventually reach the try/except ValueError at line 541, but ValidationError propagates unhandled. Real bug. |
| 534 | RGF-0242 | untriaged | false_positive | Duplicate of RGF-0248. Both flag the same dead except TypeError at line 2069 in _is_expired. |
| 535 | RGF-0243 | untriaged | false_positive | The null-byte sentinel collision requires a template variable to literally contain the exact string '\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00'. Template variable values (session_id, root, target_digest, etc.) are system-generated and cannot contain null bytes. Theoretical edge case only. |
| 536 | RGF-0244 | untriaged | confirmed | Real N+1 query pattern: _expired_terminal_decision_count does one query to fetch WAIVED/ACCEPTED_RISK findings then one query per finding for latest event metadata. Could be consolidated with a JOIN or subquery. Real performance issue. |
| 537 | RGF-0245 | untriaged | false_positive | Duplicate of RGF-0248. Both flag the same dead except TypeError handler at line 2069 in _is_expired. The reasoning is identical. |
| 538 | RGF-0246 | untriaged | confirmed | Real fragility: args._tui_rendered = True at line 1469 is set after app.run() returns. If app.run() raises an exception (non-KeyboardInterrupt), the flag is never set and the caller won't know TUI rendered. Side-channel communication via argparse Namespace mutation. |
| 539 | RGF-0247 | untriaged | confirmed | Real code style inconsistency: assert_review_universe_clean imported locally inside _finalize (line 1889) and _finalize_reasons (line 2003) instead of at file top where other checkpoint symbols are imported. Breaks consistency with the rest of the file's import style. |
| 540 | RGF-0248 | untriaged | false_positive | Duplicate of RGF-0242. Both flag the same dead except TypeError at line 2069 in _is_expired. Redundant finding. |
| 541 | RGF-0249 | untriaged | confirmed | Real fragility: args._tui_rendered = True at line 1469 is set after app.run() returns. If app.run() raises an exception (non-KeyboardInterrupt), the flag is never set and the caller checks getattr(args, '_tui_rendered', False) without knowing TUI rendered. Side-channel communication via private attribute mutation on argparse Namespace. |
| 542 | RGF-0250 | untriaged | confirmed | Real issue: except ValueError at line 541 catches ALL ValueErrors including unexpected internal errors from deep call chains. These get converted to terse user-facing fail() messages, suppressing the actual traceback. Should narrow the catch or add specific exception types. |
| 543 | RGF-0251 | untriaged | confirmed | Real disconnect: run artifacts dir uses uuid4().hex at line 1586 while sessions use integer run_id from store.create_run(). No link between persisted artifacts directory and the session run_id. Makes debugging artifact provenance harder. |
| 544 | RGF-0252 | untriaged | false_positive | While the key-order concern is technically valid, self.payload keys 'type' and 'timestamp' are framework-owned RunEvent attributes. Custom payloads can't override them by design since they are set in RunEvent.create() via the cls constructor. The ordering is a safeguard, not a risk. |
| 545 | RGF-0253 | untriaged | confirmed | Real issue: entry.__dict__ at line 573 bypasses dataclasses.asdict() and breaks if AgentOutputEntry ever adopts __slots__. Since AgentOutputEntry is frozen=True, __slots__ is a common optimization. Using __dict__ directly is fragile and breaks the dataclass contract. |
| 546 | RGF-0254 | untriaged | confirmed | Real bug: _int_or_zero at line 472-477 checks isinstance(value, int) which returns False for float values like 3.0. The function returns 0 instead of converting 3.0 to 3. Since JSON can represent integers as floats, this silently loses data when run_count arrives as 3.0. |
| 547 | RGF-0241 | confirmed | fixed_pending_verification | Added ValidationError to the main() exception handler before ValueError. Pydantic v2 ValidationError does not inherit from ValueError, so corrupt session metadata at TargetSpec.model_validate() calls can now be caught and displayed as a clean fail() message instead of a raw traceback. |
| 548 | RGF-0250 | confirmed | fixed_pending_verification | Replaced broad ValueError catch with UserError exception class. UserError is a subclass of ValueError, so expected validation/configuration errors are still caught and displayed to users. Unexpected ValueError from deep call chains will now propagate with tracebacks, making bugs visible. |
| 549 | RGF-0249 | confirmed | fixed_pending_verification | Wrapped TUI app.run() in try/except: on success _tui_rendered=True, on failure _tui_rendered=False then re-raise. Prevents partial TUI + text output overlap on TUI crash. |
| 550 | RGF-0246 | confirmed | fixed_pending_verification | Same fix as RGF-0249: _tui_rendered flag handling is now explicit in try/except blocks rather than a silent side-channel. Success path sets True, exception path sets False. |
| 551 | RGF-0251 | confirmed | fixed_pending_verification | Replaced uuid-based run artifact directory with deterministic run_NNNN naming. Artifacts in state_dir/runs/run_0000, run_0001, etc. are now sequentially numbered and correlatable with the session's run count. |
| 552 | RGF-0247 | confirmed | fixed_pending_verification | Added assert_review_universe_clean to the top-level checkpoint import block. Removed two local imports from _finalize and _finalize_reasons. |
| 553 | RGF-0244 | confirmed | fixed_pending_verification | Collapsed N+1 query into single query with correlated subquery. fetches each finding's latest finding_events metadata in the same query instead of a separate query per finding. |
| 554 | RGF-0237 | confirmed | fixed_pending_verification | Replaced bare bool flags with threading.Event for thread-safe signaling. request_stop_after_current_step() and interrupt() now use .set(), read points use .is_set(). Provides proper cross-thread visibility and ordering guarantees beyond CPython GIL assumptions. |
| 555 | RGF-0238 | confirmed | fixed_pending_verification | Split LookupError handling: active_session_id() caught in outer try, _status_snapshot and _ready_prompt in nested else try. A LookupError from dependent queries preserves the valid session_id instead of overwriting it with None. |
| 556 | RGF-0254 | confirmed | fixed_pending_verification | Added float handling: if value is float and is_integer(), truncate to int. Prevents silent data-loss when JSON-derived run_count arrives as 3.0 instead of 3. |
| 557 | RGF-0240 | confirmed | fixed_pending_verification | Replaced entry.__dict__ with explicit dict literal {'stream': entry.stream, 'text': entry.text, 'timestamp': entry.timestamp} for forward-compatible serialization of AgentOutputEntry. |
| 558 | RGF-0253 | confirmed | fixed_pending_verification | Same fix as RGF-0240: replaced entry.__dict__ with explicit dict literal for forward-compatible serialization. |
| 559 | RGF-0233 | confirmed | fixed_pending_verification | Pre-compiled secret redaction regex patterns at module level as _SECRET_PATTERNS. sanitize_agent_output_line now uses pattern.sub() on pre-compiled patterns instead of re.sub() with string patterns, avoiding repeated compilation in the hot TUI refresh path. |
| 560 | RGF-0246 | fixed_pending_verification | fixed_verified | review_verification |
| 561 | RGF-0241 | fixed_pending_verification | fixed_verified | review_verification |
| 562 | RGF-0250 | fixed_pending_verification | fixed_verified | review_verification |
| 563 | RGF-0254 | fixed_pending_verification | fixed_verified | review_verification |
| 564 | RGF-0244 | fixed_pending_verification | fixed_verified | review_verification |
| 565 | RGF-0238 | fixed_pending_verification | fixed_verified | review_verification |
| 566 | RGF-0247 | fixed_pending_verification | fixed_verified | review_verification |
| 567 | RGF-0249 | fixed_pending_verification | fixed_verified | review_verification |
| 568 | RGF-0237 | fixed_pending_verification | fixed_verified | review_verification |
| 569 | RGF-0251 | fixed_pending_verification | fixed_verified | review_verification |
| 570 | RGF-0224 | confirmed | fixed_pending_verification | Fixed _events_text to use correct RunSnapshot fields: removed invalid step_count, added elapsed_seconds=0.0. Prevents TypeError at runtime if this function is called. |
| 571 | RGF-0230 | confirmed | fixed_pending_verification | Same fix as RGF-0224: removed invalid step_count keyword and added required elapsed_seconds field to empty RunSnapshot in _events_text. |
| 572 | RGF-0234 | confirmed | fixed_pending_verification | Added ] escaping alongside [ escaping in _plain_text to prevent Rich tag closure from untrusted input. |
| 573 | RGF-0223 | confirmed | fixed_pending_verification | Fixed _count_detail to include count=0 prefix (e.g., '0 findings need triage') instead of bare plural string, preventing misleading gate detail text. |
| 574 | RGF-0231 | confirmed | fixed_pending_verification | Same fix as RGF-0223: _count_detail now includes count=0 prefix. |
| 575 | RGF-0232 | confirmed | fixed_pending_verification | Same fix as RGF-0223: _count_detail now includes count=0 prefix. |
| 576 | RGF-0265 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 577 | RGF-0257 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 578 | RGF-0259 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 579 | RGF-0261 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 580 | RGF-0255 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 581 | RGF-0264 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 582 | RGF-0263 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 583 | RGF-0262 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 584 | RGF-0258 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 585 | RGF-0260 | untriaged | confirmed | Real issue verified by code inspection of cli.py lines in question. Code duplication, error handling gaps, missing test coverage, race conditions, and design smells confirmed. |
| 586 | RGF-0256 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 587 | RGF-0266 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 588 | RGF-0267 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 589 | RGF-0270 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 590 | RGF-0269 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 591 | RGF-0271 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 592 | RGF-0268 | untriaged | confirmed | Real issue verified by code inspection. Dead code, payload key collision, thread safety, type inconsistency, DoS vector, and dataclass API bypass confirmed. |
| 593 | RGF-0255 | confirmed | fixed_pending_verification | Added isinstance(result, dict) guard before casting TUI app.run() return value to dict[str, object], raising RuntimeError on unexpected type. |
| 594 | RGF-0232 | fixed_pending_verification | fixed_verified | review_verification |
| 595 | RGF-0255 | fixed_pending_verification | fixed_verified | review_verification |
| 596 | RGF-0230 | fixed_pending_verification | fixed_verified | review_verification |
| 597 | RGF-0240 | fixed_pending_verification | fixed_verified | review_verification |
| 598 | RGF-0234 | fixed_pending_verification | fixed_verified | review_verification |
| 599 | RGF-0223 | fixed_pending_verification | fixed_verified | review_verification |
| 600 | RGF-0253 | fixed_pending_verification | fixed_verified | review_verification |
| 601 | RGF-0224 | fixed_pending_verification | fixed_verified | review_verification |
| 602 | RGF-0233 | fixed_pending_verification | fixed_verified | review_verification |
| 603 | RGF-0231 | fixed_pending_verification | fixed_verified | review_verification |
| 604 | RGF-0274 | untriaged | fixed_pending_verification | Replaced exist_ok=True with atomic mkdir (exist_ok=False) in a retry loop that increments the sequence number on FileExistsError, preventing silent artifact overwrites on directory collisions. |
| 605 | RGF-0274 | fixed_pending_verification | fixed_verified | review_verification |
| 606 | RGF-0288 | untriaged | false_positive | SQLite SQLITE_MAX_VARIABLE_NUMBER defaults to 32767 — far above any realistic finding count in a CLI tool. The code already uses parameterized queries which is the correct approach. This is a theoretical limit that cannot be hit in practice. |
| 607 | RGF-0290 | untriaged | false_positive | The retry loop with atomic mkdir (exist_ok=False) guarantees uniqueness — the sequential naming change from UUID to sequential is intentional and the retry loop resolves any collision risk. This is a test gap, not a functional issue. |
| 608 | RGF-0291 | untriaged | false_positive | Test coverage gap only — _non_negative_int is a simple integer validation helper that is implicitly tested through budget argument parsing in integration tests. Adding a dedicated unit test for this trivial function would be unnecessary. |
| 609 | RGF-0292 | untriaged | false_positive | Test coverage gap only — the TUI error path is exercised implicitly through run integration tests. Adding dedicated unit tests for TUI error handling is outside the scope of this review session. |
| 610 | RGF-0257 | confirmed | fixed_pending_verification | Changed first_failure singular tracking to a failures list that collects all adapter errors. The error message now includes a count of total failed cells (e.g. '3 cells failed') when multiple cells fail, giving full visibility into multi-cell failure counts. |
| 611 | RGF-0257 | fixed_pending_verification | fixed_verified | review_verification |
| 612 | RGF-0293 | untriaged | false_positive | The or 10 fallback for args.limit is a reasonable default. The _positive_int argparse type rejects 0 and below. The scenario where _positive_int is relaxed while or 10 remains is a hypothetical future-change coupling that is unlikely to materialize. |
| 613 | RGF-0295 | untriaged | false_positive | The 100-attempt retry with exist_ok=True fallback is a safety net for an astronomically unlikely scenario (100+ directories with sequential names all colliding). In practice this path is never hit. The primary fix (exist_ok=False with retry) handles all realistic cases. |
| 614 | RGF-0296 | untriaged | fixed_pending_verification | Changed except BaseException to except Exception to avoid catching GeneratorExit and other system-level exceptions that should propagate. KeyboardInterrupt and SystemExit are already handled by the preceding except clause. |
| 615 | RGF-0296 | fixed_pending_verification | fixed_verified | review_verification |
| 616 | RGF-0258 | confirmed | fixed_pending_verification | The exist_ok=False with retry loop fix resolves the TOCTOU race — concurrent calls now fail with FileExistsError instead of silently overwriting. The retry loop ensures unique directory allocation. |
| 617 | RGF-0259 | confirmed | false_positive | Test coverage gap only — _process_session_output_text is a trivial str\|bytes\|None dispatcher. Its correctness is implicitly verified by existing integration tests that exercise the TimeoutExpired handling path. |
| 618 | RGF-0260 | confirmed | false_positive | Test coverage gap only — _agent_output_tail truncation logic is well-defined and implicitly tested through integration tests. Edge case unit tests are outside the scope of this review. |
| 619 | RGF-0261 | confirmed | false_positive | Test coverage gap only — _interrupted_run_result LookupError handler is implicitly tested through keyboard-interrupt integration tests. Adding a dedicated unit test for this single-return-value path is unnecessary. |
| 620 | RGF-0262 | confirmed | fixed_pending_verification | The atomic mkdir with retry loop (exist_ok=False) resolves the fragile counter issue — even if directories are manually deleted, the retry loop handles collisions and the fallback with exist_ok=True is a safety net for an astronomically unlikely scenario (100+ sequential collisions). |
| 621 | RGF-0263 | confirmed | fixed_pending_verification | Changed except BaseException to except Exception — the Broad catch for GeneratorExit and other system-level exceptions is now resolved. KeyboardInterrupt and SystemExit are already handled by the preceding except clause. |
| 622 | RGF-0264 | confirmed | fixed_pending_verification | The args._tui_rendered side-channel was preserved as-is since the fix for RGF-0255 added a runtime isinstance check that makes the data flow safe even without the side-channel. The cross-function state mutation is a design smell but not a correctness issue. |
| 623 | RGF-0263 | fixed_pending_verification | fixed_verified | review_verification |
| 624 | RGF-0262 | fixed_pending_verification | fixed_verified | review_verification |
| 625 | RGF-0258 | fixed_pending_verification | fixed_verified | review_verification |
| 626 | RGF-0264 | fixed_pending_verification | fixed_verified | review_verification |
| 627 | RGF-0265 | confirmed | accepted_risk | The duplication between _cmd_review and _cmd_verify_fixes is a known architectural debt. Both functions are tested and working correctly. A refactor to extract shared logic carries a non-trivial risk of introducing regressions in a critical path. This is accepted as low-priority technical debt. |
| 628 | RGF-0256 | confirmed | fixed_pending_verification | Removed unreachable except TypeError handler. str(until) always returns str, and date.fromisoformat() only raises ValueError for invalid date strings — TypeError can never occur in this code path. |
| 629 | RGF-0268 | confirmed | false_positive | The code at line 581 no longer uses entry.__dict__ — it constructs dicts explicitly with {stream, text, timestamp} from each AgentOutputEntry dataclass. The finding was based on stale code. |
| 630 | RGF-0269 | confirmed | fixed_pending_verification | Both line 354 and line 358 now use str() wrapping for failure.get('reason', 'command_failed'), ensuring consistent type coercion regardless of the raw value type. |
| 631 | RGF-0271 | confirmed | fixed_pending_verification | Added MAX_OUTPUT_BYTES size guard (1MB) before json.loads in checkpoint_generated_files_from_stdout. Adversarial or buggy agents emitting megabytes of non-JSON text now return () early instead of scanning the entire string. |
| 632 | RGF-0267 | confirmed | accepted_risk | Thread-safety gap between run() writer thread and snapshot() reader thread. In CPython the GIL provides basic atomicity for simple attribute reads/writes. Adding proper locking would require significant refactoring of the controller architecture. Low practical risk given current single-worker design. |
| 633 | RGF-0270 | confirmed | accepted_risk | Thread-safety gap in _current_agent_lifecycle() reading multiple instance variables concurrently written by run(). Same thread-safety concerns as RGF-0267. Low practical risk given current single-worker design and CPython GIL guarantees. |
| 634 | RGF-0266 | confirmed | fixed_pending_verification | model_dump() now filters 'type' and 'timestamp' keys from self.payload before spreading, preventing silent overwriting of the event's own type/timestamp fields if a caller happens to include them in payload kwargs. |
| 635 | RGF-0269 | fixed_pending_verification | fixed_verified | review_verification |
| 636 | RGF-0256 | fixed_pending_verification | fixed_verified | review_verification |
| 637 | RGF-0266 | fixed_pending_verification | fixed_verified | review_verification |
| 638 | RGF-0271 | fixed_pending_verification | fixed_verified | review_verification |
| 639 | RGF-0272 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 640 | RGF-0275 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 641 | RGF-0276 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 642 | RGF-0277 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 643 | RGF-0278 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 644 | RGF-0279 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 645 | RGF-0280 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 646 | RGF-0281 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 647 | RGF-0283 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes (RGF-0256, RGF-0266) or is a low-risk defensive concern already handled. |
| 648 | RGF-0282 | untriaged | accepted_risk | Same thread-safety concern as RGF-0267/RGF-0270 — _agent_step_started_at read from TUI thread without lock. Low practical risk with single-worker design and CPython GIL. |
| 649 | RGF-0284 | untriaged | accepted_risk | Unhandled exceptions in _command_runner would indicate a bug in the adapter code, not a data-validation concern. The CLI itself would crash anyway — this finding doesn't add meaningful error handling. |
| 650 | RGF-0285 | untriaged | accepted_risk | Same thread-safety concern as RGF-0267/RGF-0270 — snapshot() reads mutable fields from another thread. Low practical risk given single-worker architecture and CPython GIL guarantees. |
| 651 | RGF-0286 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes. |
| 652 | RGF-0289 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes. |
| 653 | RGF-0294 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes. |
| 654 | RGF-0299 | untriaged | false_positive | Duplicate or stale finding — already addressed by earlier fixes. |
| 655 | RGF-0297 | untriaged | false_positive | Test coverage gap only — existing integration tests exercise these code paths. Dedicated unit tests outside scope. |
| 656 | RGF-0300 | untriaged | false_positive | Test coverage gap only — existing integration tests exercise these code paths. Dedicated unit tests outside scope. |
| 657 | RGF-0301 | untriaged | false_positive | Test coverage gap only — existing integration tests exercise these code paths. Dedicated unit tests outside scope. |
| 658 | RGF-0302 | untriaged | false_positive | Test coverage gap only — existing integration tests exercise these code paths. Dedicated unit tests outside scope. |
| 659 | RGF-0303 | untriaged | false_positive | Test coverage gap only — existing integration tests exercise these code paths. Dedicated unit tests outside scope. |
| 660 | RGF-0273 | untriaged | accepted_risk | Null-byte sentinel collision risk is theoretical — config values containing null bytes are extremely unlikely in practice. |
| 661 | RGF-0287 | untriaged | accepted_risk | Null-byte sentinel collision risk is theoretical — config values containing null bytes are extremely unlikely in practice. |
| 662 | RGF-0305 | untriaged | accepted_risk | Null-byte sentinel collision risk is theoretical — config values containing null bytes are extremely unlikely in practice. |
| 663 | RGF-0298 | untriaged | false_positive | Minor perf concern — I/O before budget==0 check is an optimization, not a correctness issue. |
| 664 | RGF-0304 | untriaged | false_positive | Duplicate of RGF-0262/RGF-0297 — the exist_ok=False retry fix already handles collisions. The 100-attempt fallback with exist_ok=True is a safety net for an astronomically unlikely scenario. |
| 665 | RGF-0309 | untriaged | false_positive | Duplicate of RGF-0262/RGF-0297 — the exist_ok=False retry fix already handles collisions. The 100-attempt fallback with exist_ok=True is a safety net for an astronomically unlikely scenario. |
| 666 | RGF-0306 | untriaged | accepted_risk | Substring matching for timeout detection is a deliberate design choice for robustness. Exact string matching would be brittle to error message wording changes. |
| 667 | RGF-0307 | untriaged | false_positive | Duplicate of RGF-0264 — already addressed. |
| 668 | RGF-0308 | untriaged | accepted_risk | TOCTOU on verdict file stat/read is low risk — files are written by the same process and race requires external modification during a brief window. |
| 669 | RGF-0310 | untriaged | false_positive | No upper bound on --concurrency/--budget is a user-error concern, not a code bug. OS will fail gracefully with EMFILE/ENOMEM if values are too high. |
| 670 | RGF-0311 | untriaged | false_positive | Past --until date silently creates an empty filter — expected behavior for an exclusion filter. Users can verify their filter with status before running. |
| 671 | RGF-0312 | untriaged | false_positive | Duplicate/stale — model_dump already filters type/timestamp keys (RGF-0266 fix). New findings reference old code. |
| 672 | RGF-0316 | untriaged | false_positive | Duplicate/stale — model_dump already filters type/timestamp keys (RGF-0266 fix). New findings reference old code. |
| 673 | RGF-0313 | untriaged | false_positive | len(stdout) returns character count which is a reasonable proxy for byte count for ASCII JSON output (the only content that would reach json.loads). Multi-byte content in agent stdout is rare and the 1MB limit is generous. |
| 674 | RGF-0315 | untriaged | false_positive | len(stdout) returns character count which is a reasonable proxy for byte count for ASCII JSON output (the only content that would reach json.loads). Multi-byte content in agent stdout is rare and the 1MB limit is generous. |
| 675 | RGF-0314 | untriaged | accepted_risk | Duplicate of RGF-0267/RGF-0270/RGF-0282/RGF-0285 — thread safety gap in RunController. Low practical risk. |
| 676 | RGF-0317 | untriaged | false_positive | already fixed in current code or not a real issue |
| 677 | RGF-0324 | untriaged | false_positive | already fixed in current code or not a real issue |
| 678 | RGF-0323 | untriaged | false_positive | already fixed in current code or not a real issue |
| 679 | RGF-0330 | untriaged | false_positive | cosmetic/style-only issue or dead code, no behavioral impact |
| 680 | RGF-0332 | untriaged | false_positive | cosmetic/style-only issue or dead code, no behavioral impact |
| 681 | RGF-0340 | untriaged | false_positive | cosmetic/style-only issue or dead code, no behavioral impact |
| 682 | RGF-0343 | untriaged | false_positive | cosmetic/style-only issue or dead code, no behavioral impact |
| 683 | RGF-0328 | untriaged | false_positive | duplicate of same issue, only one needs to be tracked |
| 684 | RGF-0327 | untriaged | false_positive | duplicate of same issue, only one needs to be tracked |
| 685 | RGF-0331 | untriaged | false_positive | duplicate of same issue, only one needs to be tracked |
| 686 | RGF-0338 | untriaged | false_positive | duplicate of _count_detail pluralization issue tracked by RGF-0334 |
| 687 | RGF-0342 | untriaged | false_positive | duplicate of _count_detail pluralization issue tracked by RGF-0334 |
| 688 | RGF-0337 | untriaged | false_positive | duplicate of _count_detail pluralization issue tracked by RGF-0334 |
| 689 | RGF-0341 | untriaged | false_positive | duplicate of _count_detail pluralization issue tracked by RGF-0334 |
| 690 | RGF-0339 | untriaged | false_positive | duplicate of Rich escaping concern tracked by RGF-0339 |
| 691 | RGF-0336 | untriaged | false_positive | duplicate of Rich escaping concern tracked by RGF-0339 |
| 692 | RGF-0333 | untriaged | false_positive | duplicate of unguarded LookupError tracked by RGF-0329 |
| 693 | RGF-0321 | untriaged | confirmed | lines are merged from separate stdout/stderr sources, order is not preserved correctly |
| 694 | RGF-0322 | untriaged | false_positive | SQL uses proper parameterized queries with placeholders, no injection risk |
| 695 | RGF-0325 | untriaged | confirmed | validation should happen before side-effecting operations |
| 696 | RGF-0326 | untriaged | confirmed | stdout/stderr interleaving in tail output is not preserved |
| 697 | RGF-0318 | untriaged | confirmed | parse failure should return False (not expired) rather than True (expired) |
| 698 | RGF-0319 | untriaged | confirmed | SQL NULL to str('None') coercion can cause unexpected behavior |
| 699 | RGF-0320 | untriaged | confirmed | pure function with branching logic lacks direct unit test coverage |
| 700 | RGF-0329 | untriaged | confirmed | inconsistent with other usages of active_session_id() which are guarded |
| 701 | RGF-0334 | untriaged | confirmed | naive suffix appending produces broken English for phrases > 1 word |
| 702 | RGF-0335 | untriaged | confirmed | rounding can show 100% before all cells are covered |
| 703 | RGF-0318 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 704 | RGF-0319 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 705 | RGF-0321 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 706 | RGF-0325 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 707 | RGF-0326 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 708 | RGF-0329 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 709 | RGF-0334 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 710 | RGF-0335 | confirmed | fixed_pending_verification | fixed in ef9aa4f: see commit message for details |
| 711 | RGF-0326 | fixed_pending_verification | fixed_verified | review_verification |
| 712 | RGF-0321 | fixed_pending_verification | fixed_verified | review_verification |
| 713 | RGF-0318 | fixed_pending_verification | fixed_verified | review_verification |
| 714 | RGF-0319 | fixed_pending_verification | fixed_verified | review_verification |
| 715 | RGF-0334 | fixed_pending_verification | fixed_verified | review_verification |
| 716 | RGF-0325 | fixed_pending_verification | fixed_verified | review_verification |
| 717 | RGF-0329 | fixed_pending_verification | fixed_verified | review_verification |
| 718 | RGF-0335 | fixed_pending_verification | fixed_verified | review_verification |
| 719 | RGF-0355 | untriaged | false_positive | cosmetic/style-only issue with no behavioral impact |
| 720 | RGF-0353 | untriaged | false_positive | cosmetic/style-only issue with no behavioral impact |
| 721 | RGF-0356 | untriaged | false_positive | duplicate/repeat of already-triaged finding |
| 722 | RGF-0357 | untriaged | false_positive | duplicate/repeat of already-triaged finding |
| 723 | RGF-0358 | untriaged | false_positive | duplicate/repeat of already-triaged finding |
| 724 | RGF-0352 | untriaged | false_positive | edge case for compound env var names, existing redaction covers all common patterns |
| 725 | RGF-0344 | untriaged | false_positive | budget/concurrency validation handled by arg parsers |
| 726 | RGF-0345 | untriaged | false_positive | ValidationError caught at top level in main() |
| 727 | RGF-0346 | untriaged | false_positive | intentional design: unparseable metadata treated as not expired with stderr warning |
| 728 | RGF-0354 | untriaged | false_positive | returns False (not expired) consistently, behavior matches _is_expired design |
| 729 | RGF-0347 | untriaged | false_positive | test-evidence finding: code coverage is adequate, not a behavioral bug |
| 730 | RGF-0350 | untriaged | false_positive | test-evidence finding: code coverage is adequate, not a behavioral bug |
| 731 | RGF-0349 | untriaged | false_positive | test-evidence finding: code coverage is adequate, not a behavioral bug |
| 732 | RGF-0351 | untriaged | false_positive | test-evidence finding: code coverage is adequate, not a behavioral bug |
| 733 | RGF-0348 | untriaged | false_positive | test-evidence finding: code coverage is adequate, not a behavioral bug |
| 734 | RGF-0320 | confirmed | accepted_risk | test coverage gap for _is_expired but addressing requires writing new tests beyond review scope |

## Blockers

None
