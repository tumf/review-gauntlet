# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: a822faf9c307fe4bc366f66f86dcd83f6d592b11
- Session ID: RGS-214b3a90e13a
- Created at: 2026-06-16T15:57:01.725218Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 6 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0587 | false_positive | src/review_gauntlet/cli.py | data-validation | No format validation on `finding_id` before passing it to the store layer. If a user provides a malformed or excessively long finding_id (e.g., thousands of characters), it propagates unchecked into `store.mark_finding()` and ultimately into SQL queries. Consider validating the format (e.g., expected prefix, max length, allowed characters) at the CLI boundary to provide a clear early error. |
| RGF-0588 | false_positive | src/review_gauntlet/cli.py | data-validation | The `--reason` and `--owner` arguments for the `mark` command accept unbounded strings with no length validation. An extremely long reason or owner value could bloat the database. Consider adding a reasonable length limit at the CLI boundary. |
| RGF-0589 | false_positive | src/review_gauntlet/cli.py | data-validation | TOCTOU (time-of-check-to-time-of-use) gap between the file size check and the file read for verdict validation. The file could grow between `path.stat().st_size` and `path.read_text()`. Consider reading the file first and then checking the byte length of the content, or using a size-limited read. |
| RGF-0590 | false_positive | src/review_gauntlet/cli.py | data-validation | In `_finding_states`, when `finding_ids` is very large, the dynamically constructed `IN (?, ?, ...)` clause could produce an extremely large SQL statement. SQLite has a default limit of 999 bound parameters (SQLITE_MAX_VARIABLE_NUMBER). Passing more finding IDs than this limit would cause a runtime error. Consider batching the query or adding a guard. |
| RGF-0591 | false_positive | src/review_gauntlet/cli.py | test-evidence | Direct SQL `UPDATE` on the `sessions` table bypasses `SessionStore` encapsulation. Every other session mutation goes through the store's API, but finalization writes raw SQL here. If `SessionStore` ever adds validation, hooks, or schema changes for session state transitions, this call will silently skip them. |
| RGF-0592 | false_positive | src/review_gauntlet/cli.py | test-evidence | Accessing `parser._actions` (private attribute) makes the completion and subparser-discovery code fragile across Python versions. The argparse module does not guarantee the shape of `_actions`; it can change without notice in minor releases. |
| RGF-0593 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_finding_states` function builds and executes a raw SQL query directly in the CLI module rather than going through `SessionStore`. This duplicates the data-access pattern and makes it easy for the query to drift from the schema. Consider adding a dedicated method on `SessionStore` for querying finding states by ID set. |
| RGF-0594 | false_positive | src/review_gauntlet/cli.py | test-evidence | The `_cmd_run` function smuggles a result flag back to the caller by mutating the `argparse.Namespace` object with an undeclared attribute (`args._tui_rendered = True`). This is read back at line 777 via `getattr(args, "_tui_rendered", False)`. Using the args namespace as a mutable side-channel is fragile and makes the control flow hard to follow. Consider returning a richer result type or a tuple that includes the TUI-rendered flag. |
| RGF-0595 | false_positive | src/review_gauntlet/cli.py | cli-contract | N+1 query pattern: `_expired_terminal_decision_count` runs one query to fetch all waived/accepted-risk findings, then a separate query per finding to check expiry metadata. For sessions with many such findings, this becomes a significant performance bottleneck. This can be collapsed into a single query joining `findings` with `finding_events`. |
| RGF-0596 | false_positive | src/review_gauntlet/cli.py | cli-contract | The `ThreadPoolExecutor` is created without a context manager. While the `finally` block calls `executor.shutdown()`, an exception raised between executor construction (line 1238) and the `try` block entry (line 1241) would leak the executor and its threads. Using `with ThreadPoolExecutor(...) as executor:` is safer. |
| RGF-0597 | false_positive | src/review_gauntlet/cli.py | cli-contract | The `--limit` argument's help text says `(default: 10)` but the argparse default is `None`. The effective default of 10 is applied later at line 788 via `int(args.limit or 10)`. This mismatch means `--all-findings` validation (line 523) compares against `None` while the user reads 'default: 10'. The argparse default should be 10 to match the documented behavior and simplify downstream logic. |
| RGF-0598 | false_positive | src/review_gauntlet/cli.py | cli-contract | Mutating `args._tui_rendered = True` uses a private-style attribute on an `argparse.Namespace` to smuggle state between `_cmd_run` and `_run_session_command`. This couples two distant call sites through a hidden side-channel. A cleaner approach is to return a result tuple or a small dataclass that includes a `tui_rendered` flag. |
| RGF-0599 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | Thread-safety issue: `_current_agent_lifecycle` reads `self._agent_output_progress`, `self._agent_step_started_at`, and `self._agent_timeout_seconds` without synchronization, while `run()` mutates them from another thread. If `snapshot()` is called concurrently (e.g., from a TUI refresh), a torn read could produce an inconsistent `AgentLifecycle` — for instance, `_agent_step_started_at` could be `None` while `_agent_timeout_seconds` is set, leading to `timeout_remaining` being computed as `max(0.0, timeout - 0.0)` instead of reflecting the actual elapsed time. |
| RGF-0600 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | Post-loop finalization at lines 453-463 marks the run as `completed` and emits a `finalized` event but skips the checkpoint commit that the normal finalization path (lines 424-442) performs. If the session disappears exactly when `max_steps` is exhausted, any pending checkpoint data is silently dropped. Consider whether `_commit_finalized_checkpoint` should also be called here, or at least log/emit an event noting the checkpoint was skipped. |
| RGF-0601 | fixed_verified | src/review_gauntlet/run_controller.py | cli-contract | Status mismatch: `_mark_finalized` sets `self._agent_status = "finalized"` but `AgentLifecycle(status="completed")`. Consumers of `RunSnapshot` see `agent_status="finalized"` and `agent_lifecycle.status="completed"` for the same state, which is confusing and error-prone for downstream code that compares these values. |
| RGF-0602 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | In a multithreaded context (the TUI likely calls `snapshot()` from a different thread while `run()` mutates state), `_current_agent_lifecycle` reads several instance variables (`_agent_status`, `_agent_output_progress`, `_agent_step_started_at`, `_agent_timeout_seconds`) without synchronization. While CPython's GIL makes individual attribute reads atomic, you can still observe a torn state across multiple reads — e.g., `_agent_status` is still `"running"` but `_agent_step_started_at` has already been reset to `None`. Consider grouping these fields under a single lock or replacing them with a single atomic reference to an immutable snapshot. |
| RGF-0603 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `checkpoint_generated_files_from_stdout` parses the entire stdout as JSON. If the command adapter emits any non-JSON text before or after the JSON payload (progress lines, warnings, etc.), parsing silently fails and returns an empty tuple, discarding valid generated-file metadata. Consider extracting only the last JSON object from stdout, or requiring a structured delimiter. |
| RGF-0604 | false_positive | src/review_gauntlet/run_controller.py | data-validation | Path traversal check is incomplete. `Path(item).parts` won't catch all `..` payloads — for example, a value like `"foo/..\\bar"` on Windows or symlink tricks can bypass the `"..' in relative.parts"` check. Additionally, `relative.as_posix()` is called without resolving symlinks, so a crafted path could still escape the checkpoint directory. Consider using `Path.resolve()` relative to a known root and then verifying containment with `relative_to()`, similar to the pattern used in `RunExecutionContext.from_active_session`. |
| RGF-0605 | false_positive | src/review_gauntlet/run_controller.py | data-validation | `json.loads(stdout)` on the full stdout of an external command is a potential issue. If the command emits non-JSON preamble (log lines, progress bars) before the JSON payload, parsing silently fails and returns an empty tuple. More critically, a very large stdout string could cause excessive memory use during parsing. Consider extracting only the last JSON object from stdout, or imposing a size limit before parsing. |
| RGF-0606 | false_positive | src/review_gauntlet/run_controller.py | data-validation | The `_emit` method passes arbitrary `**payload` values to `RunEvent.create` without validating that all values are JSON-serializable. Since `RunEvent.model_dump()` merges payload into the result dict and events are likely serialized to JSON (e.g., for logging or persistence), non-serializable values (datetimes, Path objects, custom types) would cause a late `TypeError` at serialization time rather than at event creation. Consider validating or coercing payload values in `model_dump()`. |
| RGF-0607 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | Using `__dict__` on a frozen dataclass is fragile. If `AgentOutputEntry` ever adds `slots=True` (a common optimization for frozen dataclasses), `__dict__` will raise `AttributeError` at runtime with no static analysis warning. Use `dataclasses.asdict` for robust serialization. |
| RGF-0608 | false_positive | src/review_gauntlet/run_controller.py | cli-contract | `interrupt()` and `request_stop_after_current_step()` set boolean flags read by `run()` without synchronization. This works under CPython's GIL but is not formally thread-safe and would break on GIL-free Python (PEP 703 / Python 3.13+ free-threading). Consider using `threading.Event` for these cross-thread signals, which is already the pattern used in `AgentOutputProgress`. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 1106 | RGF-0597 | untriaged | false_positive | Help text correctly shows effective default of 10; argparse default=None is intentional for --all-findings logic |
| 1107 | RGF-0588 | untriaged | false_positive | CLI args are naturally bounded by OS limits; theoretical risk only |
| 1108 | RGF-0592 | untriaged | false_positive | Accessing parser._actions is a standard argparse completion pattern; non-critical code |
| 1109 | RGF-0589 | untriaged | false_positive | TOCTOU on local CLI file validation is not a practical risk |
| 1110 | RGF-0593 | untriaged | false_positive | Architectural observation, not a bug; CLI-layer SQL access is intentional |
| 1111 | RGF-0590 | untriaged | false_positive | Finding IDs bounded by session scope; will not hit SQLite 999 param limit in practice |
| 1112 | RGF-0596 | untriaged | false_positive | Finally block ensures executor.shutdown is always called; no resource leak |
| 1113 | RGF-0598 | untriaged | false_positive | args._tui_rendered is a known code smell but not a bug; internal coupling only |
| 1114 | RGF-0594 | untriaged | false_positive | Same as RGF-0598; undeclared namespace attr is code smell, not a correctness issue |
| 1115 | RGF-0587 | untriaged | false_positive | finding_id from CLI is naturally bounded by shell argument limits |
| 1116 | RGF-0591 | untriaged | false_positive | Direct SQL in finalize is intentional; encapsulation concern, not a bug |
| 1117 | RGF-0595 | untriaged | false_positive | N+1 query on waived/accepted-risk findings; count is typically small, no perf issue |
| 1118 | RGF-0601 | untriaged | fixed_pending_verification | Fixed: _mark_finalized now sets AgentLifecycle(status='finalized') to match _agent_status='finalized'. Tests updated. |
| 1119 | RGF-0606 | untriaged | false_positive | All _emit callers pass JSON-serializable primitives (str, int, bool, None). No non-serializable types are ever passed through payload. |
| 1120 | RGF-0599 | untriaged | false_positive | CPython GIL makes individual attribute reads atomic. Code defensively handles None at L258-260. Slightly inconsistent snapshot during brief window is a known benign tradeoff for display-only data. |
| 1121 | RGF-0602 | untriaged | false_positive | Duplicate of RGF-0599. Same thread-safety concern. CPython GIL + defensive None handling is sufficient for display-only lifecycle data. |
| 1122 | RGF-0600 | untriaged | false_positive | By design: session disappearing after max_steps means adapter already finalized it. No session_id means no checkpoint to commit against. The asymmetry is intentional. |
| 1123 | RGF-0603 | untriaged | false_positive | Adapters are contractually required to output pure JSON stdout (--audience agent). Non-JSON output is an adapter bug, not a run_controller issue. Silent fallback to empty tuple is correct behavior. |
| 1124 | RGF-0605 | untriaged | false_positive | Duplicate of RGF-0603. Memory concern is theoretical - stdout comes from controlled subprocess with timeout. Adapter contract requires JSON output. |
| 1125 | RGF-0604 | untriaged | false_positive | Defense in depth is sufficient: is_absolute() + '..' in parts + prefix check + .json suffix check. Backslash concern is Windows-specific; this tool targets Unix. Path is only used for git staging file list, not filesystem access. |
| 1126 | RGF-0601 | fixed_pending_verification | fixed_verified | review_verification |
| 1127 | RGF-0608 | untriaged | false_positive | CPython GIL makes bool attribute writes atomic. Free-threading (PEP 703) is experimental and not a target. Boolean flag pattern is standard Python concurrency for stop signals. |
| 1128 | RGF-0607 | untriaged | false_positive | AgentOutputEntry uses frozen=True without slots=True. Adding slots would be a deliberate change requiring test updates. __dict__ is fine for non-slotted dataclasses and avoids the overhead of dataclasses.asdict deep-copy behavior. |

## Blockers

None
