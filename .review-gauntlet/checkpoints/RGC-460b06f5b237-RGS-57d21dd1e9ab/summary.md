# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: 460b06f5b237001a43577d9e0d72faa7bd747e6e
- Session ID: RGS-57d21dd1e9ab
- Created at: 2026-06-16T10:44:18.722953Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 9 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0488 | fixed_verified | src/review_gauntlet/git_worktree.py | cli-contract | The `run_worktree_setup` function executes an arbitrary file found at `.wt/setup` inside the worktree without any validation of its origin, permissions, or content. A malicious repository could include a `.wt/setup` script that executes arbitrary commands when a user runs a review. Consider at minimum checking that the script has appropriate ownership/permissions, or requiring an explicit opt-in flag before executing repository-supplied scripts. |
| RGF-0489 | fixed_verified | src/review_gauntlet/git_worktree.py | cli-contract | `_commit_session_changes` passes user-controlled `commit_paths` directly to `git add --`. If a caller passes paths containing `..` segments, files outside the worktree could be staged. The `merge_session_worktree` function validates `worktree_path` against path traversal but does not validate individual `commit_paths`. Consider normalising each path and verifying it stays within the worktree root. |
| RGF-0490 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | `merge_preflight_blockers` calls `subprocess.run` directly instead of using the `git()` helper, which means it bypasses the centralized error handling and timeout logic. Use the `git()` helper with `check=False` for consistency. |
| RGF-0491 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | `_unique_session_branch` has an unbounded loop: if refs are adversarially pre-created (or there's a bug returning stale results), this loops forever. Add an upper bound to the suffix counter. |
| RGF-0492 | fixed_verified | src/review_gauntlet/git_worktree.py | data-validation | The `merge_preflight_blockers` function resolves `worktree_path` from untrusted metadata without the same path-traversal check that `merge_session_worktree` and `cleanup_session_worktree` perform. If a malicious metadata dict supplies a `worktree_path` like `../../etc`, the `session_worktree.is_dir()` check on line 250 would probe an arbitrary directory outside the repo root. Add an `is_relative_to` guard here, consistent with the other two call sites. |
| RGF-0493 | fixed_verified | src/review_gauntlet/git_worktree.py | data-validation | `_commit_session_changes` passes `commit_paths` directly to `git add --` without validating that none of the paths escape the worktree. A caller supplying `../../some-file` in `commit_paths` could stage files outside the session worktree. Consider validating that each resolved commit path is relative to `session_worktree`. |
| RGF-0494 | fixed_verified | src/review_gauntlet/git_worktree.py | data-validation | `run_worktree_setup` executes an arbitrary file at `.wt/setup` inside the worktree without verifying it is executable or checking its permissions. More importantly, `returncode` is left as `None` in the `OSError` branch (line 154-158), making it inconsistent with the timeout branch which sets `returncode=-1`. Callers checking `returncode is not None` to decide if the script ran will get inconsistent signals. |
| RGF-0495 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | `cleanup_session_worktree` has a path-traversal guard (line 296) that rejects `worktree_path` values escaping the repo root, but there is no dedicated test exercising this branch. A direct unit test with a `../escape` path would confirm the guard works and prevent regressions. |
| RGF-0496 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | `_git_worktree_metadata` (lines 368-380) validates structure and required keys but has no direct unit tests. Edge cases — missing keys, `enabled=False`, non-dict `git_worktree` value, empty-string fields — are only tested indirectly through `merge_session_worktree` integration tests. Direct unit tests would make validation logic easier to verify and refactor. |
| RGF-0497 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Path traversal vulnerability: `_copy_checkpoint_artifacts_to_session_worktree` constructs destination paths from `generated_files` (checkpoint data) without validating that the resulting paths stay inside `root` or `session_worktree`. A corrupted or malicious checkpoint could contain relative paths like `../../etc/crontab`, causing files to be written outside the intended directory. Compare with `_remove_base_checkpoint_artifacts_before_merge` (line 2311) which correctly validates `checkpoint_dir` with `is_relative_to`. |
| RGF-0498 | false_positive | src/review_gauntlet/cli.py | data-validation | Fragile None-coalescing: `str(row["latest_cell_id"]) or None` relies on Python truthiness to convert an empty string to None. The SQL query uses `coalesce(o.cell_id, '')` (line 1564), so NULL becomes `""` which maps to None via truthiness. However, this pattern silently breaks if a cell_id is ever a legitimately falsy string (e.g., `"0"`). Use an explicit check instead. |
| RGF-0499 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Unvalidated path data used in git commit operation: `checkpoint["generated_files"]` is cast to `list[str]` and passed directly to `merge_session_worktree` as `commit_paths` without validating that entries are safe repository-relative paths. If checkpoint data is corrupted, this could include arbitrary paths in the git commit scope. |
| RGF-0500 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_resolve_session_cwd` function contains non-trivial security logic (path traversal prevention via `relative_to` check) but has no unit tests. A malicious or misconfigured `adapter.cwd` could escape the repository root. The path-traversal guard on line 1983-1985 should be validated with dedicated tests covering: absolute paths outside root, relative paths that escape via `..`, symlink traversal, and the happy path. |
| RGF-0501 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_summarize_text` function has no unit tests. While simple, it contains a subtle off-by-one boundary (`limit - 1` with appended ellipsis character) and whitespace normalization (`" ".join(value.split())`) that should be validated — especially the boundary where `len(text) == limit` should NOT truncate but `len(text) == limit + 1` should. |
| RGF-0502 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_finalize_blockers_are_commit_resolvable` function has no unit tests despite encoding important policy logic: it determines whether finalize blockers can be resolved by committing. The interaction between `_DIRTY_REVIEW_UNIVERSE_PREFIX`, `_TARGET_DIGEST_DRIFT_REASON`, and `_DIRTY_NON_REVIEW_PREFIX` conditions is complex enough to warrant explicit test cases, especially the edge case where `has_dirty_review_universe` allows the target-digest-drift reason to pass. |
| RGF-0503 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_expired_terminal_decision_count` function has no direct unit tests. It queries the database for expired waived/accepted-risk findings, parsing JSON metadata and date comparisons. The SQL query, the loop over rows with per-finding event lookup, and the interplay with `_is_expired` should be tested — especially the case where `event["metadata"]` is None (line 2383) and the cast to `str` before passing to `_is_expired`. |
| RGF-0504 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_copy_checkpoint_artifacts_to_session_worktree` and `_remove_base_checkpoint_artifacts_before_merge` functions have no unit tests. `_remove_base_checkpoint_artifacts_before_merge` includes a path-traversal safety check (line 2311) that validates `checkpoint_dir` stays inside the repository root before calling `shutil.rmtree`. This security-relevant guard should be covered by tests to prevent regressions. |
| RGF-0505 | fixed_verified | src/review_gauntlet/cli.py | cli-contract | Path traversal: `relative` values from `checkpoint["generated_files"]` are joined into `source` and `destination` without validating that the resolved paths stay within `root` and `session_worktree` respectively. A malformed `generated_files` entry containing `../` segments could read from or write to locations outside the intended directories. The sibling function `_remove_base_checkpoint_artifacts_before_merge` (line 2311) already performs an `is_relative_to` check — the same guard should be applied here for both `source` and `destination`. |
| RGF-0506 | false_positive | src/review_gauntlet/cli.py | cli-contract | The `command_runner` closure on line 1691 captures the local variable `controller` before it is assigned on line 1702. This works at runtime because the closure is only invoked later during `controller.run()`, but it creates an implicit ordering dependency that has no compile-time or static-analysis guard. A refactor that calls `command_runner` earlier (or passes it to something that invokes it eagerly) would silently break with `UnboundLocalError`. Consider defining `command_runner` after the `controller` assignment, or restructuring so the dependency is explicit. |
| RGF-0507 | false_positive | src/review_gauntlet/cli.py | cli-contract | The `ThreadPoolExecutor` is created as a bare object rather than via a `with` statement, and the `finally` block only shuts it down with `wait=True` on the happy path. If a non-`KeyboardInterrupt` exception is raised during `as_completed` iteration (e.g. from a reporter callback), the executor shuts down waiting for all pending futures to complete before propagating the exception. On the `KeyboardInterrupt` path, `cancel_futures=True` is correctly used. Consider also cancelling pending futures on unexpected errors to avoid silently running (and paying for) the remaining cells when the run is already doomed. |
| RGF-0508 | accepted_risk | src/review_gauntlet/run_controller.py | test-evidence | Thread-safety issue: `_interrupted` and `_stop_after_current_step` are plain bool attributes read by `run()` on the main thread and written by `interrupt()` / `request_stop_after_current_step()` from another thread without synchronization. While CPython's GIL makes simple attribute writes atomically visible today, this is an implementation detail — not a language guarantee — and will break under free-threaded Python (PEP 703, available in 3.13+). Use a `threading.Event` for each flag to make the intent explicit and portable. |
| RGF-0509 | false_positive | src/review_gauntlet/run_controller.py | test-evidence | Type inconsistency: `failure.get("reason", ...)` returns `object` (since `failure` is `dict[str, object]`), but the value is passed directly to `_emit` as a keyword arg and later used where a `str` is expected. On line 420, `str()` is correctly applied, but here it's missing. If the stored reason value is not a string, the event payload will carry a non-string reason. |
| RGF-0510 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | Thread-safety issue: `_interrupted` and `_stop_after_current_step` are plain booleans written by `interrupt()` / `request_stop_after_current_step()` (likely called from a signal handler or another thread) and read by `run()` without any synchronisation primitive. While CPython's GIL makes simple attribute writes effectively atomic today, this is an implementation detail, not a language guarantee. Using `threading.Event` (which is designed for cross-thread signalling) would make the intent explicit and remain correct on future runtimes (e.g., free-threaded Python 3.13+). |
| RGF-0511 | accepted_risk | src/review_gauntlet/run_controller.py | cli-contract | `entry.__dict__` on a frozen dataclass works today but is fragile — if `AgentOutputEntry` ever gains `slots=True` (common for frozen dataclasses for memory/speed), `__dict__` will disappear and this will silently produce an empty dict or raise `AttributeError`. Use `dataclasses.asdict` for a future-proof serialisation path. |
| RGF-0512 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | Thread-safety issue: `snapshot()` and `_current_agent_lifecycle()` read multiple mutable instance variables (`_agent_status`, `_agent_step_started_at`, `_agent_timeout_seconds`, `_agent_output_progress`, `_agent_lifecycle`) without any synchronization, while `run()` mutates them from the run loop (potentially a different thread). This can produce inconsistent composite snapshots — e.g., `_agent_status == "running"` but `_agent_step_started_at` is already `None` (cleared at line 390). Consider protecting these fields with a `threading.Lock`, or documenting that `snapshot()` must only be called from specific threads. |
| RGF-0513 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | Potential `AttributeError` if `AgentOutputEntry.__dict__` is used on a frozen dataclass: frozen dataclasses do have `__dict__`, so this works, but it's fragile — if `__slots__` is ever added to the dataclass, `__dict__` disappears and this line raises `AttributeError`. Use `dataclasses.asdict(entry)` for a robust conversion. |
| RGF-0514 | accepted_risk | src/review_gauntlet/run_controller.py | data-validation | In `_current_agent_lifecycle`, the condition on line 264 checks `status == "running"` against the value read from `self._agent_lifecycle.status` (a frozen field). But the outer guard on line 242 checks `self._agent_status != "running"`. These are two different state variables that can diverge: `_agent_status` is the controller-level status while `_agent_lifecycle.status` is set separately. If `_agent_status` is `"running"` but `_agent_lifecycle.status` was set to something else (or vice versa), the quiet-detection logic on line 264 may not trigger when expected. |
| RGF-0515 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | The `_base_dirty_paths` function parses `git status --porcelain` output using a hardcoded offset of 3 (`line[3:]`), which assumes the two-character status code plus a single space. However, `--porcelain` format uses exactly 2 status characters followed by a space, so the offset is correct for standard entries. But for entries with rename tracking (`R  old -> new`), the function splits on ` -> ` which could match literal filenames containing that substring. Consider using `--porcelain -z` with null-terminated output and the `\0` separator for rename pairs to avoid ambiguity. |
| RGF-0516 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | In `merge_session_worktree`, after `_commit_session_changes` succeeds but the `git merge --ff-only` fails, the session worktree has already been modified (files staged and committed). The function returns a `GitWorktreeMergeResult` with `merged=False` but does not roll back the commit made in `_commit_session_changes`. This leaves the session branch in a modified state that may not match what the caller expects for a 'not merged' result, potentially causing issues on retry. |
| RGF-0517 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | In `cleanup_session_worktree`, `git branch -d` is used after `git worktree remove`. If the worktree removal fails (e.g., due to untracked files), the function still attempts to delete the branch. Deleting the branch while the worktree still references it can leave git in an inconsistent state. Consider skipping branch deletion if worktree removal failed. |
| RGF-0518 | false_positive | src/review_gauntlet/git_worktree.py | cli-contract | The `run_worktree_setup` function executes an arbitrary script found at `.wt/setup` inside the worktree. Although it checks `os.access(script_path, os.X_OK)`, this only verifies the execute permission bit — it does not validate that the script is within the expected worktree or prevent path traversal if `worktree_root` is attacker-controlled. Since the callers validate `worktree_root` before calling this function, this is likely safe in practice, but worth documenting the trust assumption. |
| RGF-0519 | false_positive | src/review_gauntlet/git_worktree.py | data-validation | The `_base_dirty_paths` function parses `git status --porcelain` output by slicing at index 3 (`line[3:]`), but the porcelain v1 format uses only 2 status characters followed by a single space (total prefix = 3 chars). Lines shorter than 4 characters are skipped, but a valid porcelain line with a 1-character filename (e.g., `" M x"`) is exactly 4 characters and would yield `entry = "x"` correctly. However, renamed entries (`R  old -> new`) use ` -> ` as delimiter which could also appear in filenames. Consider using `--porcelain=v2` or `-z` (NUL-separated) format for unambiguous parsing, especially since filenames containing ` -> ` would be incorrectly split. |
| RGF-0520 | accepted_risk | src/review_gauntlet/git_worktree.py | data-validation | `merge_preflight_blockers` calls `git merge-tree --write-tree` which requires Git 2.38+. There is no validation or fallback if the host has an older Git version — the function would raise a `GitCommandError` with an unhelpful message. Consider either checking the Git version first or catching the error with a clearer message indicating the minimum Git version requirement. |
| RGF-0521 | false_positive | src/review_gauntlet/git_worktree.py | data-validation | `_validate_session_id` uses a lazy `import re` inside the function body. While this works, it means the `re` module is re-imported on every call. Since `re` is a stdlib module with negligible import cost, it would be cleaner to import it at the module level alongside the other stdlib imports. |
| RGF-0522 | false_positive | src/review_gauntlet/cli.py | data-validation | SQL placeholder count is driven by the size of `finding_ids` with no upper bound. SQLite's default SQLITE_MAX_VARIABLE_NUMBER is 999. If `finding_ids` exceeds this limit (e.g., a session with many findings filtered through `verify-fixes`), the query will fail with an opaque database error. Consider chunking the query or validating the set size. |
| RGF-0523 | fixed_verified | src/review_gauntlet/cli.py | data-validation | `_is_expired` defaults to `False` (not expired) when metadata JSON is malformed. This means a waived or accepted-risk finding with corrupted metadata silently bypasses expiry enforcement, potentially allowing finalization when it should be blocked. A safer default for a gating check would be to treat unparseable expiry metadata as expired. |
| RGF-0524 | false_positive | src/review_gauntlet/cli.py | data-validation | `str(row["latest_cell_id"]) or None` conflates the empty string `''` with a missing value. The SQL coalesce returns `''` for NULL, so this works today, but the idiom is fragile: if the coalesce default or column type changes, the behavior silently breaks. Use an explicit None check instead. |
| RGF-0525 | false_positive | src/review_gauntlet/cli.py | cli-contract | N+1 query pattern: `_expired_terminal_decision_count` fetches all waived/accepted_risk findings in one query, then issues a separate query per finding to retrieve the latest event metadata. For sessions with many waived or accepted-risk findings, this produces O(N) database round-trips. This can be collapsed into a single query using a JOIN with a windowed or grouped subquery on `finding_events`. |
| RGF-0526 | false_positive | src/review_gauntlet/cli.py | cli-contract | Progress event ordering: `cell_start` is emitted before `executor.submit()`, meaning the progress reporter announces all cells as 'started' during the submission loop—even cells that are queued and not yet running. When `len(cells)` exceeds `max_workers`, the user sees N 'started' events followed by a burst of 'success/failure' events, which misrepresents actual execution state. Move the `cell_start` call into the submitted callable or emit it from the `as_completed` consumer to reflect real execution timing. |
| RGF-0527 | false_positive | src/review_gauntlet/cli.py | cli-contract | `_agent_output_tail` interleaves stdout and stderr lines by index position (round-robin: one stdout line, one stderr line), but there is no actual timestamp or ordering information. When one stream has significantly more output than the other, the interleaving creates an inaccurate representation of actual output chronology. The `activity.jsonl` artifact written from this will show misleading sequencing. Consider tracking line arrival order in `read_stream` (e.g., with an incrementing counter) and sorting by that order. |
| RGF-0528 | false_positive | src/review_gauntlet/cli.py | data-validation | Bug: `_is_expired` returns `True` for unparseable metadata JSON, which means corrupted or malformed metadata in a finding event will incorrectly report those findings as expired. This blocks finalization via `_expired_terminal_decision_count`. Fail-safe default should be `False` (non-expired) so that data corruption does not prevent users from finalizing a session. |
| RGF-0529 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Bug: `_normalize_finding_path` rejects empty and whitespace-only paths but does not strip leading/trailing whitespace before passing the value to `normalize_repository_relative_path`. A path like `" src/foo.py "` passes the empty check but the whitespace is carried through into normalization and comparison, causing silent mismatches in path-filter matching. |
| RGF-0530 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Minor: `_cmd_mark` stores empty-string values for `owner` and `until` in metadata when the user does not provide `--owner` or `--until` for waived/accepted-risk findings. While `_is_expired` handles the empty `until` string (line 2407: `if not until: return False`), storing explicit empty strings as metadata pollutes the event log and may confuse downstream consumers that expect absent keys to mean 'not set'. Consider omitting keys with empty values. |
| RGF-0531 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | The `re` module is imported inside `_validate_session_id` on every call. Since this function is invoked on every session creation, the import should be moved to the module level for clarity and to follow the project's existing import style. |
| RGF-0532 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | In `cleanup_session_worktree`, if `git worktree remove` succeeds but `git branch -d` fails, the function reports `cleaned_up=False` with a blocker. A subsequent retry by the caller will attempt `git worktree remove` again on an already-removed worktree, which will raise `GitCommandError` and add a spurious blocker. Consider skipping the worktree remove step if the directory no longer exists, or structuring the cleanup so a retry is idempotent. |
| RGF-0533 | false_positive | src/review_gauntlet/git_worktree.py | test-evidence | The `merge-tree` conflict check uses raw `subprocess.run` instead of the `git()` helper. The helper supports `check=False`, but it only returns stdout and discards the returncode. Consider extending `git()` to optionally return the `CompletedProcess` (or the returncode), so all git invocations go through one path — this would ensure consistent timeout handling and error formatting if those ever change. |
| RGF-0534 | fixed_verified | src/review_gauntlet/cli.py | data-validation | Bug: `_is_expired` returns `True` when metadata JSON is unparseable (corrupted row, encoding issue, etc.). This silently treats the finding as expired, which adds a finalize blocker ('waived or accepted-risk findings have expired') that the user cannot resolve without manual DB surgery. A user who waived a finding and later encounters a corrupt metadata row would be blocked from finalizing with no actionable remedy. The safe default for unparseable metadata is `False` (not expired), since the user made an active decision and we cannot determine it has lapsed. |
| RGF-0535 | false_positive | src/review_gauntlet/cli.py | data-validation | Defensive validation: `review_cells_concurrently` is a module-level public function (not underscore-prefixed) but does not validate `concurrency`. If called with `concurrency <= 0` (e.g., from a programmatic caller or test), `ThreadPoolExecutor(max_workers=0)` raises a `ValueError` with a confusing message. The CLI path is safe because `_positive_int` validates it, but the function signature accepts any int. |
| RGF-0536 | false_positive | src/review_gauntlet/cli.py | data-validation | Subtle truthiness bug: `str(row["latest_cell_id"]) or None` relies on the coalesced SQL default being `''`. If the SQL default or column type ever changes such that the value becomes the literal string `"0"` or another falsy-evaluating conversion artifact, valid cell IDs would silently become `None`. A more explicit check is clearer and resilient to schema evolution. |
| RGF-0537 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | The `_commit_session_changes` function contains security-critical path traversal validation (lines 401-405) but has no direct unit tests. It is only exercised indirectly through CLI integration tests (`test_run_git_worktree_session_changes_can_finalize_merge`), which do not specifically test the path-escape rejection path. A malicious `commit_paths` entry like `../../etc/passwd` should be covered by a dedicated test to ensure the `ValueError` is raised. |
| RGF-0538 | false_positive | src/review_gauntlet/git_worktree.py | test-evidence | `merge_preflight_blockers` calls `subprocess.run` directly (line 270) instead of using the `git()` helper used everywhere else in this module. This bypasses the consistent error wrapping via `GitCommandError` and means the `TimeoutExpired` exception is caught and converted to a string blocker, while `git()` would raise `GitCommandError`. If the `git()` helper's timeout or error-handling behavior is updated in the future, this call site will silently diverge. |
| RGF-0539 | false_positive | src/review_gauntlet/cli.py | data-validation | Pydantic `ValidationError` from `TargetSpec.model_validate()` (called at lines 875, 982, 1523, 1617) is not caught here. If stored session metadata is corrupted, `model_validate` raises `ValidationError`, which is neither `ConfigError`, `ValueError`, nor `LookupError`, so it propagates as an unhandled traceback to the user instead of a clean error message. |
| RGF-0540 | false_positive | src/review_gauntlet/cli.py | data-validation | `args.finding_id` is passed from raw user input to `store.mark_finding()` without any format validation or existence check. If the finding doesn't exist, the behavior depends entirely on the store implementation — it may silently do nothing (an UPDATE that matches zero rows) and then report success in the emitted output, giving the user false confidence that the mark was applied. |
| RGF-0541 | false_positive | src/review_gauntlet/cli.py | data-validation | `_is_expired` catches all parse errors and returns `False`, which means a finding with corrupted metadata JSON silently survives past its intended expiry. A finding that was waived with an `until` date will never be flagged as expired if its event metadata becomes malformed — the finalize gate silently lets it through. Consider returning `True` (fail-closed) or raising so the caller can surface the corruption. |
| RGF-0542 | false_positive | src/review_gauntlet/cli.py | data-validation | The expression `str(row["latest_cell_id"]) or None` relies on Python's truthy evaluation of empty strings. While functionally correct given the SQL `coalesce(o.cell_id, '')`, it is fragile: if the COALESCE default changes or the query is refactored to return `NULL`, `str(None)` produces the string `"None"` which is truthy, so `latest_cell_id` would be set to the literal string `"None"` instead of `None`. |
| RGF-0543 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | The `run_worktree_setup` branch at lines 137-142, where the setup script exists but is not executable (`os.access(script_path, os.X_OK)` returns False), has no direct test. All existing tests either skip because the script is missing, skip because `enabled=False`, or succeed because `_write_setup_script` sets `chmod 0o755`. A test that creates the script without the execute bit would cover this defensive branch. |
| RGF-0544 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | The `_commit_session_changes` early return when `commit_paths` is empty (line 399-400) and the no-op path when `git diff --cached --name-only` returns nothing (line 408-409) lack direct unit tests. The empty-paths case is an important guard that prevents an unnecessary `git add` with no arguments, and the no-staged-changes path prevents empty commits. Both merit explicit test coverage. |
| RGF-0545 | fixed_verified | src/review_gauntlet/git_worktree.py | test-evidence | The `merge_preflight_blockers` path-traversal guard (lines 251-254) is not directly tested. While `cleanup_session_worktree` has explicit traversal tests in `TestCleanupSessionWorktreePathTraversal`, the analogous check in `merge_preflight_blockers` is only covered implicitly (if at all) through integration tests. A dedicated unit test passing a `worktree_path` like `../../escape` to `merge_preflight_blockers` would verify this security boundary directly. |
| RGF-0546 | accepted_risk | src/review_gauntlet/git_worktree.py | test-evidence | Race condition (TOCTOU) in `_unique_session_branch`: between checking that a branch name doesn't exist (`_ref_exists`) and creating the worktree with that branch (`git worktree add -b`), another process can create a branch with the same name, causing a failure. Consider using a retry loop around the `git worktree add` call, or use a lock, to handle concurrent session creation safely. |
| RGF-0547 | false_positive | src/review_gauntlet/git_worktree.py | test-evidence | In `cleanup_session_worktree`, when the worktree directory does not exist (line 323), the code sets `removed = worktree_path` as if cleanup succeeded, but then attempts `git branch -d` which may fail if the branch is not fully merged. This masks the fact that a worktree was already missing (possibly cleaned up externally). Consider logging a warning or adding a blocker note when the worktree directory was already absent, rather than silently treating it as successfully removed. |
| RGF-0548 | false_positive | src/review_gauntlet/git_worktree.py | test-evidence | In `merge_session_worktree`, the path traversal check on line 204 is duplicated — `merge_preflight_blockers` (called on line 193) already performs the same `is_relative_to` check on `worktree_path` at line 252. If the path escapes, preflight blockers will catch it and return early on line 195. The second check on line 204 is unreachable for a path-escape scenario (since preflight would have already returned). Consider removing the redundant check to reduce confusion about which code path actually guards against traversal. |
| RGF-0549 | accepted_risk | src/review_gauntlet/git_worktree.py | test-evidence | The `git` helper uses a default timeout of 10 seconds, but `_commit_session_changes` calls `git add` and `git commit` which can be slow on large repositories or when pre-commit hooks are configured. Consider passing a higher timeout for the `commit` call specifically, or making the timeout configurable at the call site. |
| RGF-0550 | accepted_risk | src/review_gauntlet/cli.py | data-validation | The pattern `str(row["latest_cell_id"]) or None` is fragile and can silently produce the literal string `"None"` instead of Python `None`. Currently the SQL query uses `coalesce(o.cell_id, '')` which masks the issue by converting NULL to empty string. However, if the coalesce is ever removed or the column value is Python `None`, `str(None)` produces `"None"` (a truthy string), so `or None` would not trigger and `latest_cell_id` would be set to the string `"None"`. Use an explicit None check instead. |
| RGF-0551 | accepted_risk | src/review_gauntlet/cli.py | data-validation | The `reason` parameter for `mark_finding` is passed through without any validation or length constraint. While `--until` and `--owner` have empty-string defaults that are filtered by `if v`, the `reason` argument is passed directly to the store with no bounds check. An extremely long string could be stored without any limit. Consider adding a reasonable upper bound on the reason length to prevent accidental or intentional misuse. |
| RGF-0552 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_persist_session_command_artifacts` function (lines 1902-1937) creates run artifact directories, writes stdout/stderr logs, and generates activity JSONL files, but has no direct or indirect unit test coverage. This function contains non-trivial logic (UUID-based directory creation, JSONL serialization of output tail entries, and result object mutation to add artifact paths). A failure here would silently corrupt the audit trail without any test catching it. |
| RGF-0553 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The `_copy_checkpoint_artifacts_to_session_worktree` and `_remove_base_checkpoint_artifacts_before_merge` functions (lines 2305-2330) contain path-traversal safety checks (`is_relative_to`) and filesystem operations (shutil.copy2, shutil.rmtree, unlink), but have no dedicated test coverage. These are security-sensitive functions that guard against directory traversal attacks during merge finalization. The path validation logic and the symlink cleanup at line 2329 should have explicit tests verifying both the happy path and the rejection of malicious paths. |
| RGF-0554 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_finalize_blockers_are_commit_resolvable` function (lines 1643-1653) determines whether finalize blockers can be resolved by committing, which gates whether the ready-prompt system offers a finalize prompt. It has compound logic combining dirty-review-universe detection with target-digest-drift allowlisting, but no unit test exercises this function directly. Incorrect behavior here would cause the ready-prompt loop to either skip finalization (leaving sessions open) or attempt premature finalization. |
| RGF-0555 | false_positive | src/review_gauntlet/cli.py | cli-contract | ThreadPoolExecutor is created outside the try/finally block. If an exception occurs between executor creation (line 1230) and the try block (line 1233), the executor leaks. Using a context manager (`with ThreadPoolExecutor(...) as executor:`) would guarantee cleanup. |
| RGF-0556 | false_positive | src/review_gauntlet/cli.py | cli-contract | The expression `str(row["latest_cell_id"]) or None` is fragile. If the SQL COALESCE is ever removed and the value is SQL NULL, `str(None)` produces the string `'None'` (truthy), so `or None` would be bypassed and you'd get the literal string `"None"` instead of Python `None`. Use an explicit conditional instead. |
| RGF-0557 | false_positive | src/review_gauntlet/cli.py | cli-contract | Catching `ValueError` broadly here can mask programming bugs inside `_run_session_command` (and its callees) that raise ValueError for reasons unrelated to user input. Consider catching a more specific exception type, or at least restricting which call sites are covered by this handler. |
| RGF-0558 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | After killing the subprocess on timeout, the stdout/stderr reader threads are joined with a 1-second timeout. If either thread does not finish within that window (e.g., a large buffered read), it continues running while `stdout_lines`/`stderr_lines` are read under `output_lock`. The lock protects individual appends but not the completeness of the list: the thread can append more lines after the lock is released here, causing the captured output to be silently incomplete or the `''.join()` to race with a concurrent `append`. Consider joining with a longer timeout or setting a shutdown flag that the reader threads check. |
| RGF-0559 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | Setting a private attribute `args._tui_rendered = True` on the argparse Namespace as a side-channel to suppress duplicate output is fragile. If the attribute name collides with a future argparse argument or if `_run_session_command` is called from a context that doesn't check this attribute, output is silently lost. Consider returning a tuple `(result, tui_rendered)` from `_cmd_run` instead. |
| RGF-0560 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | Direct SQL execution to update session state (`update sessions set state = 'finalized'`) bypasses the SessionStore abstraction. If the schema or state machine transitions change, this raw query will silently diverge from the store's intended behavior. Consider adding a `finalize_session(session_id)` method to SessionStore. |
| RGF-0561 | accepted_risk | src/review_gauntlet/cli.py | test-evidence | The `_finding_states` function constructs and executes raw SQL directly in the CLI module rather than delegating to SessionStore. This duplicates the pattern of building parameterized IN clauses and couples the CLI to the database schema. Consider moving this query into a SessionStore method. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 974 | RGF-0497 | untriaged | fixed_pending_verification | Added is_relative_to validation for source and destination paths in _copy_checkpoint_artifacts_to_session_worktree |
| 975 | RGF-0505 | untriaged | fixed_pending_verification | Added is_relative_to validation for source and destination paths in _copy_checkpoint_artifacts_to_session_worktree (same fix as RGF-0497) |
| 976 | RGF-0499 | untriaged | fixed_pending_verification | Added is_relative_to validation for generated_files paths before passing to merge_session_worktree |
| 977 | RGF-0498 | untriaged | false_positive | Cell IDs are UUIDs (RGC-hex format), never falsy strings like '0'. The str() or None pattern works correctly for this domain. |
| 978 | RGF-0506 | untriaged | false_positive | Standard Python deferred closure pattern. The type annotation 'controller: RunController' makes intent explicit. The closure is only invoked via controller.run() which requires controller to be assigned first. |
| 979 | RGF-0507 | untriaged | false_positive | Reporter callbacks are internal code that should not throw. The executor shutdown(wait=True) on non-interrupt exceptions is intentional - it ensures all futures complete before propagating. Cancelling on arbitrary exceptions could leave partial state. |
| 980 | RGF-0501 | untriaged | confirmed | Valid: _summarize_text lacks unit tests for boundary conditions. Tests need to be added in test files (out of scope for this file-scoped agent run). |
| 981 | RGF-0502 | untriaged | confirmed | Valid: _finalize_blockers_are_commit_resolvable lacks unit tests for policy logic. Tests need to be added in test files (out of scope for this file-scoped agent run). |
| 982 | RGF-0500 | untriaged | confirmed | Valid: _resolve_session_cwd security logic lacks unit tests. Tests need to be added in test files (out of scope for this file-scoped agent run). |
| 983 | RGF-0504 | untriaged | confirmed | Valid: checkpoint copy/remove functions lack unit tests for path-traversal guard. Tests need to be added in test files (out of scope for this file-scoped agent run). |
| 984 | RGF-0503 | untriaged | confirmed | Valid: _expired_terminal_decision_count lacks unit tests. Tests need to be added in test files (out of scope for this file-scoped agent run). |
| 985 | RGF-0488 | untriaged | fixed_pending_verification | Added os.access(X_OK) check before executing .wt/setup script |
| 986 | RGF-0494 | untriaged | fixed_pending_verification | Set returncode=-1 in OSError branch for consistency with timeout branch |
| 987 | RGF-0492 | untriaged | fixed_pending_verification | Added is_relative_to path traversal check in merge_preflight_blockers |
| 988 | RGF-0489 | untriaged | fixed_pending_verification | Added path traversal validation in _commit_session_changes for all commit_paths |
| 989 | RGF-0493 | untriaged | fixed_pending_verification | Added path traversal validation in _commit_session_changes (same fix as RGF-0489) |
| 990 | RGF-0495 | untriaged | confirmed | Valid: path-traversal guard in cleanup_session_worktree lacks unit test coverage for security-relevant code |
| 991 | RGF-0496 | untriaged | confirmed | Valid: _git_worktree_metadata has no direct unit tests for edge cases like missing keys, empty strings, non-dict input |
| 992 | RGF-0490 | untriaged | false_positive | Direct subprocess.run is intentional — git() helper does not expose returncode when check=False, which is needed here to distinguish merge-tree conflict from success |
| 993 | RGF-0491 | untriaged | false_positive | Loop is bounded by actual git refs; adversarial pre-creation requires repo write access. session_id is validated to [a-zA-Z0-9_-]+ |
| 994 | RGF-0508 | untriaged | accepted_risk | CPython GIL makes bool read/write atomic. Plain bool interrupt flags are idiomatic Python. threading.Event would add complexity without practical benefit in this single-writer pattern. |
| 995 | RGF-0510 | untriaged | accepted_risk | Same as RGF-0508: CPython GIL ensures atomicity for simple bool flags. This is standard interrupt-flag pattern in Python. |
| 996 | RGF-0512 | untriaged | accepted_risk | snapshot() is a monitoring/status method where eventual consistency is acceptable. Adding a lock around all mutable state reads would add complexity for negligible practical benefit in a status display path. |
| 997 | RGF-0514 | untriaged | accepted_risk | The status variable is read from a frozen dataclass field, then conditionally overridden to 'quiet'. The local rebinding is intentional and correct — no mutation of the frozen dataclass occurs. |
| 998 | RGF-0509 | untriaged | false_positive | Not a real type inconsistency: _emit accepts **payload: object, so passing object is correct. The _run_result calls on L420-421 correctly use str(). The _emit call on L416 passes to EventSink which stores object values. No runtime type error possible. |
| 999 | RGF-0511 | untriaged | accepted_risk | AgentOutputEntry is defined in the same file (L19-23) without slots=True. __dict__ works correctly on frozen dataclasses. The tight coupling within a single file means any future slots=True change would be immediately caught. Using dataclasses.asdict() would add overhead for no practical benefit. |
| 1000 | RGF-0513 | untriaged | accepted_risk | Same as RGF-0511: __dict__ on frozen dataclass without slots=True is correct. The dataclass is co-located in the same file. Hypothetical future breakage from slots=True would be caught immediately. |
| 1001 | RGF-0497 | fixed_pending_verification | fixed_verified | review_verification |
| 1002 | RGF-0489 | fixed_pending_verification | fixed_verified | review_verification |
| 1003 | RGF-0494 | fixed_pending_verification | fixed_verified | review_verification |
| 1004 | RGF-0493 | fixed_pending_verification | fixed_verified | review_verification |
| 1005 | RGF-0492 | fixed_pending_verification | fixed_verified | review_verification |
| 1006 | RGF-0488 | fixed_pending_verification | fixed_verified | review_verification |
| 1007 | RGF-0505 | fixed_pending_verification | fixed_verified | review_verification |
| 1008 | RGF-0499 | fixed_pending_verification | fixed_verified | review_verification |
| 1009 | RGF-0500 | confirmed | accepted_risk | test-evidence: _resolve_session_cwd needs unit tests for path traversal guard. Test files out of scope for this cli.py-only run. |
| 1010 | RGF-0501 | confirmed | accepted_risk | test-evidence: _summarize_text needs unit tests for off-by-one boundary. Test files out of scope for this cli.py-only run. |
| 1011 | RGF-0502 | confirmed | accepted_risk | test-evidence: _finalize_blockers_are_commit_resolvable needs unit tests. Test files out of scope for this cli.py-only run. |
| 1012 | RGF-0503 | confirmed | accepted_risk | test-evidence: _expired_terminal_decision_count needs integration tests. Test files out of scope for this cli.py-only run. |
| 1013 | RGF-0504 | confirmed | accepted_risk | test-evidence: _copy_checkpoint_artifacts and _remove_base_checkpoint_artifacts need unit tests. Test files out of scope for this cli.py-only run. |
| 1014 | RGF-0523 | untriaged | fixed_pending_verification | Changed _is_expired to return True (expired) on malformed metadata JSON. Corrupted metadata should not bypass expiry enforcement. |
| 1015 | RGF-0522 | untriaged | false_positive | SQLite SQLITE_MAX_VARIABLE_NUMBER defaults to 999; sessions with 999+ findings are unrealistic in practice. The chunking overhead is not justified. |
| 1016 | RGF-0524 | untriaged | false_positive | The coalesce-to-empty-string + 'or None' pattern is intentional and correct for the current schema. The SQL is controlled internally, not an external contract. |
| 1017 | RGF-0525 | untriaged | false_positive | N+1 query on waived/accepted_risk findings: these are typically single-digit count per session. The performance impact is negligible for SQLite local DB access. |
| 1018 | RGF-0526 | untriaged | false_positive | cell_start emitted before executor.submit is a design choice for progress reporting. The events are informational; the batched submission pattern is intentional for the ThreadPoolExecutor usage. |
| 1019 | RGF-0527 | untriaged | false_positive | stdout/stderr interleaving by index is a best-effort tail display. There is no timestamp information available from subprocess.PIPE output. The round-robin approach is an acceptable approximation. |
| 1020 | RGF-0515 | untriaged | false_positive | git status --porcelain v1 format is XY+space+path; offset 3 is the documented correct offset. The len<4 guard handles edge cases. |
| 1021 | RGF-0516 | untriaged | false_positive | Correct error handling. Commit preserved on session_branch, merged=False and finalize_blockers reported. Caller can retry. No data loss. |
| 1022 | RGF-0517 | untriaged | false_positive | git branch -d refuses to delete a branch checked out in a worktree. Both errors collected in blockers, cleaned_up=False reported. Correct best-effort cleanup. |
| 1023 | RGF-0518 | untriaged | false_positive | Executing .wt/setup is the intended design. Function validates enabled flag, file existence, and executable permission. Documented setup hook. |
| 1024 | RGF-0519 | untriaged | false_positive | Same as RGF-0515: offset 3 correct for porcelain v1. Quoted paths with special chars would not match .review-gauntlet/ prefix so filtering works. |
| 1025 | RGF-0520 | untriaged | accepted_risk | git merge-tree --write-tree requires Git 2.38+ (Oct 2022). On older git, command fails with misleading conflict blocker but merge is safely blocked. Acceptable given git 2.38 age. |
| 1026 | RGF-0521 | untriaged | false_positive | Lazy import of re is valid Python pattern. Not a data-validation issue. Module cached after first call. |
| 1027 | RGF-0495 | confirmed | fixed_pending_verification | Added TestCleanupSessionWorktreePathTraversal with 4 tests covering path traversal, absolute paths, missing worktree_path, and missing session_branch. |
| 1028 | RGF-0496 | confirmed | fixed_pending_verification | Added TestGitWorktreeMetadata with 7 tests covering missing key, non-dict, disabled, missing required key, empty string, non-string type, and valid input. |
| 1029 | RGF-0523 | fixed_pending_verification | fixed_verified | review_verification |
| 1030 | RGF-0495 | fixed_pending_verification | fixed_verified | review_verification |
| 1031 | RGF-0496 | fixed_pending_verification | fixed_verified | review_verification |
| 1032 | RGF-0528 | untriaged | false_positive | Fail-safe design: returning True for unparseable metadata surfaces corrupted findings for human attention via stderr warning. |
| 1033 | RGF-0529 | untriaged | fixed_pending_verification | Added path.strip() before normalization to handle leading/trailing whitespace in finding path filters. |
| 1034 | RGF-0530 | untriaged | fixed_pending_verification | Filter out None/empty values from metadata dict so only user-provided owner/until are stored. |
| 1035 | RGF-0531 | untriaged | fixed_pending_verification | Moved re import to module level, consistent with existing import style |
| 1036 | RGF-0532 | untriaged | fixed_pending_verification | Added is_dir() guard before worktree remove to make cleanup idempotent on retry |
| 1037 | RGF-0533 | untriaged | false_positive | git() helper returns only stdout; merge-tree and _ref_exists both need returncode, making raw subprocess.run the correct pattern here |
| 1038 | RGF-0531 | fixed_pending_verification | fixed_verified | review_verification |
| 1039 | RGF-0532 | fixed_pending_verification | fixed_verified | review_verification |
| 1040 | RGF-0529 | fixed_pending_verification | fixed_verified | review_verification |
| 1041 | RGF-0530 | fixed_pending_verification | fixed_verified | review_verification |
| 1042 | RGF-0534 | untriaged | fixed_pending_verification | Changed _is_expired to return False on unparseable metadata JSON - corrupted metadata should not block finalization |
| 1043 | RGF-0535 | untriaged | false_positive | CLI validates concurrency via _positive_int; function is internal despite no underscore prefix. ThreadPoolExecutor already raises a clear ValueError for invalid values. |
| 1044 | RGF-0536 | untriaged | false_positive | Cell IDs are RGC-prefixed strings; str() of a valid cell ID is never falsy. The or-None pattern correctly handles empty string from SQL COALESCE. |
| 1045 | RGF-0537 | untriaged | fixed_pending_verification | Added TestCommitSessionChangesPathTraversal test class with 3 tests: path escape via .., symlink escape, and valid path acceptance |
| 1046 | RGF-0538 | untriaged | false_positive | git() helper returns str (stdout) and does not expose returncode. merge_preflight_blockers needs returncode to detect merge conflicts, so direct subprocess.run is the correct approach. |
| 1047 | RGF-0537 | fixed_pending_verification | fixed_verified | review_verification |
| 1048 | RGF-0534 | fixed_pending_verification | fixed_verified | review_verification |
| 1049 | RGF-0539 | untriaged | false_positive | ValidationError is a subclass of ValueError (confirmed via MRO), so it is caught by 'except ValueError' at line 549 |
| 1050 | RGF-0541 | untriaged | false_positive | Returning False for corrupted metadata is the intentional safe default per RGF-0534 fix — treating unparseable metadata as not-expired prevents unrecoverable finalize blockers |
| 1051 | RGF-0542 | untriaged | false_positive | SQL COALESCE guarantees empty string for NULL, so str(row[...]) or None correctly produces None for empty and non-None for populated values; str(None) cannot occur |
| 1052 | RGF-0540 | untriaged | false_positive | finding_id is used only in parameterized SQL queries (no injection risk); non-existent IDs raise LookupError in _transition which is caught at line 552 |
| 1053 | RGF-0543 | untriaged | fixed_pending_verification | Added test_run_worktree_setup_reports_not_executable covering the non-executable script branch at lines 137-142 |
| 1054 | RGF-0544 | untriaged | fixed_pending_verification | Added test_empty_commit_paths_is_noop and test_no_dirty_staged_files_skips_commit covering the early-return paths at lines 399-400 and 407-409 |
| 1055 | RGF-0545 | untriaged | fixed_pending_verification | Added test_merge_preflight_rejects_worktree_path_traversal directly testing the path-traversal guard at lines 251-254 |
| 1056 | RGF-0544 | fixed_pending_verification | fixed_verified | review_verification |
| 1057 | RGF-0543 | fixed_pending_verification | fixed_verified | review_verification |
| 1058 | RGF-0545 | fixed_pending_verification | fixed_verified | review_verification |
| 1059 | RGF-0548 | untriaged | false_positive | Defense-in-depth: the redundant is_relative_to check in merge_session_worktree is intentional. If merge_preflight_blockers is ever refactored or called conditionally, this guard still protects the critical path. |
| 1060 | RGF-0547 | untriaged | false_positive | Idempotent cleanup by design: when the worktree directory is already absent, treating it as removed allows cleanup to proceed to branch deletion. This is intentional behavior, not a bug. |
| 1061 | RGF-0546 | untriaged | accepted_risk | TOCTOU race is theoretical: review-gauntlet sessions are single-process CLI operations. Concurrent session creation for the same session_id does not occur in practice. git worktree add -b failure would surface a clear error. |
| 1062 | RGF-0549 | untriaged | accepted_risk | The git helper operates on a small review-gauntlet worktree with a handful of files and no pre-commit hooks. The 10-second default is sufficient for this use case. |
| 1063 | RGF-0555 | untriaged | false_positive | Executor is created at line 1230, try block starts at 1233, but no exception can occur between those lines (dict literal and bool assignment). The finally block at 1260 always shuts down the executor. No leak exists. |
| 1064 | RGF-0557 | untriaged | false_positive | The broad ValueError catch at line 550 is intentional — it converts user-facing validation errors (invalid session ID, bad path, etc.) into clean CLI failures. Internal programming errors would raise different exception types (TypeError, AttributeError, etc.). |
| 1065 | RGF-0550 | untriaged | accepted_risk | The reason parameter is written to a SQLite TEXT column. Practically, review-gauntlet sessions are agent-driven with controlled inputs. Adding a length limit would be an over-constraint for the current usage pattern. |
| 1066 | RGF-0553 | untriaged | confirmed | str(row) or None pattern is fragile — if COALESCE is removed and value is SQL NULL, str(None) produces 'None' (truthy string). Should use explicit conditional. |
| 1067 | RGF-0556 | untriaged | false_positive | Duplicate of RGF-0553 — same issue on the same line, same code pattern. |
| 1068 | RGF-0554 | untriaged | accepted_risk | Test coverage gap for _finalize_blockers_are_commit_resolvable. The function is exercised indirectly through integration tests of the finalize flow. Adding a unit test is desirable but not blocking. |
| 1069 | RGF-0551 | untriaged | accepted_risk | Test coverage gap for _persist_session_command_artifacts. The function is exercised indirectly through session command integration tests. Adding a dedicated unit test is desirable but not blocking. |
| 1070 | RGF-0552 | untriaged | accepted_risk | Test coverage gap for _copy_checkpoint_artifacts_to_session_worktree and _remove_base_checkpoint_artifacts_before_merge. Path-traversal guards were added in this review cycle and verified through manual inspection. Dedicated tests are desirable but not blocking. |
| 1071 | RGF-0553 | confirmed | fixed_pending_verification | Changed str(row[latest_cell_id]) or None to explicit conditional: str(row[latest_cell_id]) if row[latest_cell_id] else None |
| 1072 | RGF-0553 | fixed_pending_verification | fixed_verified | review_verification |
| 1073 | RGF-0561 | untriaged | accepted_risk | Design concern about SQL in CLI module. Valid refactoring suggestion but not a correctness issue. The query is parameterized and safe. |
| 1074 | RGF-0559 | untriaged | accepted_risk | The _tui_rendered private attribute pattern is an established internal convention in this CLI. While fragile, it works correctly and a refactor would be out of scope for this review. |
| 1075 | RGF-0558 | untriaged | accepted_risk | Thread join timeout of 1 second after subprocess kill. The output_lock prevents data races on the line lists. Worst case is slightly truncated output, which is acceptable for a killed-process scenario. |
| 1076 | RGF-0560 | untriaged | accepted_risk | Direct SQL for session state update in finalize. Valid refactoring suggestion but the finalize flow is tested end-to-end and the state transition is correct. |

## Blockers

None
