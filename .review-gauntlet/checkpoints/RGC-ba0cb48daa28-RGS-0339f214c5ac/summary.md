# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: ba0cb48daa28747ddb34284bbd8211498ff520d5
- Session ID: RGS-0339f214c5ac
- Created at: 2026-06-18T08:46:23.926209Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 34 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0987 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | STDOUT_JSON mode skips the VERDICT_OUTPUT_SIZE_LIMIT_BYTES guard. `_read_output_file` (line 583) enforces the 1 MB cap for FILE_JSON mode, but the STDOUT_JSON branch uses `completed.stdout` directly from `_communicate`, which accumulates chunks without any size bound. A misbehaving adapter can produce arbitrarily large stdout and exhaust memory or inflate stored artifacts without triggering the limit. |
| RGF-0988 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | `_read_cell_file_metadata` reads the file from the current working tree regardless of `review_commit`. The returned `line_count` is then used in `_validated_comments_for_cell` (line 635) to hard-fail any comment whose `end_line` exceeds it. When `review_commit` points to an older commit where the file was longer, valid line references are incorrectly rejected; when the file has since grown, out-of-range references incorrectly pass. The metadata should be derived from the commit being reviewed. |
| RGF-0989 | fixed_verified | README.ja.md | docs-accuracy | README.ja.md は `init --worktree`、`init --git-worktree`、`finalize --merge` フラグを文書化しているが、これらはレビュー対象コミット (db69cfe4) 時点の CLI に存在しない。`--git-worktree` と関連する finalize --merge / cleanup_git_worktree ロジックは Apply: remove-git-worktree-isolation (3764796) で削除済みであり、`--worktree` は元々実装されていない。基本的な使い方セクション (行152-182) のコードブロックに `review-gauntlet init --worktree`、`review-gauntlet init --git-worktree`、`review-gauntlet finalize --merge` が例示されており、行182の説明段落も両フラグの動作を詳述しているが、これらは全て架空の機能の説明である。 |
| RGF-0990 | fixed_verified | README.ja.md | docs-accuracy | コマンドセクション (行219) が「以前のワークスペース diff 既定動作が必要なスクリプトは、明示的に `--worktree` を渡してください」と記述しているが、`--worktree` フラグはレビュー対象コミット時点の CLI に存在しない。また行228の OCR 互換対象マッピングのコードブロックにも `review-gauntlet init --worktree` が記載されており、同様に不正確。 |
| RGF-0991 | false_positive | README.ja.md | docs-accuracy | 「基本的な使い方」セクションのコードブロックから `--worktree` オプションが欠落しています。英語版 README には現在のワークスペース差分をレビューする例（`review-gauntlet init --worktree`）が最初の例として含まれていますが、日本語版ではこのオプションが完全に省略されています。このオプションはユーザーが staged/unstaged/untracked ファイルをレビューする主要なユースケースです。 |
| RGF-0992 | false_positive | README.ja.md | docs-accuracy | 「コマンド」セクションの OCR 互換マッピングのコードブロックに `--worktree` エントリが欠落しています。英語版では `review-gauntlet init` の直後に `# OCR workspace diff review: staged, unstaged, and untracked non-ignored files.` というコメントと `review-gauntlet init --worktree` が含まれています。このエントリが日本語版から省略されているため、OCR 互換の対象マッピングの説明が不完全です。 |
| RGF-0993 | false_positive | README.ja.md | docs-accuracy | 「コマンド」セクションの説明文から、以前のワークスペース差分デフォルトに `--worktree` が必要という重要な注記が欠落しています。英語版では「Scripts that need the previous workspace-diff default must pass `--worktree` explicitly.」と明示されていますが、日本語版ではこの文が省略されているため、デフォルト動作が変更されたことをユーザーが認識できません。 |
| RGF-0994 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | subprocess.TimeoutExpired is not caught in _read_cell_file_metadata. When `timeout=10` is specified in subprocess.run, a TimeoutExpired exception can be raised if the git command takes too long. The except clause only catches CalledProcessError, so TimeoutExpired propagates uncaught as a raw exception instead of being wrapped in a ReviewAdapterError. This breaks the contract that callers can rely on ReviewAdapterError for all git-related failures. |
| RGF-0995 | false_positive | src/review_gauntlet/review_adapter.py | data-validation | The sentinel string used in _expand for brace escaping is constructed by concatenating a null-byte wrapper with 'OPEN'/'CLOSE'. If any variable value injected during substitution itself contains the sentinel prefix '\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00', the final replace pass will incorrectly convert it to a literal brace character, corrupting the expanded output. There is no validation that expanded variable values are free of the sentinel before performing the suffix replacements. |
| RGF-0996 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | Busy-wait loop after stdout thread finishes: the loop joins only `threads[0]`, so once stdout reading ends, every subsequent iteration returns immediately (0-second wait) and spins until stderr finishes. Under a high-volume review this causes 100% CPU usage on the polling thread until the stderr reader drains. |
| RGF-0997 | accepted_risk | src/review_gauntlet/review_adapter.py | data-validation | No file-size guard before reading the entire source file into memory. `file_path.read_bytes()` will load arbitrarily large files; the `VERDICT_OUTPUT_SIZE_LIMIT_BYTES` constant only protects verdict output. A multi-GB file passed as a review cell would exhaust process memory before any size check is possible. |
| RGF-0998 | false_positive | src/review_gauntlet/review_adapter.py | data-validation | `self._review_commit` is interpolated directly into the git-show argument without any format validation. A value such as `HEAD~1 -- /etc/passwd` or a ref containing shell-unsafe characters (even with `shell=False`, git itself interprets the ref) could produce unexpected git behavior or read files outside the intended commit. The field should be validated as a hex SHA or a safe ref name before use. |
| RGF-0999 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | File content is read entirely into memory before the size check, creating an OOM risk. A buggy or malicious adapter could write an arbitrarily large verdict file that exhausts heap memory before the `VERDICT_OUTPUT_SIZE_LIMIT_BYTES` guard on line 614 can reject it. Check the file size via `output_path.stat().st_size` before reading. |
| RGF-1000 | false_positive | src/review_gauntlet/review_adapter.py | data-validation | `assert` is used to guard `process.stdout` and `process.stderr` being non-None. Python's `-O` / `-OO` flags strip all `assert` statements at compile time, so these checks silently disappear in optimised deployments. Replace with an explicit `if`/raise to keep the guard unconditional. |
| RGF-1001 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | `item.get("input", "")` uses a default of `""`, but `dict.get(key, default)` only returns the default when the key is absent — not when the key is present with value `None`. If Pydantic stores `input: None` in the error dict (which can happen for certain validation paths), this expression evaluates to `str(None)` = `"None"` (the 4-char string) and passes it to `_line_column_position` as the source text. The position computed from the tiny "None" string is almost always wrong or `None`, giving a misleading snippet window in the failure report. |
| RGF-1002 | accepted_risk | src/review_gauntlet/review_adapter.py | data-validation | `_line_column_position` validates that `line <= len(lines)` but never checks that `column <= len(lines[line - 1])`. If the error message claims a column number beyond the actual line length, the returned byte offset falls into the following line's content. `_raw_snippet` then centres its window on the wrong location, producing a misleading error snippet. |
| RGF-1003 | accepted_risk | src/review_gauntlet/review_adapter.py | data-validation | TOCTOU race condition in size validation: `stat().st_size` is checked at line 606 before the file is read at line 625. A verdict file that is within the size limit at stat-time but grows between the two calls will bypass the `VERDICT_OUTPUT_SIZE_LIMIT_BYTES` guard. The validated size may not correspond to the content that is ultimately returned and written to `verdict.raw.json`. The fix is to read the raw bytes first, validate their length, then decode — eliminating the window between check and use. |
| RGF-1004 | false_positive | src/review_gauntlet/review_adapter.py | data-validation | STDOUT_JSON mode has no size limit on `completed.stdout`, while FILE_JSON mode enforces `VERDICT_OUTPUT_SIZE_LIMIT_BYTES` via `_read_output_file`. A misbehaving adapter can emit unbounded stdout, consuming unlimited memory without any guard. |
| RGF-1005 | fixed_verified | src/review_gauntlet/review_adapter.py | data-validation | `self._fixtures.get(cell.id) or self._fixtures.get(cell.file_path)` uses truthiness rather than an explicit `None` check. If a fixture intentionally maps `cell.id` to an empty list `[]` (meaning "no comments for this cell"), the falsy empty list causes the expression to fall through to the `cell.file_path` lookup, potentially applying the wrong fixture data. |
| RGF-1006 | false_positive | src/review_gauntlet/review_adapter.py | test-evidence | In `_communicate`, the poll loop only joins `threads[0]` (the stdout reader). When stdout closes before stderr, `threads[0].join(timeout=0.05)` returns immediately every iteration, causing a 100% CPU busy-wait until the stderr thread finishes. No test exercises the case where stderr outlives stdout (e.g. a command that writes nothing to stdout but emits large stderr), so this defect has no test evidence. |
| RGF-1007 | accepted_risk | src/review_gauntlet/review_adapter.py | test-evidence | The `quiet_timeout_seconds` path (`_QuietTimeoutExpired`) has no test coverage through the public adapter API. `test_command_adapter_timeout_failure` covers absolute timeout (`timeout_seconds`) but there is no analogous test that runs a command that produces initial output and then goes silent, triggering the quiet-timeout branch and verifying partial stdout/stderr are saved to artifacts. |
| RGF-1008 | false_positive | src/review_gauntlet/review_adapter.py | cli-contract | Busy-wait when only stderr thread is still alive. The loop condition `any(t.is_alive() for t in threads)` stays True while stderr runs, but `threads[0].join(timeout=0.05)` returns immediately once the stdout thread is already dead. This causes a tight spin loop consuming 100% CPU until stderr finishes or a timeout fires. |
| RGF-1009 | false_positive | src/review_gauntlet/review_adapter.py | cli-contract | STDOUT_JSON mode has no size limit on the verdict, while FILE_JSON mode enforces `VERDICT_OUTPUT_SIZE_LIMIT_BYTES` (1 MB) via `_read_output_file`. A runaway or adversarial subprocess could produce unbounded stdout, causing OOM or excessive memory use before the parse step. |
| RGF-1010 | false_positive | README.ja.md | docs-accuracy | `init --worktree`、`init --git-worktree`、`finalize --merge` の各オプションがドキュメントに記載されていますが、このコミット時点の実際の CLI コードには存在しません。`src/review_gauntlet/cli.py` の `init` サブパーサーには `--from`、`--to`、`--commit`、`--all` しかなく、`finalize` サブパーサーにも `--merge` オプションはありません。ユーザーがこれらのコマンドを実行するとエラーになります。 |
| RGF-1011 | false_positive | README.ja.md | docs-accuracy | 「以前のワークスペース diff 既定動作が必要なスクリプトは、明示的に `--worktree` を渡してください」と「OCR ワークスペース diff レビュー: `review-gauntlet init --worktree`」の記述が、実装されていないオプションを参照しています。このコミット時点の CLI には `--worktree` オプションが存在しないため、この手順を実行するとエラーになります。 |
| RGF-1012 | dismissed | src/review_gauntlet/review_adapter.py | data-validation | TOCTOU (time-of-check-time-of-use) in `_read_output_file`: `stat().st_size` is checked before the file is read, leaving a window where the file could grow between the check and the read. This is inconsistent with the STDOUT_JSON path (lines 325-326) which checks `len(completed.stdout.encode("utf-8"))` *after* the data is already in memory. The stat-then-read pattern provides a false size guarantee and could allow a race-condition write to bypass the size limit. |
| RGF-1013 | dismissed | src/review_gauntlet/review_adapter.py | data-validation | Imprecise comments bypass all line-range validation, including the basic invariant check (`start_line < 1` or `end_line < start_line`). If an imprecise comment carries semantically invalid line numbers (e.g. `start_line=0`), they pass through unchecked and are inserted into the accepted tuple returned to callers. Downstream consumers that read those line numbers without their own guards will encounter out-of-range values. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 1628 | RGF-0989 | untriaged | confirmed | README.ja.md が init --worktree, init --git-worktree, finalize --merge を文書化しているが、これらはレビュー対象コミット(db69cfe4)時点のCLIに存在しない。--git-worktree/finalize --mergeはremove-git-worktree-isolation(3764796)で削除済み、--worktreeは未実装。 |
| 1629 | RGF-0990 | untriaged | confirmed | READMEのコマンドセクション(行219,228)でも--worktreeフラグを参照しているが、このフラグはレビュー対象コミット時点のCLIに存在しない。init --worktreeのOCR互換マッピング例示も不正確。 |
| 1630 | RGF-0987 | untriaged | confirmed | STDOUT_JSON mode uses completed.stdout directly at line 323-327 without any size guard, skipping the 1 MB VERDICT_OUTPUT_SIZE_LIMIT_BYTES check enforced by _read_output_file (line 583). A buggy or malicious adapter returning huge stdout bypasses this limit. |
| 1631 | RGF-0988 | untriaged | confirmed | _read_cell_file_metadata (line 488) reads from the working tree via self._root / cell.file_path regardless of review_commit. The returned line_count is used at line 344 for comment line range validation. If the working tree differs from review_commit, validation uses the wrong line count, causing incorrect acceptance or rejection of adapter comments. |
| 1632 | RGF-0989 | confirmed | fixed_pending_verification | Removed non-existent --worktree, --git-worktree flags and finalize --merge from 基本的な使い方 section; these flags do not exist in CLI at commit db69cfe4 |
| 1633 | RGF-0990 | confirmed | fixed_pending_verification | Removed --worktree reference from コマンド section: deleted the sentence about passing --worktree for workspace diff behavior and removed the init --worktree example from OCR mapping |
| 1634 | RGF-0987 | confirmed | fixed_pending_verification | Added VERDICT_OUTPUT_SIZE_LIMIT_BYTES guard for STDOUT_JSON mode: check stdout byte size before processing, matching the file-mode guard in _read_output_file |
| 1635 | RGF-0988 | confirmed | fixed_pending_verification | Modified _read_cell_file_metadata to read file content from review_commit via git show when self._review_commit is set, rather than always reading from the current working tree |
| 1636 | RGF-0990 | fixed_pending_verification | fixed_verified | review_verification |
| 1637 | RGF-0989 | fixed_pending_verification | fixed_verified | review_verification |
| 1638 | RGF-0991 | untriaged | confirmed | 英語版 README.md 184行目に存在する '# Review the current workspace diff. / review-gauntlet init --worktree' の例が日本語版の「基本的な使い方」セクション（154-170行）から欠落している。実際のドキュメント不整合。 |
| 1639 | RGF-0992 | untriaged | confirmed | 英語版 README.md 265行目に存在する '# OCR workspace diff review: staged, unstaged, and untracked non-ignored files. / review-gauntlet init --worktree' のエントリが日本語版の「コマンド」セクション OCR マッピングコードブロック（213-234行）から欠落している。実際のドキュメント不整合。 |
| 1640 | RGF-0993 | untriaged | confirmed | 英語版 README.md 256-257行目に存在する 'Scripts that need the previous workspace-diff default must pass --worktree explicitly.' という重要な注記が日本語版の「コマンド」セクション説明文（209行）から欠落している。実際のドキュメント不整合。 |
| 1641 | RGF-0991 | confirmed | false_positive | --worktree フラグはコミット db69cfe4 時点の CLI ソースコードに実装されていない。英語版 README も同様に不正確で、前ターンで RGF-0989/0990 として修正済み。欠落ではなく意図的な削除。 |
| 1642 | RGF-0992 | confirmed | false_positive | --worktree フラグはコミット db69cfe4 時点の CLI ソースコードに実装されていない。英語版 README の init --worktree エントリも不正確。意図的な削除。 |
| 1643 | RGF-0993 | confirmed | false_positive | --worktree フラグはコミット db69cfe4 時点の CLI ソースコードに実装されていない。英語版 README の worktree 関連注記も不正確。意図的な削除。 |
| 1644 | RGF-0987 | fixed_pending_verification | fixed_verified | review_verification |
| 1645 | RGF-0988 | fixed_pending_verification | fixed_verified | review_verification |
| 1646 | RGF-0994 | untriaged | confirmed | subprocess.TimeoutExpiredがキャッチされていない。line 510でtimeout=10を指定しているが、exceptブロック(line 514)はCalledProcessErrorのみを捕捉している。git showが10秒超過した場合、TimeoutExpiredがReviewAdapterErrorでラップされず呼び出し元に漏出する。 |
| 1647 | RGF-0995 | untriaged | false_positive | センチネル文字列'\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00'にはNULバイトが含まれており、テンプレート変数値(ファイルパス・ID・設定ファイル由来のプロンプト等)はPOSIX仕様上NULバイトを含めないため、実際の衝突は不可能。理論上のリスクはあるがNULバイトが事実上の保護として機能している。 |
| 1648 | RGF-0994 | confirmed | fixed_pending_verification | Added subprocess.TimeoutExpired handler in _read_cell_file_metadata before CalledProcessError, raising ReviewAdapterError with an informative timeout message. |
| 1649 | RGF-0994 | fixed_pending_verification | fixed_verified | review_verification |
| 1650 | RGF-0996 | untriaged | confirmed | stdout thread (threads[0]) が終了すると threads[0].join(timeout=0.05) が即時返却するため、stderr thread がまだ動いている間はループが 50ms スリープなしでスピンする。実際の CPU 無駄遣いだが正確性への影響はなく、タイムアウトで上限がある。 |
| 1651 | RGF-0997 | untriaged | accepted_risk | コードレビューツールとしてソースファイルを対象とする用途では、任意サイズのファイルをメモリに読み込むリスクは低い。サイズガード追加はメリットに対して複雑性のコストが高い。git show パスも同様に全内容をメモリに読んでいる。 |
| 1652 | RGF-0998 | untriaged | false_positive | subprocess.run はリスト引数で shell=False のためシェルインジェクションなし。引数形式が '{commit}:{path}' なので '--option' が含まれても git はリビジョン仕様として扱い、オプションフラグと誤解しない。また値は内部メタデータ(review_head_commit)由来で直接ユーザ入力ではない。 |
| 1653 | RGF-0996 | confirmed | fixed_pending_verification | threads[0].join(timeout=0.05) を alive スレッドを動的に選択するよう変更: alive = next((t for t in threads if t.is_alive()), None); if alive: alive.join(timeout=0.05) — stdout スレッド終了後のビジーウェイトを解消 |
| 1654 | RGF-0996 | fixed_pending_verification | fixed_verified | review_verification |
| 1655 | RGF-0999 | untriaged | confirmed | read_text() loads the entire file into memory before the size guard at line 614. A buggy or adversarial adapter can produce arbitrarily large output, triggering OOM before the limit is enforced. The fix is to stat the file size first. |
| 1656 | RGF-1000 | untriaged | false_positive | The only call site (line 396) always passes stdout=PIPE and stderr=PIPE to Popen, so process.stdout and process.stderr are guaranteed non-None. Stripping these asserts with -O has no practical effect here. |
| 1657 | RGF-0999 | confirmed | fixed_pending_verification | stat() the file before read_text() to check size without loading content into memory, eliminating the OOM risk for oversized output files |
| 1658 | RGF-0999 | fixed_pending_verification | fixed_verified | review_verification |
| 1659 | RGF-1001 | untriaged | confirmed | item.get('input', '') returns None when key exists with None value, causing str(None)='None' to be passed to _line_column_position instead of ''. Pydantic json_invalid errors likely always provide a string input, but the coding pattern is incorrect and should use 'item.get("input") or ""'. |
| 1660 | RGF-1002 | untriaged | accepted_risk | column bounds check is missing, but the out-of-bounds position value is only used in _raw_snippet where line 746 clamps it via min(start + limit, len(value)). No crash or incorrect behavior beyond a slightly off snippet window. Low practical impact. |
| 1661 | RGF-1001 | confirmed | fixed_pending_verification | input=None のケースで str(None)='None' が _line_column_position に渡される問題を修正。raw_input = item.get('input') で取得後、isinstance チェックで str 以外は '' にフォールバックする実装に変更。 |
| 1662 | RGF-1001 | fixed_pending_verification | fixed_verified | review_verification |
| 1663 | RGF-1003 | untriaged | accepted_risk | アダプター出力ファイルは外部プロセスが一度書き込み後に読まれる設計で並行書き込みは発生しない。理論的なTOCTOU raceは存在するが、実運用上のリスクは無視できる水準。read_text()のOSErrorハンドリングで最悪ケースも安全に処理される。修正するには全ファイルを読んでからサイズ確認が必要になりメモリ安全性チェックの意味が失われる。 |
| 1664 | RGF-1004 | untriaged | false_positive | STDOUT_JSON size limit already implemented at lines 323-331 with VERDICT_OUTPUT_SIZE_LIMIT_BYTES check |
| 1665 | RGF-1009 | untriaged | false_positive | Duplicate of RGF-1004; size limit already enforced in the STDOUT_JSON branch |
| 1666 | RGF-1006 | untriaged | false_positive | Busy-wait already fixed: code uses next(t for t in threads if t.is_alive()) to join the correct alive thread |
| 1667 | RGF-1008 | untriaged | false_positive | Duplicate of RGF-1006; same busy-wait fix is already in place |
| 1668 | RGF-1007 | untriaged | accepted_risk | Missing quiet_timeout test coverage is a test gap; tests/ are excluded from review scope and test addition is deferred |
| 1669 | RGF-1005 | untriaged | confirmed | FakeReviewAdapter uses truthiness check on list; empty [] fixture would fall through to file_path lookup |
| 1670 | RGF-1010 | untriaged | confirmed | --worktree, --git-worktree, --merge options documented in README.ja.md but not present in CLI |
| 1671 | RGF-1011 | untriaged | confirmed | --worktree option in README.ja.md but not in CLI |
| 1672 | RGF-1010 | confirmed | false_positive | README.ja.md does not contain --worktree, --git-worktree, or --merge options at the specified line range; finding based on stale/incorrect code snapshot |
| 1673 | RGF-1011 | confirmed | false_positive | README.ja.md does not contain --worktree reference at lines 219-228; finding based on stale/incorrect code snapshot |
| 1674 | RGF-1005 | confirmed | fixed_pending_verification | Replaced or-chain with explicit is None check at review_adapter.py:67; committed in 89ce9e6 |
| 1675 | RGF-1005 | fixed_pending_verification | fixed_verified | review_verification |
| 1676 | RGF-1013 | open | dismissed | 設計上の意図的な動作: imprecise=TrueのコメントはOCR specに基づきstart_line=0,end_line=0を正規の表現として受け入れる。行番号バリデーションのスキップは設計通り。 |
| 1677 | RGF-1012 | open | dismissed | 実用的には非現実的なTOCTOU: アダプタプロセス終了後にstat()してread_text()する手順で、ローカルファイルシステムの単一ユーザーツールとしてレースウィンドウを悪用する脅威モデルが存在しない。STDOUT_JSONパスとの不整合はあるが実害なし。 |

## Blockers

None
