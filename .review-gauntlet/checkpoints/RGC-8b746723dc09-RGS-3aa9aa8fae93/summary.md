# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: 8b746723dc09b102d1ea7a996baa901ca224d4dc
- Session ID: RGS-3aa9aa8fae93
- Created at: 2026-06-20T01:01:12.027701Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 9 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-1065 | confirmed | src/review_gauntlet/cli.py | test-evidence | test-evidence: `_agent_output_tail` interleaves stdout and stderr lines in a fixed alternating pattern (stdout[i], stderr[j], stdout[i+1], ...) regardless of the actual production order. The resulting `activity.jsonl` entries do not reflect when each line was produced. Agents and humans using this log for causal debugging are misled by the artificial ordering. There is no test that documents this non-chronological behaviour or asserts that the interleave contract is intentional, making this a silent regression risk. |
| RGF-1066 | confirmed | src/review_gauntlet/cli.py | cli-contract | cli-contract: `ready` exits with code 1 for both 'no ready task' (normal empty result) and error states (no active session, which propagates as LookupError→exit 1 via line 681). An agent polling `ready` cannot distinguish 'nothing to do yet' from 'session is missing' without parsing output text, breaking the agent-friendly contract. |
| RGF-1067 | confirmed | src/review_gauntlet/cli.py | cli-contract | cli-contract: `--version` is handled by manually inspecting `argv` before `parse_args()` is called (lines 643-645). As a result, `review-gauntlet --version --unknown-flag` silently prints the version with exit 0 instead of erroring. Agents that probe for the version alongside other flags receive a misleading success signal. Use argparse's built-in `parser.add_argument('--version', action='version')` to enforce consistent argument validation. |
| RGF-1068 | confirmed | src/review_gauntlet/cli.py | data-validation | data-validation: `_validate_date_arg` validates only the regex pattern `\d{4}-\d{2}-\d{2}` without checking calendar correctness. Values like '2024-13-99' or '2024-02-31' silently pass and are persisted as expiry timestamps in finding metadata (used by `mark --until` for accepted_risk and waived states). A malformed expiry date may cause a finding to never expire or expire immediately, silently corrupting the review ledger with no error surfaced to the user. |
| RGF-1069 | confirmed | src/review_gauntlet/run_controller.py | test-evidence | _diagnostic_string_sequence (line 738), _diagnostic_artifact_context (line 747), and _diagnostic_ignored_resolutions (line 755) have no direct unit tests. These functions contain non-trivial type-dispatch logic (Mapping checks, tuple/list guards, cast, item iteration) that is only exercised implicitly through integration-level retry scenarios. A malformed `artifact_context` dict or `ignored_resolutions` list would produce silent 'none' output, making failures harder to diagnose. Direct unit tests for each helper—especially edge cases like non-Mapping artifact_context and malformed resolution entries—would improve confidence in the diagnostic output. |
| RGF-1070 | confirmed | src/review_gauntlet/run_controller.py | data-validation | _int_or_zero silently returns 0 for float inputs (e.g. 3.0 → 0). Lines 309-310 use this helper for `run_count` and `cell_terminal_count` from the status snapshot. If any JSON-serialised status value is a float rather than an int, both fields silently display as 0 instead of the correct count. The guard `isinstance(value, int)` correctly excludes bool, but Python float is not a subtype of int so valid numeric values like 3.0 fall through to `return 0`. Consider adding `elif isinstance(value, float) and value.is_integer(): return max(0, int(value))`. |
| RGF-1071 | dismissed | src/review_gauntlet/run_tui.py | data-validation | _task_display performs an unchecked dict lookup on _ACTION_TASK_DESCRIPTIONS[title], which raises KeyError if title is not a known key. The caller format_task_title_from_action currently only passes titles that exist in the dict, but if a new entry is added to _ACTION_TASK_TITLES without a matching entry in _ACTION_TASK_DESCRIPTIONS, a runtime KeyError will be silently absent during testing and crash the TUI at display time. Defensive use of .get() with a fallback is safer. |
| RGF-1072 | dismissed | src/review_gauntlet/run_tui.py | data-validation | select_active_gate accesses gates[mapped_index - 1] and gates[-1] without bounds checking. _NEXT_ACTION_GATE_INDEX maps known action strings to indices 1-4, and derive_finalize_gates always returns exactly 4 gates, so this is safe today. However, if the function is ever called with a gates tuple that has fewer elements than mapped_index (e.g., a test with a stub or a future refactor that changes gate count), an IndexError will be raised. The function should guard against out-of-bounds access or document its precondition explicitly. |
| RGF-1073 | confirmed | src/review_gauntlet/run_tui.py | test-evidence | calculate_progress_metrics: the `percent` field uses the separate `cell_terminal_count` parameter but the sole test (test_progress_metrics_counts_pending_and_reviewed_only) never passes a nonzero cell_terminal_count, so the percent calculation is completely untested. Given the recent commit 'Proposal: fix TUI coverage ratio labels', this is the most critical gap — any regression in the terminal-count→percent path would go undetected. |
| RGF-1074 | confirmed | src/review_gauntlet/run_tui.py | test-evidence | update_tui_render_state: the flash/change-detection logic (which fields trigger flashes on change, which expire, and the special `_is_activity_value_key` new-entry flash) has zero test coverage. This is stateful, time-sensitive logic with multiple branches — expiry pruning, initialized vs. uninitialized state, and the activity-key shortcut — none of which are exercised by the test suite. |
| RGF-1075 | confirmed | src/review_gauntlet/run_tui.py | test-evidence | format_elapsed_time: the hours branch (`if hours: return f"{hours:d}:{minutes:02d}:{secs:02d}"`) and the non-finite input guard (`0 if not math.isfinite(seconds)`) have no test coverage. The `format_duration` function at line 970 has the same gap. Both are used in the TUI header that users see on every refresh. |
| RGF-1076 | confirmed | src/review_gauntlet/run_tui.py | test-evidence | _count_value is called at every finding/coverage state lookup (30+ call sites) and guards against malformed API responses returning bool, float, or string counts, yet it has no tests. A regression — e.g. accepting `True` as 1 instead of 0 — would silently corrupt all coverage and finding totals in the TUI. |
| RGF-1077 | confirmed | src/review_gauntlet/run_tui.py | cli-contract | The text-mode header status line prepends 'Finalize' to the coverage percentage, making it misleadingly read as a finalize-progress metric rather than a coverage metric. The TUI version (header_status_tui_lines, line 1139) correctly omits this prefix and just shows '{cov.percent}%'. This inconsistency between text and TUI modes causes confusion — users reading the compact/text output will see 'Finalize 45% [████░░░░] 10/22' and think 45% relates to finalization progress, whereas it is actually the review coverage percentage. |
| RGF-1078 | confirmed | src/review_gauntlet/run_tui.py | cli-contract | ProgressMetrics.incomplete and ProgressMetrics.pending receive the same value (both set to `pending`), so the two fields are always identical. The field name `incomplete` implies it counts all non-complete cells (pending + stale), but stale cells are intentionally treated as completed (line 586) and excluded from the pending count. Callers that use `coverage.incomplete` (e.g. derive_finalize_gates line 895, review_detail line 897) would be surprised to learn it equals `coverage.pending` exactly. Either rename `incomplete` to make the identity obvious, or compute it as `total - completed` to reflect its name. |
| RGF-1079 | confirmed | src/review_gauntlet/run_tui.py | cli-contract | The `if cov.stale:` branch in header_status_text (and its mirror in header_status_tui_lines at lines 1143-1145) is unreachable dead code. calculate_progress_metrics always returns ProgressMetrics with stale=0 (the local `stale` variable is initialized to 0 on line 579 and never incremented, per the comment 'stale is intentionally ignored'). The comment on line 1027 correctly documents this, but the guarded code below it will never execute, adding noise. The same dead branch exists in header_status_tui_lines. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 1712 | RGF-1065 | open | confirmed | The alternating stdout/stderr interleaving pattern is synthetic and does not reflect actual temporal output ordering. When stream sizes differ, the pattern is misleading (e.g., 1 stderr line paired with 100 stdout lines creates an interleaving that never occurred). No test evidence verifies this behavior as intentional. |
| 1713 | RGF-1066 | open | confirmed | Verified by code inspection: LookupError (no active session) is caught at cli.py:680 with fail(str(exc), code=1), producing the same exit code 1 as the prompt-is-None branch (SystemExit(1) at line 1092). Callers cannot distinguish normal 'no ready task' from error 'no active session'. |
| 1714 | RGF-1067 | open | confirmed | Confirmed by code inspection: lines 643-645 check argv == ['--version'] only, meaning --version must be the sole argument. Standard argparse add_argument('--version', action='version') would handle the flag in any position and output to stdout with SystemExit(0). This manual approach also misses edge cases like 'review-gauntlet subcommand --version'. |
| 1715 | RGF-1068 | open | confirmed | Confirmed: regex \d{4}-\d{2}-\d{2} accepts invalid calendar dates such as 2023-13-99 or 2024-02-30. Using datetime.date.fromisoformat() or strptime would reject such values. The validation gives false confidence since only format, not calendar validity, is checked. |
| 1716 | RGF-1069 | open | confirmed | Integration test (test_run_controller_retries_no_progress_with_diagnostic) exercises the happy-path call to _prompt_with_no_progress_diagnostic, but the defensive branches of all three helpers are untested: _diagnostic_string_sequence(non-list/non-str) returns empty tuple, _diagnostic_artifact_context(non-Mapping) returns 'none', and _diagnostic_ignored_resolutions items without finding_id are silently skipped. No unit tests cover these code paths. |
| 1717 | RGF-1070 | open | confirmed | isinstance(3.0, int) is False in Python, so _int_or_zero(3.0) returns 0 instead of 3. Lines 309-310 use this helper for run_count and cell_terminal_count from CLI JSON output. If the API ever returns a numeric float (e.g. from a JSON encoder that emits 3.0), the value is silently discarded. The function accepts 'object' but its behavior on floats is neither documented nor guarded. |
| 1718 | RGF-1071 | open | dismissed | format_task_title_from_action is the only caller of _task_display and exhaustively maps all next_required_action values to known string titles via _ACTION_TASK_TITLES.get() with a 'READY TASK' fallback. Every string passed to _task_display is guaranteed to be a key in _ACTION_TASK_DESCRIPTIONS. |
| 1719 | RGF-1072 | open | dismissed | derive_finalize_gates always returns a fixed 4-element tuple (lines 930-935). _NEXT_ACTION_GATE_INDEX values are 1, 2, 3, 4 mapping to indices 0, 1, 2, 3 - all within bounds. gates[-1] is also always valid on a 4-element tuple. No out-of-bounds access is possible given the invariant maintained by the call contract. |
| 1720 | RGF-1073 | open | confirmed | The sole test at test_run_tui.py:182 calls calculate_progress_metrics({'reviewed': 3, 'pending': 2}) without cell_terminal_count and does not assert on percent. The percent computation path with cell_terminal_count > 0 is exercised by the finalized snapshot test but indirectly via dashboard_state; the direct unit-level branch coverage is absent. |
| 1721 | RGF-1074 | open | confirmed | update_tui_render_state has no tests in the test suite. The flash expiry logic, the initialized flag transition, the key-in-previous vs not-in-previous branching, and _is_activity_value_key gating are all untested. |
| 1722 | RGF-1075 | open | confirmed | No direct tests for format_elapsed_time exist. The hours branch (>= 3600 seconds) is never exercised in the test suite. Non-finite input handling (math.isfinite guard at line 618) is also untested. The finalized-snapshot test at line 729 only exercises the MM:SS path (75 seconds = '01:15'). |
| 1723 | RGF-1076 | open | confirmed | _count_value (lines 2228-2242) is called at 30+ call sites to guard against malformed API responses including bool/float/str/None variants, but has no tests. The bool branch returning 0, the non-integer float branch, and the string parse error path are all unexercised. |
| 1724 | RGF-1077 | open | confirmed | header_status_text (line 1023) builds 'Finalize {cov.percent}% {bar} {cov.terminal}/{cov.total}' where 'Finalize' is a hardcoded label prefix. The TUI counterpart header_status_tui_lines (line 1138) omits this prefix, showing just '{cov.percent}% {bar} {cov.terminal}/{cov.total}'. The text-mode prefix makes the coverage count read as a finalization percentage rather than a review coverage metric. |
| 1725 | RGF-1078 | open | confirmed | ProgressMetrics.incomplete (positional arg 5) and ProgressMetrics.pending (positional arg 6) are both set to the same pending variable at lines 594-595. The fields have different semantic contracts: incomplete should count all non-complete cells while pending should count only 'pending'-state cells. Adding a new incomplete state would silently fail to update pending, causing them to diverge without detection. |
| 1726 | RGF-1079 | open | confirmed | cov.stale is always 0 because calculate_progress_metrics sets stale=0 and never modifies it (line 578, comment 'stale is intentionally ignored'). The if cov.stale: branches at lines 1027-1029 (header_status_text) and lines 1142-1143 (header_status_tui_lines) are dead code. Both even have a comment confirming this invariant (line 1026, 1141), yet the branches remain. |

## Blockers

None
