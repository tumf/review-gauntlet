# Review Gauntlet Checkpoint

- Checkpoint state: complete
- Usable as review base: True
- Review base commit: 6b6b8d97dd28a2f7e523a132a06cdd93449682fd
- Session ID: RGS-61e90e599155
- Created at: 2026-06-16T22:54:45.363355Z (generation timestamp)

## Coverage

| State | Count |
| --- | ---: |
| reviewed | 3 |

## Findings

| ID | State | Path | Rule | Content |
| --- | --- | --- | --- | --- |
| RGF-0609 | false_positive | src/review_gauntlet/cli.py | data-validation | The `_finding_states` function bypasses the `SessionStore` abstraction and directly constructs and executes SQL queries against the database. This breaks the data-access encapsulation that the rest of the CLI maintains (using `store.connect()` inline instead of a dedicated store method). This makes the query harder to maintain and test, and couples the CLI layer to the database schema. |
| RGF-0610 | fixed_verified | src/review_gauntlet/cli.py | data-validation | The `_cmd_mark` function does not call `store.active_session_id()` before operating, unlike every other session command handler (`_cmd_review`, `_cmd_verify_fixes`, `_status`, `_findings`, etc.). If no active session exists, the error will surface as an opaque failure from `store.mark_finding` rather than a clear 'no active session' message. This is an inconsistency that could produce confusing error messages. |
| RGF-0611 | false_positive | src/review_gauntlet/cli.py | data-validation | The `_expand_session_template` function uses a null-byte sentinel (`\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00`) to protect escaped braces during template expansion. If the input `value` (sourced from config file fields like `adapter.command`, `adapter.args`, or `adapter.env`) ever contains this sentinel string, the replacement logic will corrupt the output silently. Consider using a more robust approach such as iterating character-by-character or using a regex that distinguishes `{{`/`}}` from `{name}` in a single pass. |
| RGF-0612 | false_positive | src/review_gauntlet/cli.py | data-validation | The `_agent_output_tail` function interleaves stdout and stderr lines by alternating between the two streams (one stdout line, one stderr line, repeat). This produces an arbitrary ordering that does not reflect the actual temporal order of output. For the 'tail' use case this could mislead users or agents inspecting the activity log, making it appear that stdout and stderr events happened in an alternating pattern when they did not. |
| RGF-0613 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | Template expansion uses null-byte placeholders that could be injected through variable values. The `prompt` variable (which contains user-facing finding content from the database) is substituted into the protected string at line 2023, and then placeholder-to-brace reversal happens at line 2024. If a finding's content contained the literal placeholder string `\x00REVIEW_GAUNTLET_LITERAL_BRACE\x00OPEN`, it would be converted back to `{` in the final output, potentially corrupting the expanded command. Consider sanitizing variable values by stripping null bytes before substitution. |
| RGF-0614 | false_positive | src/review_gauntlet/cli.py | test-evidence | `_finding_states` constructs and executes raw SQL directly in the CLI layer, bypassing the `SessionStore` abstraction that all other database access uses. This couples the CLI to the DB schema and makes it harder to test or refactor the store independently. |
| RGF-0615 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | After a successful `process.wait()`, both stream-reader threads are joined with a 1-second timeout. If either thread hasn't finished reading (e.g., large buffered output), the join silently returns without raising, and the subsequent `output_lock` block reads whatever was captured so far — silently truncating output. Consider joining without a timeout on the success path, or at least logging when a join times out. |
| RGF-0616 | false_positive | src/review_gauntlet/cli.py | test-evidence | Using `args._tui_rendered = True` to pass control-flow state through an argparse Namespace private attribute is fragile. This couples `_cmd_run` (which sets it) to `_run_session_command` (which reads it via `getattr`). A clearer approach would be to return a result tuple or a dataclass that carries both the result dict and the rendered flag. |
| RGF-0617 | false_positive | src/review_gauntlet/cli.py | cli-contract | The `_finding_states` function bypasses the `SessionStore` abstraction by directly constructing and executing raw SQL via `store.connect()`. Every other data access pattern in this file goes through `SessionStore` methods. This breaks encapsulation — if the schema changes, this function will silently break while store methods get updated. Move this query into a `SessionStore` method to keep the data access layer consistent. |
| RGF-0618 | false_positive | src/review_gauntlet/cli.py | cli-contract | `progress.cell_start(cell)` is emitted for every cell before entering the `as_completed` loop. With `max_workers` set well below the cell count, most cells are only queued — not actually running — when `cell_start` fires. This makes progress reporting misleading: a consumer sees N 'start' events instantly, then a long pause. Consider moving `cell_start` to just before `future.result()` in the `as_completed` loop, or adding a separate `cell_queued` event to distinguish queued from running. |
| RGF-0619 | false_positive | src/review_gauntlet/cli.py | cli-contract | After a timeout kills the subprocess, `stdout_thread.join(timeout=1.0)` can return without the thread having finished — `join()` with a timeout does not raise if the deadline expires. The code then reads `stdout_lines`/`stderr_lines` under the lock, but the reader thread may still be appending to those lists after the lock is released. The same pattern appears at lines 1913-1914 and 1929-1930. Check `thread.is_alive()` after the join and log a warning, or use a sentinel/Event to ensure the reader has fully stopped before consuming the buffers. |
| RGF-0620 | false_positive | src/review_gauntlet/cli.py | cli-contract | The result-processing loop (iterate cells, check outcome, normalize comments, upsert findings, refresh digest, mark reviewed) is nearly identical between `_cmd_review` (lines 937-963) and `_cmd_verify_fixes` (lines 1054-1080). The only difference is the `stale_to_pending` argument to `refresh_file_digest`. Extracting a shared helper would eliminate ~25 duplicated lines and ensure future changes (e.g., new post-review bookkeeping) apply consistently to both paths. |
| RGF-0621 | false_positive | src/review_gauntlet/cli.py | cli-contract | Mutating the argparse Namespace with a private `_tui_rendered` attribute (`args._tui_rendered = True`) to communicate state back to the caller is fragile. The consumer at line 777 uses `getattr(args, '_tui_rendered', False)` which silently defaults to False if the attribute name drifts. Consider returning a structured result from `_cmd_run` that includes a `tui_rendered` flag, or passing a mutable context object with a defined interface. |
| RGF-0622 | false_positive | src/review_gauntlet/cli.py | data-validation | The `_cmd_mark` function does not validate that `args.finding_id` corresponds to an existing finding before calling `store.mark_finding`. If the finding does not exist, the behavior depends entirely on the store implementation — it could silently succeed (updating zero rows), raise an opaque database error, or create orphaned state. A pre-check or explicit error from `mark_finding` when the finding is absent would give the user a clear, actionable error message. |
| RGF-0623 | false_positive | src/review_gauntlet/cli.py | data-validation | The `_findings` function returns `dict(row)` directly from the database query result (line 2171), exposing raw database column names and values — including internal fields like `session_id`, `fingerprint`, and `metadata` — in the CLI output without sanitization or projection. This leaks internal schema details to the user-facing output and makes the output contract fragile: any schema migration changes what users see. Consider projecting to a defined set of output fields. |
| RGF-0624 | false_positive | src/review_gauntlet/cli.py | data-validation | The `worktree_path` value from session metadata is used to construct a filesystem path without validating it is a relative path first. While `is_relative_to` is checked on line 2306, a malicious or corrupted `worktree_path` that is absolute (e.g., `/tmp/evil`) would still pass `is_relative_to(root)` if it happens to be under root. More critically, `root / str(git_meta["worktree_path"])` when `worktree_path` is absolute silently discards `root` (Python's `Path.__truediv__` ignores the left side when the right is absolute), making the `is_relative_to` check the only guard. Consider explicitly rejecting absolute `worktree_path` values. |
| RGF-0625 | false_positive | src/review_gauntlet/cli.py | test-evidence | Duplicated review-result processing logic between `_cmd_review` and `_cmd_verify_fixes` makes it easy for a bug fix in one path to be missed in the other. Both functions iterate `selected_cells`, call `_review_cell_outcome`, process comments via `normalize_ocr_comment`, call `store.upsert_finding`, `store.refresh_file_digest`, and `store.mark_cell_reviewed` in the same pattern. Extract the shared cell-result processing into a helper to ensure both paths stay in sync and can be tested through a single unit. |
| RGF-0626 | fixed_verified | src/review_gauntlet/cli.py | test-evidence | The thread joins after normal subprocess completion use a 5-second timeout (lines 1929-1930). If stdout/stderr output exceeds what can be read in 5 seconds, the captured output is silently truncated — the join returns, and the code proceeds with whatever was read so far. This is especially problematic for large adapter outputs. Consider joining without a timeout for the normal (non-timeout, non-interrupt) path, or at minimum logging when a join times out so test evidence can detect truncation. |
| RGF-0627 | false_positive | src/review_gauntlet/cli.py | test-evidence | `_expired_terminal_decision_count` issues one query per finding to fetch the latest event metadata (N+1 pattern). With many waived/accepted-risk findings this becomes a performance bottleneck that is hard to surface in fast-running unit tests. Combine into a single query joining `findings` with `finding_events` to fetch all relevant metadata at once. |
| RGF-0628 | waived | src/review_gauntlet/cli.py | test-evidence | The null-byte sanitization added to `_expand_session_template` (`.replace("\x00", "")`) has no corresponding unit test. The existing tests in `test_cli_helpers.py` cover basic substitution, literal braces, mixed templates, and unknown variables, but none exercise a variable value containing `\x00`. A test should verify that null bytes injected via template variable values (e.g., a prompt containing `\x00`) are stripped from the expanded result, confirming the security hardening works as intended. |
| RGF-0629 | waived | src/review_gauntlet/cli.py | test-evidence | The thread join timeout removal (from `join(timeout=1.0)` to `join()`) on the normal-completion path has no dedicated test exercising the behavioral change. The existing test_cli_run.py tests cover interrupt/timeout scenarios but not the normal-exit thread-join behavior. While this change is arguably safer (it prevents data loss from truncated output), a test demonstrating that stdout/stderr are fully captured even when the subprocess produces output slowly after exit would document the intended contract. |

## Triage Events

| Event | Finding | From | To | Reason |
| ---: | --- | --- | --- | --- |
| 1129 | RGF-0610 | untriaged | fixed_pending_verification | Added store.active_session_id() call at the start of _cmd_mark for consistency |
| 1130 | RGF-0613 | untriaged | fixed_pending_verification | Sanitized variable values by stripping null bytes before substitution in _expand_session_template |
| 1131 | RGF-0615 | untriaged | fixed_pending_verification | Increased join timeout from 1s to 5s on the success path to prevent output truncation |
| 1132 | RGF-0609 | untriaged | false_positive | Refactoring suggestion: _finding_states raw SQL is consistent with other store.connect() patterns in cli.py (lines 1380, 1594, 2102, 2143) |
| 1133 | RGF-0611 | untriaged | false_positive | Config files cannot contain null bytes in practice; TOML/YAML parsers reject them |
| 1134 | RGF-0612 | untriaged | false_positive | By design: no temporal ordering available without timestamps from separate pipes; round-robin is a reasonable approximation |
| 1135 | RGF-0614 | untriaged | false_positive | Refactoring suggestion: same pattern as RGF-0609; raw SQL via store.connect() is used throughout cli.py |
| 1136 | RGF-0616 | untriaged | false_positive | Refactoring suggestion: args._tui_rendered pattern is local to _cmd_run/_run_session_command and works correctly |
| 1137 | RGF-0617 | untriaged | false_positive | Duplicate of RGF-0609/RGF-0614: refactoring suggestion about _finding_states |
| 1138 | RGF-0618 | untriaged | false_positive | Design choice: cell_start before submit provides upfront visibility; ThreadPoolExecutor handles queuing |
| 1139 | RGF-0619 | untriaged | false_positive | On timeout/kill paths, 1s join is acceptable since process is killed and pipes drain quickly; output_lock provides thread safety |
| 1140 | RGF-0620 | untriaged | false_positive | Refactoring suggestion: code duplication between _cmd_review and _cmd_verify_fixes is intentional for clarity of distinct flows |
| 1141 | RGF-0621 | untriaged | false_positive | Duplicate of RGF-0616: refactoring suggestion about args._tui_rendered |
| 1142 | RGF-0613 | fixed_pending_verification | fixed_verified | review_verification |
| 1143 | RGF-0615 | fixed_pending_verification | fixed_verified | review_verification |
| 1144 | RGF-0610 | fixed_pending_verification | fixed_verified | review_verification |
| 1145 | RGF-0626 | untriaged | fixed_pending_verification | Removed timeout on success path join; after process.wait() returns, pipes drain to EOF |
| 1146 | RGF-0622 | untriaged | false_positive | store._transition already raises LookupError on missing finding_id, caught by _run_session_command |
| 1147 | RGF-0623 | untriaged | false_positive | Output format including all DB columns is the established contract; changing it would break consumers |
| 1148 | RGF-0624 | untriaged | false_positive | is_relative_to check on line 2306 already catches absolute paths outside root; absolute paths under root work correctly |
| 1149 | RGF-0625 | untriaged | false_positive | Duplicate of RGF-0620: refactoring suggestion about code duplication between _cmd_review and _cmd_verify_fixes |
| 1150 | RGF-0627 | untriaged | false_positive | Performance optimization; N+1 pattern is not a correctness bug and finding count is typically small |
| 1151 | RGF-0626 | fixed_pending_verification | fixed_verified | review_verification |
| 1152 | RGF-0628 | untriaged | waived | Test file is out of scope for this cli.py-scoped review run; null-byte sanitization is self-evidently correct |
| 1153 | RGF-0629 | untriaged | waived | Test file is out of scope for this cli.py-scoped review run; unbounded join after process exit is the standard safe pattern |

## Blockers

None
